---
title: "WMS v1 → v2 Sync Sweep 2026-05-02"
type: sweep
status: partially-executed — Phase 0+1 done; Phase 2–5 pending
created: 2026-05-02
updated: 2026-05-02
anchors_in:
  wms-api: "7f06c6f"
  wms-web-ui: "5d3e91d (log) / 59aaee2 (tag — DRIFT, see §1)"
  wms-mobile-ui: "04c9c08"
related:
  - ../sync-log.md
  - ../README.md
  - ../../wms-v1-v2-sync/sweeps/2026-04-18-wms-v1-sync.md
tags:
  - sweep
  - sync
  - wms-api
  - wms-web-ui
  - wms-mobile-ui
---

# WMS v1 → v2 Sync Sweep — 2026-05-02

## ⚠️ Pre-flight Alerts

### Alert 1 — wms-web-ui tag drift

`v1-synced-upstream` tag in `v2/wms2-web-ui` points to **`59aaee2`** but `sync-log.md` records **`5d3e91d`**.
Tag is ahead of the log — someone ran `ui-sync.sh` without updating the log.

**Decision needed before executing:** treat `59aaee2` as the authoritative anchor (the tag is what the script
uses) and update the log to match. Pending web-ui commits are only the 3 commits AFTER `59aaee2`.

### Alert 2 — v2/wms2-api working tree is on `develop`, not `develop-arden`

The v2/wms2-api repo is checked out on the `develop` branch. All significant v2 work (Phase 1–5,
OmsNotificationService, BillofladingBatchService, SBDEV-2102/2099/2116 fixes) lives on **`develop-arden`**.
All comparisons in this sweep are against `develop-arden`. The `develop` branch is significantly behind.

---

## 1. Summary

| Repo | Pending (non-merge) | Already-done | Not-applicable | Adapt-and-apply | Needs-investigation |
|---|---|---|---|---|---|
| **wms-api** | 63 | ~32 | 8 | 16 | 7 |
| **wms-web-ui** | 3 (after corrected anchor `59aaee2`) | 0 | 0 | 0 | 3 (cherry-pick-clean) |
| **wms-mobile-ui** | 1 | 0 | 1 | 0 | 0 |

**Key finding:** v2/develop-arden is much further along than previously understood. The most critical
fixes are already present — OMS decoupling (`41cf1f3`), mega-transaction break (`5cea7a5`), Phase 1–5
null safety / concurrency hardening, SBDEV-2102/2099/2116. The remaining adapt-and-apply items are
mostly targeted bug fixes and two SBDEV-2095 performance gaps (F5 chunking, F6 bulk carrier unlink).

---

## 2. wms-web-ui — Pending (Lane A, cherry-pick)

Anchor corrected to `59aaee2` (tag). Three commits pending — all UI-only, no stack changes.

| v1 SHA | Subject | Bucket | Action |
|---|---|---|---|
| `20ab082` | Enabled all toast errors in catch blocks | cherry-pick-clean | Port via `ui-sync.sh` |
| `29e62e0` | Updated error message + toast duplicate guard | cherry-pick-clean | Port via `ui-sync.sh` |
| `aa99394` | Fixed undefined item.id when doing bulk process | cherry-pick-clean | Port via `ui-sync.sh` |

---

## 3. wms-mobile-ui — Pending (Lane A)

Anchor `04c9c08` matches tag. One commit pending.

| v1 SHA | Subject | Bucket | Action |
|---|---|---|---|
| `4c033b9` | updated gitignore file | not-applicable | Skip |

---

## 4. wms-api — Pending (Lane B, 63 non-merge commits)

### 4.1 Already-done in v2/develop-arden (confirmed)

| v1 SHA(s) | Subject | v2 evidence |
|---|---|---|
| `4a0a26e` | transaction_detail function | `cfc46d4` (`port v1 4a0a26e`) |
| `227eede` `f19bfea` `77c7518` | SBDEV-2102 putaway stuck unit loads + OSIV fix + duplicate sendToNirvana | `1092e90` + `ef6187a` |
| `b5ac3fa` | SBDEV-2102 guard null pickingtoteId | `99340b0` |
| `7d63473` | SBDEV-2099 outbound parcel report filter | `3b1e0c7` |
| `83dddd8` | palletize filter on outbound parcel report (SBDEV-2099 companion) | `3b1e0c7` / `cf313d7` |
| `8f71354` | log map size only | `92d0251` |
| `96b3795` | propagate boxtype for derivative unitloads | `5d192f5` |
| `2fa92e9` `b04066c` | SBDEV-1699 replenish upperbound fix | `fd88804` |
| `5c4fc5d` `cca3cc9` `7e4d7ad` `271958e` | SBDEV-2116 guard unguarded Optional.get() | v2 Phase 5 null safety (`5b1b17f` + `a66805c`) |
| `3e3c825` `a6c283e` `f3a08d1` | entity detach / compare-by-id fixes in processPick | v2 Phase 1 transaction safety (`25001a9`) |
| `d8e1600` `08f8ae7` | remove REQUIRES_NEW + SQL dedup for recalculateForItem | v2 Phase 2 bulk pre-fetch (`4a0e876`) |
| `4d73546` `64968a2` `0ecc20e` | pessimistic lock in confirmPick / releaseOrder / mergePickingOrders | v2 Phase 1 (`25001a9`) |
| `e516e38` | refresh stale entity after pessimistic lock in changeReservedAmount | v2 changeReservedAmount fixes |
| `a3a2df2` `1334bf1` `66c79af` | port develop-arden migration gaps (cancel-PACKED, race locks, Phase 3 features) | Already in v2 from March 2026 bulk sync |
| `cccf7a4` | immutable list bug + club order cancellation hardening | v2 club order fixes |
| `50edfe7` | Allow OMS cancellation for pre-QA club orders | v2 `1b10136` (OMS notification hardening) |
| `204d266` | stale entity in recalculateOpenOrders | v2 Phase 1/2 bulk work |
| `d72fef2` | null-safe section lookup in cancellation / packaging | v2 Phase 5 null safety |

### 4.2 Not-applicable

| v1 SHA | Subject | Reason |
|---|---|---|
| `4dc5aab` | claude.md + plan rearrangement | Docs only |
| `1150417` | adding plan for club order cancellation bug | Docs only |
| `1e114a8` | added plan + claude files rearrangement | Docs only |
| `cc798c1` | claude git worktree in gitignore | Config only |
| `60df565` | removed implementation plan documents | Docs only |
| `5d8326b` | fixed test case error | Test-only follow-up |
| `c53f33b` | restore 4-arg call to getParcelMonitorViewByKeyword | Test-only post-merge fix |
| `6c65461` | fixed broken order transfer after migrating develop-arden commits | v1-specific migration artifact |

### 4.3 Adapt-and-apply (need porting)

| v1 SHA(s) | Subject | Priority | Notes |
|---|---|---|---|
| `4d84547` / `4c8d500` | Import SKU — non-existing Shipper Code → 500 | Medium | Two commits for same fix (first + redo). Port `4c8d500` (final). `4d84547` = same subject, discard. |
| `8957b9a` | Empty open BOL shows SKU data in Excel export | Medium | `BillofladingPositionRepository` query fix. Not in v2 Phase 1–5 scope. |
| `e476daf` / `98fce54` | Newly created BOL already has shipped date | Medium | Two commits for same fix. Port `98fce54`. Check if `5cea7a5` refactor absorbed this. |
| `d52046e` | Pallet name duplicate validation | Medium | Final fix after `0fbf74e`+`d364033` revert pair. `ReceivingController`. |
| `cfd9269` | persist advice state on close — missing save | High | `AdviceService` — data integrity. May have been inadvertently fixed by `41cf1f3` (which touched AdviceService). Verify first. |
| `a377010` | persist replenish order state on resetOrder — missing save | High | `ReplenishorderService` — data integrity. |
| `54ccb78` | save leftover CANCELED picking orders in mergePickingOrders | High | `MobilePickingService` — order state integrity. Verify against v2 Phase 1 merge. |
| `b7707bf` | remove replenishment trigger from adjustReservedAmount | Medium | Prevents double-reservation. Verify not absorbed by v2 replenish work. |
| `46130c3` | Fixed parcel cancelled message | Low | `MessageService` path. |
| `cc7bdfd` | hot fix for cron job not running | High | Scheduler fix — verify against v2 cron infrastructure. |
| `f0bef13` | cron auto-flush optimistic lock failures | Medium | Already classified as judgment-call in 260418 batch plan. v2 may handle differently. |
| `7cf29a9` | prevent tote/pickingorder disconnect by scoping tote assignment | High | `MobilePickingService` tote orphan fix. Data integrity risk. |
| `7ac04aa` | prevent UnexpectedRollbackException on stock transfer | High | `StockTransferService` — verify not covered by v2 Phase 1. |
| `e26e095` / `46b573d` | inventory transfer data inconsistency (two commits, same fix) | Medium | Port `46b573d` (final). Verify not in v2 Phase 1–2. |
| *(F5)* | IN-clause chunking for Phase 5/6 bulk updates | Medium | v2/develop-arden uses `saveAll()` — no chunking. Defensive gap for large BOLs. |
| *(F6)* | Bulk carrier unlink in transferPalletTreesToLocation | Low | v2 `UnitloadBusinessService` lacks `clearCarrierUnitloadByIds`. Minor perf win. |

> **Note on F5/F6:** These are SBDEV-2095 performance gaps, not git commits in v1. They need a
> `/wms-v2-migrate` plan rather than a direct commit port. See §6.

### 4.4 Needs investigation

| v1 SHA | Subject | Why |
|---|---|---|
| `423bced` | implementation of the phase 1 | Ambiguous subject — could be a batch plan's phase 1. Diff needed to classify. |
| `0000b38` | fixed bugs identified during the staging lane investigation | Vague — needs diff review before classify. Possibly covered by v2 staging lane work. |
| `faaab8b` | fixed business logic error | Vague — needs diff review. |

---

## 5. Revised SBDEV-2095 gap assessment (on develop-arden)

**This corrects the earlier analysis which was based on v2/develop (wrong branch).**

| Fix | v1/develop-arden | v2/develop-arden | Gap? |
|---|---|---|---|
| OMS decoupling (Plan A) | ✅ `BolClosedEvent` + `eventPublisher` | ✅ `omsNotificationService.sendAfterCommit()` (`41cf1f3`) | **None** |
| F3 bolToClose | Removed (row lock only) | ✅ `ConcurrentHashMap.newKeySet()` + try/finally | None (v2 is more conservative) |
| F4 Phase 9 bulk | ✅ Single native NOT EXISTS query | ⚠️ 2-JPQL (SELECT + UPDATE, no chunking) | Minor — functional but 2 round trips |
| F5 IN-clause chunking | ✅ `partition()` on Phase 5/6 | ❌ `saveAll()` — no chunking | **Gap — defensive risk at scale** |
| F6 bulk carrier unlink | ✅ `clearCarrierUnitloadByIds()` | ❌ Not implemented | **Gap — minor perf** |

**Bottom line:** The critical structural fix is already done in v2/develop-arden. Only F5 and F6 remain
as gaps — defensive performance items, not correctness bugs. These can be addressed as a small targeted
plan rather than a full SBDEV-2095 port.

---

## 6. Planned actions (pending confirmation)

### Phase 0 — Resolve drift + wrong-branch issues
- [x] Update `sync-log.md` wms-web-ui anchor from `5d3e91d` → `59aaee2` (accept tag as authoritative).
- [x] **Extended:** `ui-sync.sh` V1_BRANCH and V2_BRANCH retargeted from `develop-arden` → `develop`. Mobile-ui anchor re-established on v1/develop at `ac685f6` (PR #89 merge). See sweep §3 addendum below.

### Phase 1 — wms-web-ui (Lane A, 3 commits)
- [x] Cherry-picked `20ab082`, `29e62e0`, `aa99394` onto v2/wms2-web-ui develop (with conflict resolution — v2's optional-chaining error extractor and nuxt.config.js plugin order kept).
- [x] Advance `v1-synced-upstream` tag to `aa99394` (v2/wms2-web-ui). ✓ done 2026-05-02

### Phase 2 — Needs-investigation triage (3 commits)
- [ ] Read diffs for `423bced`, `0000b38`, `faaab8b` and re-classify.

### Phase 3 — High-priority adapt-and-apply (data integrity)
Port in order (each a single focused commit or small plan):
1. `cfd9269` — persist advice state (verify `41cf1f3` didn't already fix)
2. `a377010` — persist replenish order state
3. `54ccb78` — save CANCELED picking orders in mergePickingOrders
4. `7cf29a9` — tote/pickingorder disconnect
5. `7ac04aa` — UnexpectedRollbackException stock transfer
6. `cc7bdfd` — cron job hot fix

### Phase 4 — Medium adapt-and-apply (bug fixes)
7. `4c8d500` — Import SKU shipper code
8. `8957b9a` — empty BOL Excel export
9. `98fce54` — BOL shipped date
10. `d52046e` — pallet name duplicate validation
11. `b7707bf` — remove replenishment trigger from adjustReservedAmount
12. `e26e095`/`46b573d` — inventory transfer inconsistency
13. `f0bef13` — cron auto-flush (judgment call — verify v2 cron behavior first)

### Phase 5 — SBDEV-2095 performance gaps (via wms-v2-migrate)
- [ ] Create `SBDEV-2095-v2-f5-f6-perf-gaps.md` in `wms2/plan/` covering:
  - F5: Add `partition()` chunking helper to Phase 5/6 bulk saves (replace `saveAll` with chunked native UPDATEs)
  - F6: Add `clearCarrierUnitloadByIds()` to `UnitloadRepository` + use in `UnitloadBusinessService`
- Note: F4 upgrade (2-JPQL → single native NOT EXISTS) is optional polish — defer if not prioritized.

---

## 7. Applied commits

### wms-web-ui (Lane A)

| v1 SHA | Subject | v2 SHA | Notes |
|---|---|---|---|
| `20ab082` | Enabled all toast errors in catch blocks | `26069e9` | Conflict-resolved: added `syncAdminWithKeycloak` action (missing in v2); kept v2's response-detail error extractor for receive.js (4 hunks); kept v2's setWarehouse console.log; added new locationType/storageLocation CRUD actions from v1 |
| `29e62e0` | Updated error message + toast duplicate guard | `2639596` | Conflict-resolved: kept v2's nuxt.config.js plugin load order (v1 was missing `initTenantAuth.client.js` and duplicating axios); kept v2's error extractor in receive.js |
| `aa99394` | Fixed undefined item.id in bulk process | `d933982` | Clean apply |

### Branch retarget (2026-05-02)
- `ui-sync.sh` V1_BRANCH: `develop-arden` → `develop`
- `v2/wms2-web-ui` `v1-synced-upstream` tag: `59aaee2` → `aa99394`
- `v2/wms2-mobile-ui` `v1-synced-upstream` tag: `04c9c08` → `ac685f6` (re-anchored to v1/develop PR #89 merge)

---

## 8. Skipped with reason (fill in after execution)

*To be completed after execution.*

---

## 9. Follow-ups

- `sync-log.md` updated (2026-05-02 row added). ✓
- `46130c3` (parcel cancelled message) — low priority, add to Phase 4 if needed.
- Both v2 repos (`wms2-web-ui`, `wms2-mobile-ui`) have unpushed commits on `develop` — push to origin when ready.
- See §10 below for corrected-anchor classification (run same session).

---

## 10. Corrected-Anchor Classification (2026-05-02 — session 2)

Anchors used: `v1/wms-api develop` after `7f06c6f`, `v1/wms-mobile-ui develop` after `ac685f6`.
wms-web-ui skipped (just synced, anchor `aa99394`, nothing pending).

### 10.1 wms-api — 20 new commits (develop only, not in develop-arden)

The remaining 43+ commits in `7f06c6f..develop` were already classified in §4 above (develop-arden scope). Only the 20 below are net-new.

| v1 SHA | Subject | Bucket | Evidence / Notes |
|---|---|---|---|
| `ded7bca` | "## Changes Made" (OMS decoupling initial impl) | not-applicable | v1-specific BolClosedEvent pattern; v2 uses OmsNotificationService instead |
| `ed2a4b8` | refactor: BOL exception handling + closing logic cleanup | needs-investigation | Touches BillofladingService + BolClosedEventListener; compare with v2 BOL refactors before porting |
| `07ae9ab` | Fixed Inbound Notices Qty Required/Received sort | adapt-and-apply | Sort fix in AdviceRepository + ViewDtoService; v2 has qty calc (`24ae342`) but sort order may differ |
| `2a2cab1` | test: ViewDtoServiceUnitTest fixtures 18-col (dup) | not-applicable | Test-only; duplicate of `baa41f2` — same fix on two branches |
| `2351004` | fix(picking): StaleObjectState in changeReservedAmount (release dup) | already-done | Same fix as `dee2e0f`; v2 StockunitBusinessService has `entityManager.refresh(stockUnit)` lines 367/404 |
| `1752452` | fix(picking): stale tote null pickingtote_id (release dup) | already-done | Same fix as `90ef812`; v2 MobilePickingService has `setPickingtoteId(null)` line 439 |
| `baa41f2` | test: ViewDtoServiceUnitTest fixtures 18-col (dup) | not-applicable | Test-only; duplicate of `2a2cab1` |
| `dee2e0f` | fix(picking): StaleObjectState in changeReservedAmount | already-done | v2 StockunitBusinessService: `entityManager.refresh(stockUnit)` confirmed at lines 367, 404 |
| `90ef812` | fix(picking): stale tote null pickingtote_id | already-done | v2 MobilePickingService: `setPickingtoteId(null)` confirmed at line 439 |
| `7e1b5e6` | fix(picking): capture unitloadId before lambda | not-applicable | Lambda capture workaround; v2 uses Java 21 — not applicable or already absorbed in v2's refactors |
| `f46cf06` | fix(club-run): runClubLine transaction + OMS notification | needs-investigation | v2 has `5cea7a5` (mega-transaction break) + `omsNotificationService.sendAfterCommit`; diff carefully before porting |
| `827a77e` | fix(putaway): null-safe sysprop + new-SKU FLA transactional safety | adapt-and-apply | FixLocationAssignmentService + MobilePutAwayService; NOT found in v2 |
| `142f7bb` | fix(replenish): rebind sourceStock after changeReservedAmount | adapt-and-apply | MobileReplenishService: `changeReservedAmount` called at line 468 with no post-call refresh |
| `c70569e` | fix(order-release): rebind stockUnit after changeReservedAmount | adapt-and-apply | ReleaseOrderJobService: `changeReservedAmount` at line 572 with no post-call refresh |
| `f52c69e` | SBDEV-1699: Replen qty req not matching (ViewDtoService) | already-done | v2 ViewDtoService has `locationStock` + `itemdataId` (from `55a396a`) |
| `f918203` | fixed test case compilation error (MobileReplenishServiceUnitTest) | not-applicable | Test-only; v1-specific test setup |
| `41140ce` | SBDEV-1699: FLA itemdataId + locationStock in monitor view | already-done | v2 ViewDtoService lines 655–656: `itemdataId` + `locationStock` confirmed |
| `b746c39` | feat(club): SBDEV-2164 — stale club batch cleanup cron job | adapt-and-apply | New `StaleClubBatchCleanupJob` + `StaleClubBatchCleanupJobService`; NOT in v2 |
| `1715962` | feat(club): SBDEV-2163 — prevent finished batch reassign to staging lane | adapt-and-apply | Guard in `CustomerorderBatchService`; NOT found in v2 |
| `3db0ee3` | fix(test): pre-existing test failures (BillofladingService, MobileMoveStockService) | not-applicable | Test-only; v1-specific fixture setup |

**Totals:** 6 already-done · 6 not-applicable · 6 adapt-and-apply · 2 needs-investigation

### 10.2 wms-mobile-ui — 18 non-merge commits (develop after ac685f6)

| v1 SHA | Subject | Files | Bucket | Action |
|---|---|---|---|---|
| `2c45565` | hotfix 1.26.12 — pickable location display (dup) | moveStock x3 | not-applicable | Duplicate of `91e8732`; skip |
| `91e8732` | hotfix 1.26.12 — pickable location display | moveStock x3 | cherry-pick-clean | Port: `inputAmount.vue`, `scanDestination.vue`, `moveStock.js` |
| `186776d` | added to gitignore | `.gitignore` | not-applicable | Config only |
| `3866931` | updated gitignore file | `.gitignore` | not-applicable | Config only |
| `db2ebf8` | optional chaining storeBox.vue (dup) | `storeBox.vue` | not-applicable | Duplicate of `54dd733`; skip |
| `4494d8c` | CLAUDE.md + gitignore | `.gitignore`, `CLAUDE.md` | not-applicable | Docs/config |
| `04920aa` | Playwright test setup | `playwright.config.ts`, `tests/e2e/*` | not-applicable | No test suite in v2 mobile-ui |
| `bd46bec` | SBDEV-1699 locationStock replenish.vue (dup) | `replenish.vue` | not-applicable | Duplicate of `e1cec3c`; skip |
| `6ce1555` | putaway test cases + gitignore | `package.json`, `.gitignore`, `tests/e2e/putaway.spec.ts` | not-applicable | Test + config |
| `0995591` | index reset putaway (dup1) | `store/putaway.js` | not-applicable | Duplicate of `d02a5a3`; skip |
| `a9e283c` | index reset putaway (dup2) | `store/putaway.js` | not-applicable | Duplicate of `d02a5a3`; skip |
| `e1cec3c` | SBDEV-1699: locationStock from API DTO for held-up items | `pages/replenish.vue` | cherry-pick-clean | Port: replenish.vue fix |
| `df32fcd` | CLAUDE.md + gitignore | `.gitignore`, `CLAUDE.md` | not-applicable | Docs/config |
| `54dd733` | optional chaining storeBox.vue | `components/putaway/storeBox.vue` | cherry-pick-clean | Port: optional chaining guard |
| `a97fcde` | replenishment test cases | `tests/e2e/replenish.spec.ts` | not-applicable | Test only |
| `8e4fb0f` | Replenish — stale cache short-circuit fix | `selectSource.vue`, `selectUnitLoad.vue` | cherry-pick-clean | Port: stale unit load qty fix |
| `07bbdbd` | Playwright tests picking + replenishment | `tests/e2e/*` | not-applicable | Test only |
| `d02a5a3` | missing index reset putaway (final) | `store/putaway.js` | cherry-pick-clean | Port: putaway index reset |

**Totals:** 5 cherry-pick-clean · 13 not-applicable · 0 adapt-and-apply · 0 needs-investigation

### 10.3 Planned execution (pending user confirmation)

**Lane A — wms-mobile-ui (5 commits via `ui-sync.sh` or direct cherry-pick):**
1. `91e8732` — hotfix pickable location display
2. `e1cec3c` — SBDEV-1699 locationStock replenish
3. `54dd733` — optional chaining putaway storeBox
4. `8e4fb0f` — stale replenish cache fix
5. `d02a5a3` — putaway index reset

**Lane B — wms-api additions (6 adapt-and-apply, add to Phase 3/4 queue):**

*High priority (data integrity):*
- `827a77e` — null-safe sysprop + new-SKU putaway FLA (add to Phase 3)
- `142f7bb` — rebind sourceStock in finishReplenishmentOrderInternal (add to Phase 3)
- `c70569e` — rebind stockUnit in createPickingForOrder (add to Phase 3)
- `b746c39` — SBDEV-2164 stale club batch cron (add to Phase 3)
- `1715962` — SBDEV-2163 finished batch reassign guard (add to Phase 3)

*Medium priority:*
- `07ae9ab` — Inbound Notices sort (add to Phase 4)

**Needs-investigation (defer):**
- `ed2a4b8` — BOL exception handling refactor (compare v1 diff vs v2 BillofladingService)
- `f46cf06` — runClubLine hardening (compare with v2's `5cea7a5`)
