---
title: "WMS v1 → v2 Sync Sweep — 2026-06-26 (b)"
type: workflow
status: completed
version: both
scope: wms-v1-v2-sync
owner: Nam Park
created: 2026-06-26
updated: 2026-06-26
repos_in_scope:
  - wms-api
  - wms-web-ui
  - wms-mobile-ui
related:
  - ../sync-log.md
  - ../README.md
  - ./2026-06-26-wms-v1-sync.md
  - "[[260626-restore-replenishment-triggers-on-lock-state-changes]]"
tags:
  - sync
  - sweep
---

# WMS v1 → v2 Sync Sweep — 2026-06-26 (b)

Second sweep on 2026-06-26. The earlier sweep advanced the `wms-api` anchor to `5d83e06`. After that, the `release` branch was merged back into `develop` (`b54aa0b7 Merge branch 'release' into develop`), pulling in release-side duplicates of already-ported hot-fixes **plus two genuinely-new commits dated Jun 26 17:26 / 17:41** (`d3f5ce18`, `5e259791`). This sweep reconciles `5d83e06..develop` (HEAD `b54aa0b`).

## 1. Summary

| Repo | Anchor (before) | develop HEAD | Pending (non-merge) | Action |
|------|-----------------|--------------|---------------------|--------|
| wms-api | `5d83e06` | `b54aa0b` | 8 | 1 adapt-and-apply port; rest already-done / not-applicable |
| wms-web-ui | `ec6a3c8` | `ec6a3c8` | 0 | nothing to sync |
| wms-mobile-ui | `73d540c` | `73d540c` | 0 | nothing to sync |

**Buckets (wms-api):**
- adapt-and-apply: **1** (`d3f5ce18`)
- already-done: **4** (`64d84cf7`, `591b27e4`, `9412fa4e`, `5e259791`)
- not-applicable: **2** (`5c501597`, `cdddd8b5`)
- context-only (folds into the port): **1** (`0d6f989d`)

> The 5 already-done / not-applicable / context commits entered `develop` only because the `release` branch was merged back in — they are release-side re-authorings (different SHAs) of work already resolved in the 2026-06-09 and 2026-06-11 sweeps, or v1-only build/migration housekeeping.

## 2. Per-repo pending commit table

### wms-api (`5d83e06..b54aa0b`)

| v1 SHA | Subject | Lane | Bucket | Proposed action |
|--------|---------|------|--------|-----------------|
| `64d84cf7` | restore rollbackFor for checked exceptions across 12 service files | B | already-done | Release-side dup of v1 `a3b4a9b` (ported 2026-06-09). v2 already uses the `rollbackFor` convention. No action. |
| `591b27e4` | SBDEV-2033 remove triggerReplenishmentMaintenance from transferStock | B | already-done | Release-side dup of v1 `927b8379` → v2 `38fcc13` (2026-06-09). No action. |
| `0d6f989d` | SBDEV-2033 remove remaining 5 call sites (bring release to develop parity) | B | context-only | Defines v1's final trigger state (transferStock / setLockOnHold / setLockDamaged / adjustAmount / removeLock all OFF) that `d3f5ce18` then partially restores. Fed as context into the `d3f5ce18` port; no standalone v2 action. |
| `9412fa4e` | SBDEV-2384 classify replenishable stock by `useforreplenish` flag | B | already-done | Release-side dup of v2 `143fa65` / `V2.1.16` (2026-06-11). No action. |
| `5c501597` | rename Flyway migration V1.1.06 → V1.26.29 (SBDEV-2384) | B | not-applicable | v2 maintains an independent migration sequence (`V2.1.16`); pure v1 filename rename. No action. |
| `cdddd8b5` | remove dead JCenter/Bintray repos breaking release build | B | not-applicable | v1-only `pom.xml` cleanup; v2 has its own POM. No action. |
| `d3f5ce18` | **restore recalc triggers on lock-state stock changes (SBDEV-2033 follow-up)** | B | **adapt-and-apply** | **Port via `wms-v2-migrate`.** Reconcile v2 trigger state to v1 intent (see §3). v1 plan: `[[260626-restore-replenishment-triggers-on-lock-state-changes]]`. |
| `5e259791` | make closeBOL idempotent + JVM guard for concurrent close | B | already-done | **v2 is the canonical source** — the v1 commit message says it ports the v2 fix *into* v1 ("Supersedes #168, re-authored from the canonical v2 source"). Verified: v2 `BillofladingService` already has `bolToClose` guard (`:159`), the "BOL is currently in process." fast-fail (`:299`), and the idempotent CLOSED/CANCELLED no-op return (`:320-324`). No action. |

### wms-web-ui

Nothing pending — anchor `ec6a3c8` equals `origin/develop` HEAD. (The local `v1-synced-upstream` tag is absent on this clone, but the log is authoritative and anchor == HEAD, so there is no sync gap.)

### wms-mobile-ui

Nothing pending — anchor `73d540c` equals `origin/develop` HEAD. (Same tag note as web-ui.)

## 3. Per-commit detail — `d3f5ce18` (adapt-and-apply)

**v1 final trigger state** (after `0d6f989d` removed all 5, then `d3f5ce18` restored 3):

| Method | v1 final | Rationale |
|--------|----------|-----------|
| `transferStock` | **OFF** | Operator-intent path leaves source UL `NOT_LOCKED`/available → original SBDEV-2033 re-reservation bug. |
| `adjustReservedAmount` | **OFF** | Same SBDEV-2033 boundary. |
| `adjustAmount` | **OFF** | Deferred — not restored by `d3f5ce18`. |
| `setLockOnHold` | **ON** | Held UL excluded by `isSourceUsable()` → cannot be re-grabbed; keeps replenishment accurate. |
| `setLockDamaged` | **ON** | Damaged qty moves to QUALITY_FAULT UL at DAMAGED location → excluded from sourcing. |
| `removeLock` | **ON** | Unlock re-enables sourcing; re-reserving against an open shortage is the intended outcome. |

**v2 current trigger state** (`StockunitService`):

| Method | v2 current | Line |
|--------|-----------|------|
| `transferStock` (`:150`) | OFF | (removed by `38fcc13`, SBDEV-2033) |
| `setLockOnHold` (`:297`) | **OFF** | — |
| `setLockDamaged` (`:351`) | **ON** | `:401` |
| `adjustAmount` (`:407`) | **ON** | `:441` |
| `adjustReservedAmount` (`:448`) | OFF | — |
| `removeLock` (`:477`) | OFF | — |

**Required v2 delta to reach v1 intent** (finalized via `wms-v2-migrate` + ralplan + a follow-up `adjustAmount` decision):

- **ADD** trigger to `setLockOnHold` (v2 OFF → v1 ON).
- **ADD** trigger to `removeLock` (v2 OFF → v1 ON).
- **`adjustAmount` — KEEP ON in v2 (deliberate v1↔v2 divergence, decided 2026-06-26).** v1's OFF was a *scope deferral*, not a safety fix (`adjustAmount` was never one of the two SBDEV-2033 re-reservation paths). v2 currently has a working, safe trigger there; an amount edit genuinely changes availability (`amount − reserved`), so the recalc is correct. Removing it would regress v2 for parity-only benefit → kept. Future sweeps must NOT reconcile this.
- `setLockDamaged` already matches (ON); `transferStock` + `adjustReservedAmount` already match (OFF).

Net v2 code delta: **2 edits** (+setLockOnHold, +removeLock).

> Note v2 historically only ever had 3 trigger sites vs v1's 5 (per the 2026-05-07 sweep), so this is a genuine architectural reconciliation, not a mechanical cherry-pick. The `wms-v2-migrate` skill will produce the v2 plan, gate it through ralplan, and verify the `isSourceUsable()` exclusion holds in v2 before adding the `setLockOnHold`/`removeLock` triggers.

## 4. Planned actions (execution order)

1. **Lane B — `wms-v2-migrate`** on `[[260626-restore-replenishment-triggers-on-lock-state-changes]]` (with `0d6f989d` + `d3f5ce18` as the source commits) → produce `sbdocs/1-Projects/wms2/plan/260626-restore-replenishment-triggers-on-lock-state-changes.md`.
2. Gate the v2 plan (ralplan → architect/critic), TDD-gate the acceptance criteria, implement in `v2/wms2-api`, verify (`mvn clean compile` + unit tests; ITs `@Disabled` per SBDEV-2217).
3. Commit to `v2/wms2-api` as `port v1 d3f5ce18 — restore replenishment recalc triggers on lock-state changes`; open PR.
4. **Advance `wms-api` anchor** `5d83e06` → `b54aa0b` (full range reconciled).
5. web-ui / mobile-ui anchors unchanged (nothing pending).
6. Update `sync-log.md`; close this report (§5/§6).

## 5. Applied commits + resulting v2 SHAs

| v1 SHA | v2 commit | v2 PR | Notes |
|--------|-----------|-------|-------|
| `d3f5ce18` (+ `0d6f989d` context) | `64e8516` → squash-merged `develop` as `6ab2f37` | [#54](https://github.com/SiteBossInc/wms2-api/pull/54) (**merged** 2026-06-28) | `wms-v2-migrate` → ralplan (APPROVE, 2 iters) → tdd-gate → executor → code-review (0 High, 3 Medium all fixed). Restored triggers on `setLockOnHold` + `removeLock`; **`adjustAmount` kept ON** (deliberate v1↔v2 divergence — v1's OFF was scope, not safety). `mvn clean compile` SUCCESS; `StockunitServiceUnitTest` 70/0-fail; ITs N/A (no repo/SQL change). Plan: `[[260626-restore-replenishment-triggers-on-lock-state-changes]]` §12. Docs: `wms2-stockunit-design.md` trigger topology corrected. |

## 6. Skipped with reason

- **already-done (release-branch re-merges of prior ports):** `64d84cf7` (rollbackFor = v1 `a3b4a9b`, 2026-06-09), `591b27e4` (SBDEV-2033 transferStock = v2 `38fcc13`, 2026-06-09), `9412fa4e` (SBDEV-2384 = v2 `143fa65`/V2.1.16, 2026-06-11), `5e259791` (BOL idempotency — **v2 is the canonical source**; verified present at `BillofladingService:159/299/320`).
- **already-done (context only):** `0d6f989d` (SBDEV-2033 remove-remaining — folded into the `d3f5ce18` port as the trigger-state baseline; no standalone v2 action).
- **not-applicable:** `5c501597` (Flyway V1.1.06→V1.26.29 rename — v2 has independent migration sequence), `cdddd8b5` (dead JCenter/Bintray repos in v1 `pom.xml` — v2 has its own POM).

## 7. Follow-ups

- **`adjustAmount` trigger — RESOLVED (kept ON in v2):** decided 2026-06-26 to retain v2's existing trigger as a deliberate v1↔v2 divergence (v1's OFF was scope, not safety). Recorded in the v2 plan §2/§3/§11. **Future sweeps must not re-remove it for parity.**
- **NEW-1a (fast-follow):** wrap `removeLock` in `@Transactional(tenantTransactionManager)` to make unlock+message+recalc atomic and remove the in-tx/separate-tx asymmetry (low risk).
- **NEW-1b (separate investigation):** `setLockDamaged` / `adjustAmount` `@Transactional` gap — non-trivial because `setLockDamaged` calls `printService.cupsPrint` (external I/O); needs its own design pass.
