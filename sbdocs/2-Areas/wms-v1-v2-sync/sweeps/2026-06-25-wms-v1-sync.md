---
title: "WMS v1 → v2 Sync Sweep — 2026-06-25"
type: workflow
status: completed
version: both
scope: wms-v1-v2-sync
owner: Nam Park
created: 2026-06-25
updated: 2026-06-25
repos_in_scope:
  - wms-api
  - wms-web-ui
related:
  - ../sync-log.md
  - ../README.md
  - "[[260624-stock-unit-history-on-unitload-relocation]]"
  - "[[SBDEV-2481-stale-pick-line-realignment-on-stock-move]]"
  - "[[SBDEV-2486-club-lane-blank-screen-split-adjust]]"
tags:
  - sync
  - sweep
---

# WMS v1 → v2 Sync Sweep — 2026-06-25

**Scope (user-requested):** `wms-api` (Lane B) + `wms-web-ui` (Lane A). `wms-mobile-ui` **excluded** this sweep.

**Anchors (from `sync-log.md`, verified no drift):**

| Repo | Anchor | Source | Drift check |
|------|--------|--------|-------------|
| wms-api | `a3b4a9b` | log-only (Lane B has no git tag) | — anchor commit exists ✅ |
| wms-web-ui | `8730f94` | `v1-synced-upstream` tag in v2/wms2-web-ui | tag == log == `8730f94` ✅ no drift |

Enumeration is against **`origin/develop`** (post the 2026-05-02 `develop-arden`→`develop` retarget). Both v1 repos fetched 2026-06-25.

---

## 1. Summary

| Repo | Pending (non-merge) | not-applicable | adapt-and-apply | cherry-pick-clean | needs-investigation |
|------|--------------------:|---------------:|----------------:|------------------:|--------------------:|
| wms-api | 8 | 3 | 5 (→ 3 features) | — | 0 |
| wms-web-ui | 1 | 0 | — | 1 | 0 |

- **wms-api** has **3 substantial API ports** to produce (SBDEV-2481, relocation/SBDEV-2488, SBDEV-2486), all confirmed **absent** in v2. None has an existing v2 plan.
- **wms-web-ui** has **1 clean cherry-pick** (SBDEV-2486 club-fulfillment blank-screen — the web-ui counterpart of the API SBDEV-2486 fix).
- All three v1 features already have **v1 plans** in `sbdocs/1-Projects/wms1/plan/`; the relocation v1 plan (§10-#3) explicitly calls for a paired v2 `wms-v2-migrate` plan.

---

## 2. Per-repo pending commit table

### wms-api (Lane B) — `a3b4a9b..origin/develop`

| # | v1 SHA | Subject | Bucket | Proposed action |
|---|--------|---------|--------|-----------------|
| 1 | `84c8c25` | Add portainer update step in UAT workflow | not-applicable | v1-only CI (`.github/workflows/docker-image-uat.yml`); v2 has its own GitHub Actions. No port. |
| 2 | `ae98c34` | claude.md updated | not-applicable | v1 repo docs (`CLAUDE.md` typo-file cleanup). No port. |
| 3 | `f3c0cae` | add `ro_id` column to `replenishment_monitor_view` (SBDEV-2384) | not-applicable | v2 supplies `ro_id` via the **inline native query** in `ReplenishmentMonitorViewRepository` (`ro.id AS ro_id`, :105/:34/:113) — not via a view column. v2 has no view-drift to fix. No port. |
| 4 | `7c47a2b` | SBDEV-2481 — realign/block stale pick lines on stock & UL moves | adapt-and-apply | **BIG (13 files, ~1049 ins).** 2 NEW services (`PickLineActivityCodeClassifier`, `PickLineRealignmentService`) + IT. v2 has none (`grep realign/stale-pick` → 0). → `wms-v2-migrate` plan, then port. |
| 5 | `c46688e` | relocation logging base (`recordRelocation`, `STOCK_RELOCATED`) | adapt-and-apply | v2 has **no** `recordRelocation`/`STOCK_RELOCATED`. Port as **one** v2 feature with #6+#7 folded in (final state). v1 plan §10-#3 already designates this. |
| 6 | `8767fd3` | relocation follow-up — specific NoSuchElement msgs + null-amount guard | adapt-and-apply | Same files as #5; fold into the single relocation port. |
| 7 | `b39b44d` | SBDEV-2488 — record moved amount on relocation rows | adapt-and-apply | Same files as #5; **this is the final amount semantics** (`amount = stockunit.getAmount()`). Port reflects #5+#6+#7 combined end state. |
| 8 | `0b1e0ec` | SBDEV-2486 — harden club-lane SKU/unit-load queries (NPE/wrong-sum) | adapt-and-apply | v2 **has** `getClubLineSKUOverview` (:1233), `getClubLineUnitLoads` (:939), `getCustomerorderBatchDetails` (:1292). Review v2 against v1 B1–B4; port the deltas v2 is missing. |

### wms-web-ui (Lane A) — `8730f94..origin/develop`

| # | v1 SHA | Subject | Bucket | Proposed action |
|---|--------|---------|--------|-----------------|
| 1 | `2b41185` | SBDEV-2486 — keep club-fulfillment screen from going blank on error/refresh | cherry-pick-clean | 8 files (`.vue`/`.js` + Jest specs), no `nuxt.config.js`/`store/index.js`. Lane A via `ui-sync.sh`; `git cherry` will confirm not-already-in-v2 (brand-new v1 work → expected fresh). |

---

## 3. Per-commit detail (adapt-and-apply)

### Feature A — SBDEV-2481 stale pick-line realignment (`7c47a2b`)
- **v1 surface:** `PickLineActivityCodeClassifier` (NEW, 97 LoC), `PickLineRealignmentService` (NEW, 158 LoC), `FixLocationAssignmentService` (+37), `StockunitBusinessService` (+34), `UnitloadBusinessService` (+27), `MobileMoveStockService` (+2), `PickingorderPositionRepository` (+8) + 1 IT (436) + 4 unit tests.
- **v2 adaptations to expect:** Jakarta namespace; `@Transactional("tenantTransactionManager")`; check whether v2's `FixLocationAssignmentService`/`StockunitBusinessService` already diverged (relocation port #5 also touches `FixLocationAssignmentService` — sequence matters).
- **v1 plan:** `1-Projects/wms1/plan/SBDEV-2481-stale-pick-line-realignment-on-stock-move.md`.
- **Action:** `wms-v2-migrate` → new `1-Projects/wms2/plan/SBDEV-2481-...md`; implement; test; commit `port v1 7c47a2b — …`.

### Feature B — Relocation stock-unit history / SBDEV-2488 (`c46688e`+`8767fd3`+`b39b44d`)
- **v1 surface:** `WmsConstants` (+`STOCK_RELOCATED`), `StockrecordService.recordRelocation`, `FixLocationAssignmentService` (call site), `StockunitService` (call site) + 3 ITs + unit tests.
- **Final-state semantics (must reflect b39b44d):** `recordRelocation` sets `amount = stockunit.getAmount()` (moved qty shows in UI "Moved"); report-safety via the `(activitycode,type)` allow-list, NOT via zeroing amount.
- **v2 adaptations to expect:** verify v2 report functions (`transaction_detail`/`transaction_summary`) carry the same allow-list so `STOCK_RELOCATED` stays excluded; Jakarta; `tenantTransactionManager`.
- **v1 plan:** `1-Projects/wms1/plan/260624-stock-unit-history-on-unitload-relocation.md` (§10-#3 designates this port).
- **Action:** `wms-v2-migrate` → `1-Projects/wms2/plan/260624-stock-unit-history-on-unitload-relocation.md` (same base name); implement reflecting the **combined** end state; commit `port v1 c46688e+8767fd3+b39b44d — …`.

### Feature C — SBDEV-2486 club-lane API hardening (`0b1e0ec`)
- **v1 surface:** `CustomerorderBatchService` B1–B4 (forEach→for + `orElseThrow(BusinessException)`; reference-equality sum fix + null-amount guard; client `ifPresent`; SKU filter null-guard) + unit tests.
- **v2 adaptations to expect:** v2's three methods exist but may already be partially correct or differently shaped — **review each against B1–B4** and port only the missing deltas. Confirm whether v2 entity equality / Optional usage already covers some cases.
- **v1 plan:** `1-Projects/wms1/plan/SBDEV-2486-club-lane-blank-screen-split-adjust.md`.
- **Action:** `wms-v2-migrate` → `1-Projects/wms2/plan/SBDEV-2486-...md`; implement deltas; test; commit `port v1 0b1e0ec — …`.

---

## 4. Planned actions (execution order)

**Lane A first (fast, low-risk):**
1. `./sbdocs/9-System/scripts/ui-sync.sh v1/wms-web-ui v2/wms2-web-ui` → cherry-pick `2b41185` (SBDEV-2486 club-fulfillment). Resolve any conflict; let the script advance the `v1-synced-upstream` tag to `2b41185` (v1 develop HEAD for web-ui).

**Lane B (3 ports — each is a full migrate→implement→test cycle):**
2. **Feature B (relocation/SBDEV-2488)** — port first; it's the most self-contained and I just authored the v1 final state, so adaptation risk is lowest. NOTE its overlap with Feature A on `FixLocationAssignmentService`.
3. **Feature C (SBDEV-2486 API)** — smallest (1 service), review-and-port deltas.
4. **Feature A (SBDEV-2481)** — largest; port last so Features B/C's `FixLocationAssignmentService`/`StockunitBusinessService` edits are already in place.
5. After each port: `mvn clean compile` + targeted tests; do **not** run full suite as a gate (known pre-existing failures). Code-review each diff.
6. Update `sync-log.md`: advance **wms-api** anchor to `b39b44d` (v1 develop HEAD) **only after all three API ports land**; advance **wms-web-ui** to `2b41185` after Lane A.

**Anchor-advance rule (from log Notes):** never move an anchor forward if its ports didn't actually land. If any API feature is deferred, the wms-api anchor stays at `a3b4a9b` and the landed ports are logged as out-of-order (per prior-sweep convention).

---

## 5. Applied commits + resulting v2 SHAs

| v1 SHA(s) | v2 SHA | Repo | Notes |
|-----------|--------|------|-------|
| `2b41185` | `aa849c3` | wms2-web-ui | **Lane A — DONE 2026-06-25.** `ui-sync.sh` cherry-pick; patch-id identical to v1 (`3bae10c…`). The range pick errored on the PR-merge `ec6a3c8` (no `-m`) *after* applying the content commit — merge skipped (no extra content), sequencer `--quit`, ff-merged into develop manually. Tag `v1-synced-upstream` advanced `8730f94 → ec6a3c8`. **Not pushed** (review-first). v2 Jest (`--testPathPattern=clubRuns`): **2 suites / 4 tests pass**. |

| `c46688e`+`8767fd3`+`b39b44d` | `7f82adb` | wms2-api | **Lane B Feature B (relocation/SBDEV-2488) — DONE 2026-06-25.** Ported via `wms-v2-migrate` → ralplan consensus (Architect SOUND-WITH-NITS → Critic APPROVE) → `wms-tdd-gate` (4 unit fail-first) → implement. v2 plan: `1-Projects/wms2/plan/260624-stock-unit-history-on-unitload-relocation.md`. F-A..F-D + **NEW-1** (`@Transactional(tenantTransactionManager)` added to `FixLocationAssignmentService.move()` — had none). Unit: **126/126**; `mvn clean compile` SUCCESS. AC-7/8/9 ITs written but **`@Disabled` (SBDEV-2217** v2 Testcontainers lane broken repo-wide). [PR #49](https://github.com/SiteBossInc/wms2-api/pull/49) → develop (branch `port/SBDEV-2488-relocation-stock-history`). |

| `0b1e0ec` | `1d871f7` | wms2-api | **Lane B Feature C (SBDEV-2486 API) — DONE 2026-06-25.** Scope collapsed after architect review: B1/B3 already correct in v2 (`EntityNotFoundException`, stricter than v1), B2 wrong-sum neutralized by id-based `AbstractBaseEntity.equals()`. Ported only **B4** (SKU-filter null-guard), **B2 null-amount/receiver guard**, and **NEW-1** (removed dead `findAllById` `itemDataMap` in `calc()`). Small-port: ralplan skipped (architect review = review of record), test-first via tdd-gate (AC-1/2 NPE fail-first → green). Unit **99/99**; `mvn clean compile` SUCCESS. v2 plan: `1-Projects/wms2/plan/SBDEV-2486-club-lane-blank-screen-split-adjust.md`. [PR #50](https://github.com/SiteBossInc/wms2-api/pull/50) → develop (branch `fix/SBDEV-2486-club-lane-api-hardening`). |

| `7c47a2b` | `024eab3` | wms2-api | **Lane B Feature A (SBDEV-2481) — DONE 2026-06-25.** Largest port. ralplan consensus (Architect SOUND-WITH-NITS → Critic APPROVE iter 2; caught the lock-insertion point `:184`, the pre-walk tree-locking, the false `CustomerorderBatchRepository:33` anchor) → tdd-gate (8 unit fail-first) → implement. P0 detector SQL + 2 new services (classifier + repos-only realign, **no DI cycle — compile-verified**) + Hook A (pre-walk lock) + Hook B (PO lock before src-SU) + FLA cluster deletion (param dropped) + MobileMoveStockService `@Transactional`. Unit **103/103**; `mvn clean compile` SUCCESS. Concurrency/detector ITs `@Disabled` (SBDEV-2217). **Stacked on the relocation branch** → [PR #51](https://github.com/SiteBossInc/wms2-api/pull/51) (base = `port/SBDEV-2488-relocation-stock-history`). v2 plan: `1-Projects/wms2/plan/SBDEV-2481-stale-pick-line-realignment-on-stock-move.md`. |

**Sweep complete.** All Lane A + Lane B work items resolved. **wms-api anchor advanced `a3b4a9b` → `73e2cdd`** (full `a3b4a9b..develop` range reconciled: 3 ported + 3 not-applicable). **All three v2 PRs pending merge** — #49 (relocation) → #50 (SBDEV-2486, independent off develop) → #51 (SBDEV-2481, stacked on #49). Merge #49 before/with #51, or retarget #51 to develop after #49.

---

## 6. Skipped (with reason)

| v1 SHA | Reason |
|--------|--------|
| `84c8c25` | not-applicable — v1-only portainer/CI |
| `ae98c34` | not-applicable — v1 repo docs |
| `f3c0cae` | not-applicable — v2 provides `ro_id` via inline repo query, not a view column |

---

## 7. Follow-ups

- **wms-mobile-ui — checked 2026-06-25 (added to scope on request): nothing to sync.** `v1-synced-upstream` == `v1/wms-mobile-ui` develop HEAD `db6f573` (tip 2026-06-11); 0 pending non-merge commits, no drift. None of the three swept tickets produced a v1 mobile-ui commit (SBDEV-2481's `BusinessException` block surfaces via the existing error-toast path — no mobile code change). Anchor unchanged at `db6f573`.
- The three API ports may warrant **separate sessions** given their combined size (~1.8k LoC of v1 surface). This report can drive them incrementally; the anchor advances only when all three land.
- v1 plans for all three features are linked above; v2 plans to be created via `wms-v2-migrate` (same base names).
