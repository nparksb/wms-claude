---
title: "WMS v1 → v2 Sync Sweep — 2026-06-18"
type: sweep
status: completed
version: both
scope: wms-v1-v2-sync
owner: Nam Park
created: 2026-06-18
updated: 2026-06-18
related:
  - ../sync-log.md
  - ../README.md
  - ./2026-06-09-wms-v1-sync.md
tags:
  - sync
  - sweep
  - lane-a
---

# WMS v1 → v2 Sync Sweep — 2026-06-18

## Summary

Lane A only — UI cherry-picks for `wms-web-ui` and `wms-mobile-ui` (per user scope). No API (Lane B) work this sweep.

**Setup note:** this Linux clone (`/home/nampark/dev/wms-claude`) never had the one-time Lane A git plumbing — no `v1` remote and no `v1-synced-upstream` tag in either v2 UI repo (the `ui-sync.sh` example paths are macOS; setup was done on a different machine). This is **missing setup, not drift** — the tag does not exist, so there is nothing to disagree with the log. The `sync-log.md` anchors are authoritative. I added a read-only `v1` remote pointing at the local v1 clones and fetched `develop` into each v2 repo to run `git cherry`; I have **not** created the tag or applied any commits yet.

**Execution wrinkle:** the pending ranges contain merge commits and many already-patch-equivalent commits, so a blind `ui-sync.sh` range cherry-pick would halt on the first merge / empty commit. Recommended path is a **manual selective cherry-pick** of just the genuinely-pending UI fixes, then advance the tag — see §4.

| Repo | v1 anchor (before) | v1 anchor (after) | Cherry-picked | Already in v2 | Already-done (end-state) | Not-applicable | Merge-passive |
|------|--------------------|-------------------|---------------|---------------|--------------------------|----------------|---------------|
| wms-web-ui    | `d44f593` | `8730f94` ✅ | **1** (`28e6009`→`9879247`) | 20 | 1 (Dockerfile) | 1 (CI/CD) | 2 |
| wms-mobile-ui | `226537b` | `db6f573` ✅ | **1** (`1cf9eca`→`510c945`) | 0 | 1 (Dockerfile) | 1 (CI/CD) | 0 |

> **Execution correction:** three web-ui commits drafted as `cherry-pick-clean` — `608d4d3`, `523e4e2`, `80041f4` — turned out to be **already present in v2** when actually cherry-picked (empty cherry-picks / a whitespace-only `try{ ` conflict on `80041f4`). `git cherry` flagged them `+` because v2 had applied the same end-state independently with differing patch-ids. Verified by direct inspection of v2 `shipperBrandTable.vue`/`zoneViewTable.vue` (`itemHeadersBase`/`hasPrintableItems` present), `replenishViewTable.vue` (actions header already commented out), and `section.js` (`createSection` already un-commented). Only `28e6009` was genuinely new in web-ui.

## Per-repo pending commit tables

### wms-web-ui (range `d44f593..origin/develop`, HEAD `8730f94`)

25 commits in range (23 non-merge + 2 merges). `git cherry -v develop v1/develop d44f593` run in the v2 repo.

| v1 SHA | Subject | Bucket | Action |
|--------|---------|--------|--------|
| `233d801` | Usage of both "canceled"/"cancelled" text | already-done | Skip — `git cherry` `-` |
| `6f2ae9b` | Cycle count export filename always "CC_Test_.." | already-done | Skip — `-` |
| `6d2c08f` | Replenishment monitor column not capitalized | already-done | Skip — `-` |
| `7fb2a04` | Update Dockerfile (node 18→24) | already-done (end-state) | Skip — v2 web Dockerfile **already** pins `node:24.0.0-alpine` (builder + final). `git cherry` `+` only because v2's Dockerfile was set independently. |
| `608d4d3` | Dashboard table excess column display | already-done | Skip — cherry-pick empty; v2 `shipperBrandTable.vue`/`zoneViewTable.vue` already have `itemHeadersBase` + `hasPrintableItems` |
| `523e4e2` | Dashboard table excess column display | already-done | Skip — cherry-pick empty; v2 `replenishViewTable.vue` already has the `actions` header commented out |
| `3e03b4a` | Cycle count export confirmation misworded | already-done | Skip — `-` |
| `691c075` | SKU Data export csv confirm shows two `??` | already-done | Skip — `-` |
| `80041f4` | Sections: Create section doesn't work (false success popup) | already-done | Skip — only a whitespace (`try{ `) conflict; v2 `store/masterData/section.js` `createSection` already has the POST call un-commented + error handling |
| `afff34d` | Parcel Picking Report "ItemName" spacing | already-done | Skip — `-` |
| `c1262de` | Fixed Location Assignments: negative Min/Hysteresis | already-done | Skip — `-` |
| `99822b1` | Packaging: negative capacity on edit | already-done | Skip — `-` |
| `5361930` | Packaging: name/external ID blank on edit | already-done | Skip — `-` |
| `fdca2ec` | Handling Units: adjust amount -1 error 500 | already-done | Skip — `-` |
| `db87577` | Replenishment priority update doesn't preselect | already-done | Skip — `-` |
| `e2f1ce2` | Club: priority revert doesn't update | already-done | Skip — `-` |
| `f0b9a34` | Club: Parcels column sort broken | already-done | Skip — `-` |
| `5e4e1be` | Lock Report: Stock Lock column sort broken | already-done | Skip — `-` |
| `ab75c65` | Pick pack SKU detail returns error | already-done | Skip — `-` |
| `588b52b` | Transfer list→detail shows error (base fix) | already-done | Skip — `-` (v2 has it; `28e6009` builds on this) |
| `51ebeaf` | Replenishment Dest/Qty shows 0 when no dest | already-done | Skip — `-` |
| `28e6009` | Transfer list→detail shows error (follow-up) | **cherry-pick-clean** | **Applied** as v2 `9879247` — `components/outbound/transfer/transferDetails.vue` (+8). Adds `isInitializingShipDate` guard so the `shipDate` watcher doesn't fire `saveDate` during initial load. Auto-merged cleanly on top of `588b52b` (already in v2). |
| `bd06d46` | update github workflow for uat CD via portainer | **not-applicable** | Skip — adds a **v1-specific** portainer webhook (`…v1 wms web ui…`) to `.github/workflows/docker-uat-image.yml` + new v1 `BRANCHING.md`. v2 maintains its own independent CI/CD workflows. |
| `b12e21f` | Merge `task/SBDEV-2295` into develop-arden | merge-passive | n/a (excluded by `git cherry`) |
| `8730f94` | Merge develop into develop | merge-passive | n/a — becomes the new anchor |

### wms-mobile-ui (range `226537b..origin/develop`, HEAD `db6f573`)

3 commits, all `git cherry` `+`.

| v1 SHA | Subject | Bucket | Action |
|--------|---------|--------|--------|
| `1cf9eca` | Added trims to these screens | **cherry-pick-clean** | **Applied** as v2 `510c945` — `cycleCount/bySku/scanUnitLoad.vue`, `moveStock/scanSource.vue`, `putaway/scanPallet.vue`, `replenish/request/scanLocation.vue` (4 files, +4). Each adds `this.scannedValue = typeof this.scannedValue === 'string' ? this.scannedValue.trim() : this.scannedValue`. Follow-on to `43dd052` (trim helper) ported in the 2026-05-08 sweep. |
| `e0f1e88` | Update Dockerfile (node 18→24) | already-done (end-state) | Skip — v2 mobile Dockerfile **already** pins `node:24.0.0-alpine`. |
| `db6f573` | update github workflow for uat CD via portainer | **not-applicable** | Skip — v1-specific portainer webhook + v1 `BRANCHING.md`. Becomes the new anchor. |

## Planned actions (execution order — pending your approval)

1. **One-time Lane A setup** (idempotent; `v1` remote already added + fetched for classification):
   - `git -C v2/wms2-web-ui tag -f v1-synced-upstream d44f593` (the recorded anchor — back-fills the missing tag at the current log position).
   - `git -C v2/wms2-mobile-ui tag -f v1-synced-upstream 226537b`.
   - *(Recording the tag at the existing anchor first keeps the tag in sync with the log; step 4 advances it after the picks land.)*
2. **wms-web-ui** — on `develop`, create `sync-2026-06-18`, manual selective cherry-pick `-x` in order: `608d4d3`, `523e4e2`, `80041f4`, `28e6009`. (Range cherry-pick via `ui-sync.sh` is unsuitable here — merges + 17 already-done commits would halt it.)
3. **wms-mobile-ui** — on `develop`, create `sync-2026-06-18`, cherry-pick `-x` `1cf9eca`.
4. **Advance tags + merge:** ff-merge each sync branch into `develop`, then `git tag -f v1-synced-upstream` to the new anchors (`8730f94` web / `db6f573` mobile).
5. **No push** (per guardrails — review first).
6. Update `sync-log.md` with a 2026-06-18 row; flip this report's `status` to `completed` and fill §5/§6.

> ⚠️ Branch note: both v2 UI repos currently sit on `develop` with clean working trees. A `feature/utc-timezone` branch also exists in each (active UTC work, per project memory). This sweep operates only on `develop` and will not touch `feature/utc-timezone`.

## 5. Applied commits + resulting v2 SHAs

| Repo | v1 SHA | v2 SHA | Files | Notes |
|------|--------|--------|-------|-------|
| wms-web-ui    | `28e6009` | `9879247` | `components/outbound/transfer/transferDetails.vue` (+8) | `isInitializingShipDate` guard on the `shipDate` watcher. Cherry-picked `-x`, ff-merged into `develop`. |
| wms-mobile-ui | `1cf9eca` | `510c945` | 4 scan components (+1 each) | `.trim()` on `scannedValue` in cycleCount/moveStock/putaway/replenish. Cherry-picked `-x`, ff-merged into `develop`. |

Tags advanced: `v1-synced-upstream` → `8730f94` (web), `db6f573` (mobile). **Neither repo pushed** (review-first guardrail) — each is 1 commit ahead of `origin/develop`.

## 6. Skipped with reason

- **web-ui:** 20 already-in-v2 (17 via `git cherry -`, plus `608d4d3`/`523e4e2`/`80041f4` proven already-applied at cherry-pick time — see §summary correction), 1 already-done end-state (`7fb2a04` Dockerfile — v2 already `node:24.0.0`), 1 not-applicable (`bd06d46` v1-specific portainer CI + `BRANCHING.md`), 2 merge-passive (`b12e21f`, `8730f94`).
- **mobile-ui:** 1 already-done end-state (`e0f1e88` Dockerfile — v2 already `node:24.0.0`), 1 not-applicable (`db6f573` v1-specific portainer CI + `BRANCHING.md`).

## 7. Follow-ups

- **Push pending:** both ports await review + push to `origin/develop`.
- The v1 Node 18→24 Dockerfile bumps and the portainer-webhook CI changes are a recurring v1-only pattern; future sweeps can fast-skip Dockerfile/`.github/`/`BRANCHING.md`-only commits for the UI repos.
- **`git cherry` is unreliable here** — it flagged 3 web-ui commits `+` that were already in v2 (v2 applied the same end-state with differing patch-ids). Always confirm by actually cherry-picking (empty/whitespace-only conflicts = already-done) before counting a commit as a real port.
- The one-time Lane A plumbing (`v1` remote + `v1-synced-upstream` tag) is now set up on this Linux clone, so the next sweep can lean on `ui-sync.sh` for clean ranges.
