---
title: "WMS v1 → v2 Sync Sweep — 2026-06-09 (API)"
ticket: ""
ticket_url: ""
type: sync-sweep
priority: high
status: completed
project: [wms1, wms2]
version: both
requester: Nam Park
created: 2026-06-09
updated: 2026-06-09
related:
  - ../sync-log.md
  - 2026-05-07-wms-v1-sync.md
tags:
  - sync
  - sweep
  - api
---

# WMS v1 → v2 Sync Sweep — 2026-06-09 (API lane only)

**Scope:** API lane only — `v1/wms-api develop` → `v2/wms2-api develop`. (UI lanes not run this sweep.)

**Anchors at sweep start:**

| Repo | Anchor SHA | Notes |
|------|-----------|-------|
| `v1/wms-api` | `7f06c6f` | 2026-03-20 baseline; never advanced. `develop` == `develop-arden` (both `9eceb89`) + today's `a3b4a9b`. |

**Pre-sweep prerequisite (done):** release-only hotfix `64d84cf7` (restore `rollbackFor` across 12 service files) cherry-picked into `develop` → `a3b4a9b`, build-verified (Java 8 `mvn clean package`), pushed to `origin/develop`. The other 5 release-ahead commits were already-present/not-applicable. See sync-log.

---

## 1. Summary

`7f06c6f..develop` = **133 commits** (34 merges + 99 non-merge).

| Disposition | Count | How determined |
|---|---|---|
| **Already reconciled before 2026-05-09** (ported out-of-order, verified done, not-applicable, or doc/merge) | 93 non-merge | The 2026-05-07 sweep + the 2026-05-07/08 sync-log verification rows resolved every commit dated ≤ 2026-05-08. |
| **New since last sweep (commit date > 2026-05-08)** | 6 non-merge | Enumerated below. |
| └ already-done in v2 | 3 | `4652a80c`, `a3b4a9b`, `7086e6d2` |
| └ not-applicable | 2 | `d0658e79`, `f336903a` |
| └ **adapt-and-apply (genuine v2 port)** | **1** | **`927b8379`** |

> **Correction during port prep:** `7086e6d2` was initially classed adapt-and-apply, but reading v2's
> repository for the TDD-gate showed v2 **already has** the fix (subquery wrapper + `countQuery` at
> `CustomerorderBatchRepository:102-122`, returning a `Page<OrderBatchPageView>` projection). Reclassified
> to already-done; no port. (The earlier "gap" call came from a grep that didn't surface the wrapper lines —
> the test-first step caught it before any redundant code landed.)

**Net work this sweep: 1 API port (Lane B) — `927b8379`.** After it lands, *every* commit in
`7f06c6f..develop` is ported / already-done / not-applicable → the `wms-api` anchor can finally advance to
`a3b4a9b` (develop tip).

---

## 2. New-since-last-sweep commits (the only ones needing classification)

| v1 SHA | Date | Subject | Bucket | v2 evidence |
|--------|------|---------|--------|-------------|
| `d0658e79` | 2026-05-22 | Create .sdkmanrc | **not-applicable** | tooling only; v2 maintains its own toolchain config |
| **`927b8379`** | 2026-05-22 | fix(SBDEV-2033): remove `triggerReplenishmentMaintenance` from `transferStock` | **adapt-and-apply (HIGH)** | v2 `StockunitService.transferStock` (L145) still calls `triggerReplenishmentMaintenance(...)` at **L252** (inside the method) — bug present |
| `7086e6d2` | 2026-05-26 | Fixed V1 Club Run screen: Column Parcels sort error | **already-done** | v2 `CustomerorderBatchRepository:102-122` already wraps the paged query in `SELECT * FROM (...) cb` + supplies a `countQuery` (returns `Page<OrderBatchPageView>`) — the fix is already present |
| `f336903a` | 2026-06-02 | fix(build): remove dead JCenter/Bintray repos | **not-applicable** | v1 `pom.xml` repo cleanup; v2 has its own build/repos (v2's own JCenter fix already handled separately) |
| `4652a80c` | 2026-06-02 | fix(replenish): SBDEV-2384 classify replenishable by `useforreplenish` flag | ~~already-done~~ **MISCLASSIFIED → ported 2026-06-11** | The already-done call cited `:73`, which is the `available_amount` bucket — flag-based in v1 too (never the bug). The defective `on_replenishable_location(_names)` predicates still used the area-name list in v2 (`ReplenishmentMonitorViewRepository:92,96` + the `V1.2.04`/`V1.2.99` view recreates). Ported as wms2-api `143fa65` (`feature/utc-timezone`): repo query fix + new `V2.1.16` + `V1.2.04`/`V1.2.99` predicate swap + bridge toolkit watermark → V2.1.16. See §5 row |
| `a3b4a9b` | 2026-06-09 | fix(transactional): restore `rollbackFor` across 12 service files | **already-done** | v2 mandates `@Transactional(value="tenantTransactionManager", rollbackFor={BusinessException, FacadeException})` by convention; rollbackFor present across the same services |

> The 93 pre-2026-05-09 commits are not re-tabulated here — their dispositions are recorded in
> [2026-05-07-wms-v1-sync.md](2026-05-07-wms-v1-sync.md) §2 and the 2026-05-07/08 sync-log rows (Groups
> P/R/C/A/B/M/T/X all ported-or-verified; 7 needs-investigation all verified; doc/merge skipped).

---

## 3. Per-commit detail — adapt-and-apply (Lane B)

### `927b8379` — SBDEV-2033: drop `triggerReplenishmentMaintenance` from `transferStock` (HIGH)
- **v1 change:** removed the trailing `triggerReplenishmentMaintenance(stockUnit.getItemdataId())` in `StockunitService.transferStock` (MANUAL_SPLIT path), replaced with an explanatory comment. It was the *last* call site after `b7707bf` removed the others; the synchronous `recalculateForItem()` was immediately re-reserving stock the operator had just released (reserve-amount adjust "not sticking" on **WineCo production**).
- **v2 gap:** confirmed — v2 `transferStock` (L145) still calls it at L252. v2's *other* two calls (L388, L428 — different methods) stay; only the transferStock one is removed.
- **v2 relevance:** high — WineCo is a v2 migration target; this is a live production-correctness bug.
- **Plan:** trivial 1-line removal + comment. Will invoke `wms-v2-migrate` to record the port rationale, then apply. v2 commit: `port v1 927b8379 — drop triggerReplenishmentMaintenance from transferStock (SBDEV-2033)`.

### `7086e6d2` — Club Run Parcels column sort — ALREADY-DONE (no port)
Verified while preparing the TDD-gate: v2 `CustomerorderBatchRepository.findByStateAndTypeAndKeywordPage`
(L102-122) already wraps the grouped query in `SELECT * FROM (...) cb` and supplies a matching `countQuery`
— identical in intent to v1 `7086e6d2`, and returns a `Page<OrderBatchPageView>` projection (cleaner than
v1's `Object[]`). No port needed; no TDD-gate run.

---

## 4. Planned actions (await confirmation before executing)

1. **Port `927b8379`** (Lane B) — remove the transferStock `triggerReplenishmentMaintenance` call + comment. ✅ applied.
2. **Build gate** — `mvn clean test-compile` on wms2-api `develop` (Java 21). ✅ exit 0.
3. **Code-reviewer pass** over the `927b8379` diff (separate lane).
4. **Advance the `wms-api` anchor** in sync-log.md to **`a3b4a9b`** (develop tip) — first real advance off the baseline; all prior out-of-order ports + this one close the gap.

---

## 5. Applied commits
_(filled after execution)_

| v1 SHA | Subject | v2 SHA | Notes |
|--------|---------|--------|-------|
| `927b8379` | SBDEV-2033 — drop `triggerReplenishmentMaintenance` from `transferStock` | `38fcc13` | wms2-api `develop`; call replaced with comment, other 2 call sites retained. `mvn clean test-compile` clean (Java 21); code-reviewer **APPROVE**. **Not pushed.** |
| `4652a80c` | SBDEV-2384 — flag-based replenishable classification (correction of §2/§6 already-done call) | `143fa65` | wms2-api `feature/utc-timezone`, 2026-06-11. Both `on_replenishable_location(_names)` predicates → `useforreplenish = true`; new `V2.1.16` (= standalone patch for already-converted DBs — applied 2026-06-11 to hydra-dev2, wineco `wh01_om1_v2` dev2 copy + `dev_wh01_om1`; wineco **uat @10.0.0.6 pending**); `V1.2.04` + both `V1.2.99` rollbacks swapped; bridge toolkit watermark → V2.1.16. `mvn test-compile` clean. Hydra impact quantified on dev2: name list over-counted 18,607 vs flag-true 9,221 (9,386 bottles on pick-only `Storage and Picking`); Hydra's comma-less `Storage Picking and Replenish (from)` never matched the list. Pushed 2026-06-11 (`91fb5546..143fa655`). |

## 6. Skipped (with reason)

| v1 SHA | Subject | Reason |
|--------|---------|--------|
| `d0658e79` | Create .sdkmanrc | tooling only — v2 has own toolchain config |
| `f336903a` | remove dead JCenter/Bintray repos | v1 pom cleanup — v2 build independent |
| `7086e6d2` | Club Run Parcels column sort | already-done in v2 (`CustomerorderBatchRepository:102-122` — wrapper + countQuery) |
| ~~`4652a80c`~~ | ~~SBDEV-2384 flag-based replenishable classification~~ | ~~already-done in v2~~ **correction 2026-06-11: misclassified — ported as `143fa65`, see §5** |
| `a3b4a9b` | restore rollbackFor | already-done in v2 (rollbackFor by convention) |
| (93 commits ≤ 2026-05-08) | — | reconciled in prior sweeps (see 2026-05-07 sweep + sync-log) |

## 7. Follow-ups

- [x] Advanced `wms-api` anchor `7f06c6f` → `a3b4a9b` in sync-log.md (first advance off the 2026-03-20 baseline).
- [ ] (Optional) per-method `rollbackFor` parity audit between v1 develop and v2 — low priority; v2 convention already covers it.
- [ ] Confirm v2's exact club-run paged-query projection during the `7086e6d2` port (column list differs slightly from v1).
- [ ] **Apply `V2.1.16` to WineCo's `wh01_om1_v2` on uat (@10.0.0.6)** — hydra-dev2, wineco dev2 (`wh01_om1_v2`) and `dev_wh01_om1` were patched + verified 2026-06-11; the uat copy was unreachable from the session.
- [ ] Sweep-process lesson from the `4652a80c` misclassification: an "already-done" verdict for a fix that touches **both a query and a DB view** must verify *each* read path (and the `V1.2.04`/`V1.2.99` view recreates), not a single line citation.
