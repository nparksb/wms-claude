---
title: "WMS v1 → v2 Sync Sweep — 2026-07-10"
type: workflow
status: draft
version: both
scope: wms-v1-v2-sync
owner: Nam Park
created: 2026-07-10
updated: 2026-07-10
repos_in_scope:
  - wms-api
  - wms-web-ui
  - wms-mobile-ui
related:
  - ../sync-log.md
  - ../README.md
  - ./2026-06-26b-wms-v1-sync.md
  - "[[SBDEV-2496-prshw222-trailing-space-sku-duplication]]"
  - "[[SBDEV-2507-web-palletize-already-truckloaded-guard]]"
  - "[[SBDEV-2512-partitionallowed-split-pick-overstock-guard]]"
  - "[[260709-multi-unitload-replen-reserve-availability-guard]]"
  - "[[260709-duplicate-replenishment-orders-concurrent-generation]]"
  - "[[260709-picking-nirvana-guard-blocks-self-depleting-pick]]"
  - "[[260709-putaway-blank-screen-stale-persisted-state]]"
tags:
  - sync
  - sweep
---

# WMS v1 → v2 Sync Sweep — 2026-07-10

First sweep since 2026-06-26b. Two weeks of v1 activity: a batch of SBDEV-ticketed API fixes (2496 / 2507 / 2512) plus the 2026-07-09 bug-fix wave (duplicate replen orders, multi-UL replen guard, picking nirvana guard, mobile putaway blank screen). **Every pending wms-api commit has a matching v1 plan doc in `sbdocs/1-Projects/wms1/plan/`** — Lane B ports feed those plans into `wms-v2-migrate`.

Pre-flight: `v1-synced-upstream` tags match the log anchors in both v2 UI repos (web `ec6a3c8`, mobile `73d540c`) — no drift. All four working repos clean and level with `origin/develop`.

## 1. Summary

| Repo | Anchor (before) | develop HEAD | Pending (non-merge) | Action |
|------|-----------------|--------------|---------------------|--------|
| wms-api | `b54aa0b` | `8be4afe` | 9 | 6 adapt-and-apply ports (one is a 2-commit group); 1 revert pair not-applicable |
| wms-web-ui | `ec6a3c8` | `ec6a3c8` | 0 | nothing to sync |
| wms-mobile-ui | `73d540c` | `9f71ff5` | 4 | 4 cherry-pick-clean via `ui-sync.sh` |

**Buckets (wms-api):**
- adapt-and-apply: **7 commits / 6 port units** (`decff33`, `9134417`, `30a6ca4`, `1ff0d85`, `1d5d847`+`8be4afe`, `573eff5`)
- not-applicable: **2** (`b9655bf` + `5ea181b` — apply/revert pair, net zero; superseded by the reapply `30a6ca4`)

**Buckets (wms-mobile-ui):**
- cherry-pick-clean: **4** (`296dd1d`, `c052d06`, `4271491`, `ad5a581`)

## 2. Per-repo pending commit table

### wms-api (`b54aa0b..8be4afe`) — Lane B

Merge commits (`2c0cb37`, `74e08ec`, `681ba76`, `7c90d68`, `3be0966`, `940607b`, `923d3bf`, `9814d83`) are passive and omitted.

| v1 SHA | Subject | Lane | Bucket | Proposed action |
|--------|---------|------|--------|-----------------|
| `decff33` | fix(sku): normalize SKU codes to prevent trailing-space itemdata duplication (SBDEV-2496) | B | adapt-and-apply | Port via `wms-v2-migrate`. New `SkuCodes` util + normalization at 4 controller entry points + `Itemdata` model + Flyway `V1.26.31` data-trim migration (v2 needs its own `V2.1.x` number). v1 plan: `[[SBDEV-2496-prshw222-trailing-space-sku-duplication]]`. |
| `9134417` | fix(palletize): reject palletizing a parcel already shipped on a truck-loaded pallet (SBDEV-2507) | B | adapt-and-apply | Port via `wms-v2-migrate`. Shared server-side target-pallet guard in `BillofladingPositionService`, consumed by web (`ParcelMonitorViewService`) + mobile (`MobilePalletizingService`) — closes the web-palletize double-ship gap. v1 plan: `[[SBDEV-2507-web-palletize-already-truckloaded-guard]]`. |
| `b9655bf` | fix(release): honor partitionallowed=false in overstock release (SBDEV-2512) | B | not-applicable | Net-zero: fully reverted by `5ea181b` (PR #192). Content re-lands as `30a6ca4`. No action. |
| `5ea181b` | Revert "fix(release): honor partitionallowed=false…" (SBDEV-2512) | B | not-applicable | Second half of the revert pair. No action. |
| `30a6ca4` | fix(release): honor partitionallowed=false in overstock release (SBDEV-2512, reapply) | B | adapt-and-apply | Port via `wms-v2-migrate`. Guard in `ReleaseOrderJobService` + 2 `WmsConstants` sysprops. Compare against the reverted `b9655bf` for what changed in the reapply. v1 plan: `[[SBDEV-2512-partitionallowed-split-pick-overstock-guard]]`. |
| `1ff0d85` | fix(replenish): reject already-reserved unit loads up front in multi-UL fulfillment | B | adapt-and-apply | Port via `wms-v2-migrate`. Up-front availability guard in `MobileReplenishService` + new `messages_en_US` key. v1 plan: `[[260709-multi-unitload-replen-reserve-availability-guard]]`. |
| `1d5d847` | fix(replenish): serialize generation + idempotent create to stop duplicate replenishment orders | B | adapt-and-apply | Port via `wms-v2-migrate` **together with `8be4afe`** (its ANSI-CAST hotfix). v1 adds `AdvisoryLockRepository` — v2 already has `AdvisoryLockService` (JobLockId registry, used by StaleClubBatchCleanupJob); map onto it rather than duplicating. Duplicate-guard query + `ReplenishOrderJob` + `ReplenishGeneratorService` changes. v1 plan: `[[260709-duplicate-replenishment-orders-concurrent-generation]]`. |
| `573eff5` | fix(picking): detach completing pick line before transfer so nirvana guard passes (SBDEV-2481 interaction) | B | adapt-and-apply ⚠️ | Port via `wms-v2-migrate`, **last in order** — interacts with SBDEV-2481, which v2 implemented differently (`PickLineRealignmentService` + BLOCK_REALIGN loop, v2 `21370b2`/PR #52). Migration analysis must verify whether v2's nirvana guard has the same self-depleting-pick failure mode at all. v1 plan: `[[260709-picking-nirvana-guard-blocks-self-depleting-pick]]`. |
| `8be4afe` | fix(replen): use ANSI CAST instead of :: in duplicate-replen guard query | B | adapt-and-apply | Folded into the `1d5d847` port unit (5-line follow-up to the same query). |

### wms-web-ui — Lane A

Nothing pending: anchor `ec6a3c8` == `origin/develop` HEAD.

### wms-mobile-ui (`73d540c..9f71ff5`) — Lane A

Merges `1097874` (PR #97), `9f71ff5` (PR #98) handled by `ui-sync.sh`.

| v1 SHA | Subject | Lane | Bucket | Proposed action |
|--------|---------|------|--------|-----------------|
| `296dd1d` | fix(replenish): refresh unit load sources before selection | A | cherry-pick-clean | Cherry-pick via `ui-sync.sh`. Pick **in sequence with the next two** — they form one arc (add cache → add session-clearing → strip cache, keep fresh reads). |
| `c052d06` | fix(auth): clear replenish unit load cache on session changes | A | cherry-pick-clean | Cherry-pick. Touches `plugins/axios.js`, `plugins/keycloak.client.js`, `layouts/default.vue` — watch these hunks; most are removed again by `4271491`. Keycloak plugin config differs between v1/v2 — resolve toward v2's config if conflicted. |
| `4271491` | refactor(replenish): drop dead UL cache, keep always-fresh reads | A | cherry-pick-clean | Cherry-pick. Reverts most of `c052d06`; net end-state is always-fresh `replenishUnitLoads` reads. |
| `ad5a581` | fix(putaway): stop blank Putaway screen from stale persisted state | A | cherry-pick-clean | Cherry-pick. Same stale-persistedState family as the known wms2 warehouseTimezone rehydration bug — after landing, sanity-check putaway resume flow. v1 plan: `[[260709-putaway-blank-screen-stale-persisted-state]]`. |

## 3. Per-commit detail — adapt-and-apply

### 3.1 `decff33` — SBDEV-2496 trailing-space SKU normalization

- **Surface:** `SkuCodes` util (trim/normalize), applied in `FileImportController`, `AdviceRestController`, `OrderRestController`, `SkuRestController`; `Itemdata` setter normalization; data-fix migration `V1.26.31__trim_itemdata_item_nr_whitespace.sql`.
- **v2 adaptation points:** controller names/routes may differ; migration renumbers to next free `V2.1.x`; check whether v2's import path already normalizes (v2 had a prior SKU-import port, `887cb32` "Import SKU non-existing shipper code").
- Full plan production deferred to `wms-v2-migrate`.

### 3.2 `9134417` — SBDEV-2507 palletize-onto-truck-loaded-pallet guard

- **Surface:** new guard methods in `BillofladingPositionService` (+44), `ParcelMonitorViewService` consumes for web path, `MobilePalletizingService` refactored to delegate (−24 of local logic).
- **v2 adaptation points:** v2's mobile palletizing already has cancelled-order branches (`MobilePalletizingService:112,147,344` per 2026-05-07 verification); confirm the v1 "Pallet already assigned to gate!" mobile check exists in v2 and hoist to the shared service the same way. Root cause is data-independent (code-level) — port applies regardless of tenant data state.
- Full plan production deferred to `wms-v2-migrate`.

### 3.3 `30a6ca4` — SBDEV-2512 partitionallowed=false overstock release guard

- **Surface:** `ReleaseOrderJobService` (+68), 2 new `WmsConstants` sysprops, 360-line unit test class.
- **v2 adaptation points:** v2's `ReleaseOrderJobService` diverged (pessimistic locks `892169b`, mutable-list fix); locate the overstock-release path before grafting. Sysprop constants follow v2's `WmsConstants` conventions. Check why `b9655bf` was reverted (PR #192) and what the reapply changed — carry the corrected version only.
- Full plan production deferred to `wms-v2-migrate`.

### 3.4 `1ff0d85` — multi-UL replen availability guard

- **Surface:** `MobileReplenishService` (+17), new message key, 255-line unit test class.
- **v2 adaptation points:** v2 `MobileReplenishService` structure; message catalog location.
- Full plan production deferred to `wms-v2-migrate`.

### 3.5 `1d5d847` + `8be4afe` — duplicate replenishment orders (serialize + idempotent create)

- **Surface:** new `AdvisoryLockRepository` (v1), duplicate-guard query in `ReplenishorderRepository` (with the `8be4afe` ANSI-CAST correction), `ReplenishOrderJob` lock acquisition, `ReplenishGeneratorService` idempotent create, 2 ITs + unit tests.
- **v2 adaptation points:** **v2 already has `AdvisoryLockService` with a JobLockId registry** (100006L taken by StaleClubBatchCleanupJob) — port should register a new JobLockId, not introduce a parallel repository. v2 `ReplenishOrderJob` already catches `OptimisticLockingFailureException` at multiple sites; reconcile with the new serialization. ITs land `@Disabled` (SBDEV-2217 harness still broken).
- Full plan production deferred to `wms-v2-migrate`.

### 3.6 `573eff5` — picking nirvana-guard detach (⚠️ SBDEV-2481 interaction)

- **Surface:** `PickingorderBusinessService` (+32) — detach the completing pick line before stock transfer so the nirvana guard doesn't count the line's own demand.
- **v2 adaptation points:** v2's SBDEV-2481 port is architecturally different (dedicated `PickLineRealignmentService`, Hook A/B locks, BLOCK_REALIGN loop — v2 `21370b2`, plus SBDEV-2492's source-sync hook inside that loop). The v1 fix targets an interaction with v1's in-service realignment; `wms-v2-migrate` must first establish whether v2's guard path has the failure mode. Possible verdicts: adapt-and-apply (different locus) or not-applicable. **Port last**, after the replen ports settle.
- Full plan production deferred to `wms-v2-migrate`.

## 4. Planned actions (execution order)

1. **Lane A — mobile-ui:** `./sbdocs/9-System/scripts/ui-sync.sh v1/wms-mobile-ui v2/wms2-mobile-ui` — 4 cherry-picks in commit order, tag advances `73d540c` → `9f71ff5`.
2. **Lane B — wms-api**, one `wms-v2-migrate` + port cycle per unit, independent branches off `develop`, **no stacking** (lesson of PR #51/#52), merge each PR into develop before starting a dependent one:
   1. `decff33` (SBDEV-2496) — standalone.
   2. `9134417` (SBDEV-2507) — standalone.
   3. `30a6ca4` (SBDEV-2512) — standalone.
   4. `1ff0d85` (multi-UL replen guard) — standalone.
   5. `1d5d847`+`8be4afe` (duplicate replen) — same subsystem as (4); do after it to avoid `MobileReplenishService`/replen-repo overlaps.
   6. `573eff5` (nirvana guard) — last; may resolve to not-applicable after v2 analysis.
3. **Anchors:** wms-api → `8be4afe` after all six units resolve; mobile-ui tag advanced by the script.
4. Update `sync-log.md`; close this report.

## 5. Applied commits (filled after execution)

### Lane A — wms2-mobile-ui (2026-07-10 applied; **[PR #15](https://github.com/SiteBossInc/wms2-mobile-ui/pull/15)** squash-merged → develop as `3f1156a`, 2026-07-12)

| v1 SHA | v2 SHA | Notes |
|--------|--------|-------|
| `296dd1d` | `34aa76a` | Clean pick (auto-merge in `store/replenish.js`, `replenish.spec.ts`). |
| `c052d06` | `3a3a283` | 3 conflicts resolved: import blocks in `layouts/default.vue` + `replenish.spec.ts` (kept both sides); **`plugins/keycloak.client.js` resolved to v2 unchanged** — v2's plugin is a custom `keycloak-js` 26 rewrite (SBDEV-2390/2391), v1's cache-clear hunks target the dropped `@dsb-norge/vue-keycloak-js` structure, and `4271491` deletes them again anyway. |
| `4271491` | `ae95561` | Mirror conflicts of the above (removals); resolved to net state. `util/replenishSessionCache.js` deleted. |
| `ad5a581` | `bc98df7` | Clean pick (auto-merge in `store/putaway.js`). |

Merges `1097874` / `9f71ff5` skipped (no content; sequencer `--quit` + manual `--ff-only` merge per script instructions). Tag `v1-synced-upstream` advanced `73d540c` → `9f71ff5`. **Verification:** net v2 diff (`6b36ae4..bc98df7`) is file-for-file and line-count identical to v1's net `73d540c..9f71ff5` (11 files, +110/−53); the transient keycloak/axios/layout hunks net to zero on both sides. No conflict markers; no residual `replenishSessionCache` references. Manual smoke of replenish + putaway flows deferred to user (no mobile test suite).

### Lane B — wms2-api

**Unit 1 — `decff33` (SBDEV-2496) PORTED** (2026-07-10): v2 commits `9aa6b91` + `2edbf0f` + `b4cd676` on `port/SBDEV-2496-sku-normalization`, [PR #66](https://github.com/SiteBossInc/wms2-api/pull/66) → develop (**open, pending merge**). Pipeline: `wms-v2-migrate` analysis → ralplan (Architect APPROVE ×2, Critic ITERATE ×2 → APPROVE) → `wms-tdd-gate` (15 tests, 11 fail-right baseline) → ralph implement → code review (APPROVE, 0 HIGH/0 MEDIUM, 2 LOW fixed) → reviewer verification APPROVED. Scope collapsed vs v1: SkuRest/FileImport-SKU/Advice lookups already done by 260610 (`d1a224d`); ported = order-import path (260610 carve-out 6b, 5 key spaces), OrderBatchCreationService load-bearing bind + guard, Advice transfer orElseThrow, Itemdata setter trim; **NEW-1** (v2-only): advice file-import crash on unknown SKU fixed. **No Flyway migration** (v2 runbook divergence honored); runbook updated (4-surface gated table + unique-constraint correction). Tests 193/0; verify script 16 pass/0 fail. **Deploy gate:** per-tenant 260610 cleanup census (never executed on any tenant — overdue independent of this port). Plan: `sbdocs/1-Projects/wms2/plan/SBDEV-2496-prshw222-trailing-space-sku-duplication.md` (status: implemented).

**Unit 2 — `9134417` (SBDEV-2507) PORTED** (2026-07-10): v2 commits `9686ab1` + `481be6e` + `4a77fbb` on `port/SBDEV-2507-palletize-guard`, [PR #67](https://github.com/SiteBossInc/wms2-api/pull/67) → develop (**open, pending merge**; non-stacked, disjoint from #66). Pipeline: `wms-v2-migrate` → ralplan (Architect ITERATE → **RCA re-grounded for v2**: v2-native truck-loading creates per-parcel BOL positions so the existing guard already blocks the CLOSED vector; Fix A justified as migrated-v1-data coverage + TRANSFER handling + defense-in-depth → Critic APPROVE) → tdd-gate (8 red-right + 3 pinning) → implement → code review (APPROVE, 0 HIGH/0 MEDIUM, 2 LOW fixed). Scope: shared guards in `BillofladingPositionService` (+ctor `UnitloadRepository`), Fix A in both palletise loops (offset after `removeBOLPositionIfExists` — two-layer defense pinned by AC-7a/7b), Fix B reuse branch, mobile 4-site refactor + new pinning test for the uncovered 4th site. **v1's `@Transactional` NOT ported** (v2 already stronger). Tests 87/0; verify 12/0. Doc: `wms2-bol-truck-loading-workflow.md` §4 updated. Plan: `SBDEV-2507-web-palletize-already-truckloaded-guard.md` (implemented).

**Unit 3 — `30a6ca4` (SBDEV-2512) PORTED** (2026-07-10): v2 changes on `port/SBDEV-2512-partitionallowed-guard` (`ReleaseOrderJobService` guard, `WmsConstants` key, `StockunitRepository` S6 comment), PR → develop (**non-stacked**; disjoint from #66/#67 — none touch these files). Pipeline: `wms-v2-migrate` → ralplan (Architect SOUND-WITH-CONDITIONS: 5 conditions incl. NEW-1 rationale rewrite + 3-attribute annotation pin + S6 dual-query guard + v1/v2 map-write divergence note → Critic ITERATE → APPROVE) → `wms-tdd-gate` (3 red-right AC-1/AC-5/AC-6 + 4 pinning AC-2/3/4/7) → ralph implement → code review (APPROVE, 0 HIGH/0 MEDIUM, 3 LOW non-blocking). Scope: **Fix A** cumulative round-2 hold (`reserveSingleCoveringUnit` ledger) + **Fix B** phase-3 single-covering-unit pick + kill-switch `ENFORCE_PARTITIONALLOWED` (default ON) + **NEW-1** `rollbackFor={BusinessException,FacadeException}` on the `releaseOrder` `@Transactional` (fixes a latent pre-existing partial-commit bug on the `:571` "Section not configured" checked throw AND is required before Fix B's second checked throw) + **S6** dual-query equivalence comment. **Existing-test audit:** 7 pre-existing `ReleaseOrderOverstock` tests seeded `partitionallowed=true` (they exercise the unguarded split/greedy path). Tests 36/0 (7 new gate tests); verify script 13 pass/0 fail. Docs: `wms2-sysprop-catalog` (+`ENFORCE_PARTITIONALLOWED`), `wms2-picking-workflow` (§2 guard note + §9 `rollbackFor`). Plan: `SBDEV-2512-partitionallowed-split-pick-overstock-guard.md` (implemented).

> **⚠ Intentional v1/v2 divergence (do NOT "correct" in a future sweep) — SBDEV-2512 Fix A hold branch.** v2 writes `itemDataAvailableAmountUpdateMap.put(itemdataId, available)` **UNDECREMENTED** on hold; v1's Fix A leaves the map untouched. v2's choice is deliberate (a held position reserves nothing → put the fresh aggregate back so later same-batch orders see it; matches v2's own reserved-nothing convention). Architect-verified correct/superior. A `git`-diff of the two Fix A blocks will show this delta — it is not drift.

**Unit 4 — `1ff0d85` (multi-UL replen availability guard) PORTED** (2026-07-10): v2 changes on `port/260709-multiul-replen-availability` (`MobileReplenishService.validateUnitLoadEntry` availability gate + self-source add-back; `messages_en_US.properties` +`MsgUnitLoadStockAlreadyReserved`), PR → develop (**non-stacked**; disjoint from #66/#67/#68). Pipeline: `wms-v2-migrate` → ralplan (Architect SOUND-WITH-CONDITIONS: 4 conditions incl. a **reasoned override** of the Architect's literal plain-`reservedamount` credit rec → Critic ITERATE [M1 assertion style, M2 S4-audit factually wrong, m3 mock arrangement, m4 honest `min()`-inert framing] → revised → Critic APPROVE) → `wms-tdd-gate` (8 gate tests: AC-1/3/5/8 red→green incl. the M1 `hasMessageContaining` trap, AC-2/4/6/7 validation-acceptance) → ralph implement → code review (APPROVE, 0 HIGH/0 MEDIUM; 2 LOW pre-existing). Scope: **single-locus** null-safe availability gate + self-source add-back in `validateUnitLoadEntry`; NO `@Transactional` change (v2's `fulfillMultipleUnitLoads` already had `rollbackFor` — clean port, zero NEW issues, contrast unit 3's NEW-1). **Existing-test audit:** none needed (no existing unit test drives the private `validateUnitLoadEntry` beyond the pre-validation guards; the `checkSource`/`finishReplenishmentOrder` regression pins at ~:1673-1729 fenced do-not-touch). Tests 89/0; verify script 10 pass/0 fail. Doc: `wms2-multi-unitload-replenish.md` Behavior Guarantees + guard note. Plan: `260709-multi-unitload-replen-reserve-availability-guard.md` (implemented).

> **⚠ Intentional v1/v2 divergence (do NOT "correct" in a future sweep) — 260709 self-source add-back credit.** v2 credits `min(requestedamount, reservedamount)` under a `reservedamount > 0` guard, with a **null-safe** reserved read (not `getAvailableamount()`); v1 credits plain `requestedamount` via `getAvailableamount()`. The v2 form (a) null-guards the nullable `reservedamount` column (NPE fix), (b) fixes a `reserved==0` over-credit via the `reserved>0` guard. It is **behaviorally identical to v1 wherever `reserved ≥ requested`** (all known-reachable self-source states — v1's plain `requestedamount` already safely rejected the co-reserved case); the `min()` cap only diverges in the atypical `0 < reserved < requested` partial-leak. A `git`-diff of the two `validateUnitLoadEntry` bodies will show this delta — it is not drift. (The Architect's literal plain-`reservedamount` rec was rejected: it would ride `applyExplicitSourceToOrder`'s pre-existing over-release and accept a co-reserved request that destroys another order's reservation.)

**Unit 5 — `1d5d847` + `8be4afe` (duplicate replenishment orders) PORTED** (2026-07-11): v2 commit `98732de` on `port/260709-duplicate-replen-graceful`, [PR #70](https://github.com/SiteBossInc/wms2-api/pull/70) → develop (**open, pending merge**; non-stacked, disjoint from #66–#69). Pipeline: `wms-v2-migrate` (architect + tracer pre-investigation) → **user-approved scope decision** (index + graceful handling, NO advisory lock) → ralplan (Architect SOUND-WITH-CONDITIONS: C1 couple §3.1+§3.2 + real-proxy slice, C2 constraint-name discriminator, C3 deterministic flush, C4 template residual → Critic ITERATE [MAJOR-1 slice must be raw-Testcontainers not `@DataJpaTest`, MAJOR-2 NEW-1 atomicity test, MAJOR-3 branch off clean develop not the reorg] → closed) → `wms-tdd-gate` (3 red-right + 5 guards/pinning) → executor implement → code-review APPROVE (0 High/Med; 4 LOW, 2 fixed) → verifier PASS. **The port INVERTS v1's core decision:** Fix A already present in v2 (`ReplenishOrderJob` JVM CAS + `AdvisoryLockService.tryLock(REPLENISH_ORDER)`); Fix B **obviated by v2's partial UNIQUE index** `idx_replenishorder_active_item_dest`/`_no_dest` (`state<700`) — the DB guard v1 rejected, safe in v2 because the multi-UL split is finish-then-create-then-finish; `8be4afe` ANSI-CAST **N/A** (v2 dedup pre-check is Java-side). Actual work: **NEW-1** (self-inject + route 3 same-bean `calculateOrder` calls via `self` so the 4-arg `REQUIRES_NEW` tenant tx engages on the mobile path → atomic save+reserve) + **graceful handling** (catch active-dup `DataIntegrityViolationException`/`UnexpectedRollbackException` outside the failed tx in the 3-arg + `create`, cause-chain-walk discriminator on `idx_replenishorder_active_item` with re-check fallback, `return null` idempotent skip; `flush()` after save). Files: `ReplenishGeneratorService`, `ReplenishorderService`, `ReplenishorderRepository` (widened to `JpaRepository`). Tests 85/0; verify script 10/0. **AC-6 job-nesting + AC-2 atomicity slice `@Disabled(SBDEV-2217)`** — behavioral gate is the manual test plan. Fix C conditional per tenant (expected zero dups). Plan: `260709-duplicate-replenishment-orders-concurrent-generation.md` (implemented).

> **⚠ Intentional v1/v2 divergence (do NOT "correct" in a future sweep) — Unit 5 uses NO advisory lock.** v1 (`1d5d847`) serializes generation + adds a blocking per-demand advisory lock. v2 relies on the pre-existing partial UNIQUE index (`idx_replenishorder_active_item_dest`/`_no_dest`) for committed-duplicate prevention (stronger than the lock, and compatible with v2's finish-then-create split) + graceful `DataIntegrityViolationException` handling. A `git`-diff of the two `calculateOrder` bodies will show v2 has no `AdvisoryLockRepository`/`pg_advisory_xact_lock` — deliberate, not drift (verify-script check #8 guards it).

**Unit 6 — `573eff5` (picking nirvana-guard detach) NOT-APPLICABLE** (2026-07-12): v2 analysis via `wms-v2-migrate` (tracer pre-investigation + independent grep verification). **Verdict: v2 already correct — no code change, no PR.** v2's SBDEV-2481 port (`21370b2`/`0baee3a`, PR #52) **removed the inline pick-line guard from `sendStockUnitToNirvana`** (v2 `StockunitBusinessService:401-426` has no `findByPickfromstockunitId`) and relocated it to `PickLineRealignmentService.assertNoActivePickFor`, invoked only for `BLOCK_REALIGN` activity codes (`StockunitBusinessService:355-356`). `CODE_PICKING` is `PASS_THROUGH` (`PickLineActivityCodeClassifier:60`, not in `BLOCK_REALIGN_CODES` `:33-37`), so the guard is unreachable on the picking-confirm path. v1's late-FK-null ordering is structurally still present in v2 (`PickingorderBusinessService:552` transfer → `:568` null) **but harmless** (nothing reads the live FK on that path). Completeness: every `findByPickfromstockunitId` caller is `PickLineRealignmentService` (89/102/125) or read-only `MobileInfoService:369`. **ralplan skipped** (zero-code-change N/A verdict — matches prior not-applicable analyses in `sync-log.md`). Plan: `260709-picking-nirvana-guard-blocks-self-depleting-pick.md` (status: not-applicable). Re-open triggers documented in the plan §3 (if `CODE_PICKING` ever becomes BLOCK_REALIGN, or a `findByPickfromstockunitId` guard returns to the branch-B path).

## 8. Sweep closure

All six Lane B units resolved: **Units 1–5 PORTED** (PRs #66–#70), **Unit 6 NOT-APPLICABLE** (no PR). Lane A (mobile-ui) 4 cherry-picks landed (local develop, not pushed). Web-ui nothing pending.

**✅ Sweep COMPLETE — anchor advanced `b54aa0b → 8be4afe` (2026-07-12).** All five Lane B PRs squash-merged into develop: #67 `1632b92`, #68 `79c8d0c`, #69 `7cf0c66`, #70 `c8f3f74`, #66 `997b07e` (develop HEAD). #66 required a develop-merge conflict resolve first (union of `assertj`/`hamcrest` static imports in `FileImportControllerUnitTest` — a pure import-block collision; its 6 test classes stayed green, 196/0); the other four merged clean. develop **clean-compiles + test-compiles with all 5 ports combined** (integration gate — the ports were individually verified but never together until now). All PRs were non-stacked, so no repeat of the 2026-06-25 orphan trap. Unit 6 needed no merge (N/A). **Lane A mobile-ui merged** — PR #15 squash-merged to wms2-mobile-ui develop (`3f1156a`). **Entire sweep now merged.** (Separate: the migration-dir reorg PR #71 also merged to wms2-api develop `43384c0` — tracked in SBDEV-2560, not part of this sweep.)

## 6. Skipped with reason (filled after execution)

- `b9655bf` + `5ea181b` (wms-api) — apply/revert pair, net zero; content carried by the reapply `30a6ca4`.
- `1097874`, `9f71ff5` (mobile-ui merges) and wms-api merge commits — passive, no content.

## 7. Follow-ups

- SBDEV-2217 (v2 Testcontainers lane) still broken — any ported ITs land `@Disabled` with TODO markers.
- v1 verify scripts exist for four of the port units (`sbdocs/9-System/scripts/verify-260709-*.sh`, `verify-SBDEV-2512-*.sh`) — consider adapting them as v2 post-port verification.
