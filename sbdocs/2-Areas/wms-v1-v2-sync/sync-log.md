---
title: "WMS v1 → v2 Sync Log"
type: workflow
status: active
version: both
scope: wms-v1-v2-sync
owner: Nam Park
created: 2026-04-18
updated: 2026-06-18
last_verified: 2026-06-18
verified_by: sweep 2026-06-18 (Lane A — web-ui d44f593→8730f94, mobile-ui 226537b→db6f573)
related:
  - ./README.md
  - ./sweeps/2026-04-18-wms-v1-sync.md
  - ./sweeps/2026-05-02-wms-v1-sync.md
  - ./sweeps/2026-05-07-wms-v1-sync.md
  - ./sweeps/2026-06-09-wms-v1-sync.md
  - ./sweeps/2026-06-18-wms-v1-sync.md
tags:
  - sync
  - log
---

# WMS v1 → v2 Sync Log

Running record of every sweep — what was synced, when, and up to which v1 SHA.

## Anchor columns

- **`wms-api`** — last v1 SHA that has been ported to v2/wms2-api. **Tracked only in this log** (not by a git tag) because Lane B is per-commit via `wms-v2-migrate`.
- **`wms-web-ui`** — last v1 SHA synced into v2/wms2-web-ui. Also set as the `v1-synced-upstream` git tag in the v2 repo; `ui-sync.sh` reads and advances it.
- **`wms-mobile-ui`** — same as above, for v2/wms2-mobile-ui.

## Sweeps

| Date | wms-api | wms-web-ui | wms-mobile-ui | Notes |
|------|---------|------------|---------------|-------|
| 2026-03-20 | `7f06c6f` | `5d3e91d` | `f579ad6` | **Historical baseline** — prior bulk sync "as of March 20, 2026" (e.g., v2 commit `8883ee8` on wms2-mobile-ui). Recorded 2026-04-18 retroactively for completeness. |
| 2026-04-18 | `7f06c6f` *(no API sweep yet)* | `5d3e91d` | `04c9c08` | Backfill sweep + anchor setup. **Web:** 0 pending commits post-baseline. **Mobile:** 7 pending reviewed — ported `2def879` (scan tote focus) and `04c9c08` (error message spelling); skipped `d88adea`/`f6eee3c` (label rename + revert, net zero — v2 already has the final label via `ace9948`), `223fdb1`/`bc6dc84` (amount validation + revert, net zero — proper fix landed via `ace9948`), and `d37dbfe` (= `ace9948` in v2, patch-equivalent). **API:** 87 commits pending — deferred to API-focused sessions. See `sweeps/2026-04-18-wms-v1-sync.md`. |
| 2026-05-02 | `7f06c6f` *(no API ports — branch retarget only)* | `aa99394` | `f285edc` | **Branch retarget:** `ui-sync.sh` V1_BRANCH and V2_BRANCH changed from `develop-arden` → `develop` for all repos. **Web:** cherry-picked 3 commits from v1/develop-arden (`20ab082` toast errors, `29e62e0` error message standardisation, `aa99394` undefined item.id fix) with conflict resolution (kept v2's optional-chaining error extractor; kept v2's nuxt.config.js plugin load order). **Mobile:** re-anchored from `04c9c08` (develop-arden only) to `ac685f6` (v1/develop — Merge PR #89 of develop-arden); 20 post-merge commits on v1/develop now in scope for next sweep. **API:** anchor unchanged; 28 unreviewed commits on v1/develop now in scope for next sweep (SBDEV-2163, SBDEV-2164, SBDEV-1699 hotfixes, runClubLine hardening, changeReservedAmount rebinds, putaway null-safe fix). See `sweeps/2026-05-02-wms-v1-sync.md`. |
| 2026-05-02 | `7f06c6f` *(anchor unchanged — 3 cherry-picks landed out of order)* | `aa99394` | `f285edc` | **API cherry-picks:** ported v1 `2351004` + `142f7bb` + `c70569e` (changeReservedAmount caller rebind — Fix A/B/E) as a single v2 commit `e7e4464` (wms2-api `develop`). These 3 SHAs are scattered within the 28 pending commits after `7f06c6f`; 25 intermediate commits remain unreviewed — anchor stays at `7f06c6f` to preserve sweep continuity. Plan: `sbdocs/1-Projects/wms2/plan/260502-changereservedamount-caller-rebind-fix.md`. 3811 tests pass. |
| 2026-05-03 | `7f06c6f` *(anchor unchanged — out-of-order cherry-pick)* | `aa99394` | `f285edc` | **API cherry-pick:** ported v1 `1715962` (SBDEV-2163 — prevent finished club batch staging-lane reassignment) as v2 commit `049dde0` (wms2-api `develop`). Guard + `batchHasNoActiveOrders` helper added to `CustomerorderBatchService.assignStagingLaneToOrderBatch`. SHA sits within the 25 remaining unreviewed commits — anchor stays at `7f06c6f`. Plan: `sbdocs/1-Projects/wms2/plan/SBDEV-2163-prevent-finished-club-batch-lane-reassignment.md`. 3815 tests pass. |
| 2026-05-03 | `7f06c6f` *(anchor unchanged — out-of-order port)* | `aa99394` | `f285edc` | **API port:** ported v1 `b746c39` + `38474e8` (SBDEV-2164 — stale club batch cleanup cron job) as v2 commit `57ec70e` (wms2-api `develop`). New files: `StaleClubBatchCleanupJob`, `StaleClubBatchCleanupJobService`, `V2.1.08__stale_club_batch_cleanup_sysprops.sql`. Modified: `CustomerorderBatchRepository` (findStaleClubBatchIds), `SchedulingConfiguration`, `WmsConstants` (6 sysprop constants), `AdvisoryLockService` (JobLockId 100006L). v2-only fix: `@Transactional(tenantTransactionManager)` on `CustomerorderBatchService.finalizeBatchIfComplete` (NEW-1). 9 tests: A1–A8 pass, A9–A10 @Disabled (Testcontainers/SBDEV-2099), A11 pass. Full suite: 3826 run, 0 failures. Plan: `sbdocs/1-Projects/wms2/plan/SBDEV-2164-stale-club-batch-cleanup-cron.md`. |
| 2026-05-03 | `7f06c6f` | `aa99394` | `f285edc` ✅ | **Mobile-UI fully synced — verified.** v1 local develop HEAD (`d02a5a3`) is the merge-base of `f285edc` (GitHub hotfix merge). All commits in `f285edc`'s ancestry (SBDEV-1699, optional chaining, missing index reset, CLAUDE.md, Playwright setup) were already cherry-picked into v2 in a prior session. `v1-synced-upstream` tag correctly points to `f285edc`. Nothing pending. Local v1 develop is behind GitHub remote (no `git pull`) — not a v2 sync gap. |
| 2026-05-03 | `7f06c6f` *(anchor unchanged — out-of-order port)* | `aa99394` | `f285edc` | **API port:** ported v1 `f46cf06` + `827a77e` (runClubLine transaction hardening + null-safe sysprop parsing) as v2 commits `6a01749` (Phase 1: Fix B + Fix F2) + `8de5f4b` (Phase 2: Fix F5). Modified: `FixLocationAssignmentService` (parseSyspropLong helper), `ReplenishOrderJob` (same helper), `UnitloadRepository` (findByLabelidForUpdate), `ClubLineOrderProcessor` (parcel-label collision guard), `CustomerorderBatchService` (enriched shortfall message). 5 new gate tests (A1–A5). Full suite: 3846 run, 0 failures. Plan: `sbdocs/1-Projects/wms2/plan/260503-runclubline-transaction-boundary-hardening.md`. |
| 2026-05-03 | `7f06c6f` *(anchor unchanged — already-done analysis)* | `aa99394` | `f285edc` | **API already-done:** v1 `07ae9ab` (Inbound Notices qty sort fix) — analysis confirms v2 `AdviceRepository` already has `qtyRequired`/`qtyReceived` via correlated subqueries (L85-86, L114-115), explicit `countQuery` (L91-93, L120-122), and `AdviceNoticeView` projection with no Java-side qty calc. No code changes needed. Plan: `sbdocs/1-Projects/wms2/plan/260307-inbound-notices-sort-fix.md`. |
| 2026-05-03 | `7f06c6f` *(anchor unchanged — not-applicable analysis)* | `aa99394` | `f285edc` | **API not-applicable:** v1 `ed2a4b8` (BOL exception handling + closing logic cleanup) — all 3 sub-changes not applicable: (1) `BolClosedEventListener` eliminated in v2, no OMS notification in `closeBOL`; (2) `clearCarrierUnitloadByIds` never introduced in v2; (3) `bolToClose` already upgraded to `ConcurrentHashMap.newKeySet()` with documented single-JVM limitation (L147–150). No code changes needed. Plan: `sbdocs/1-Projects/wms2/plan/260414-bol-exception-handling-cleanup.md`. **All pending items from 2026-05-02 sweep now resolved. Ready to advance wms-api anchor in next formal sweep.** |
| 2026-05-07 | `7f06c6f` *(anchor unchanged — UI-only sweep)* | `1e647d9` ✅ | `37d8ad6` ✅ | **Web-UI:** cherry-picked 3 commits from v1/develop — `4ec0571` (SBDEV-2099 outbound parcel report filter preserved after palletize; v2 `ac9280b`), `c37f5f1` (SBDEV-2099 additional — fix bitwise `&` → `&&` + empty string guard in `searchReport`; v2 `0baed09`), `096a3f0` (hid edit icons in locationType + storageLocation; v2 `62bfbf2`). Skipped as patch-equivalent: `bcd0061` (SBDEV-2100 spelling), `1835a63` (undefined item.id in transferStock). **Mobile-UI:** cherry-picked `37d8ad6` (added back deleted `package.json`) as v2 `3a820bb` — conflict resolved: kept v2's `keycloak-js` 26.2.0 (dropped v1's `@dsb-norge/vue-keycloak-js`); added `.claude/settings.local.json` to `.gitignore`. **API:** anchor unchanged; ~34 unique fixes pending — see `sweeps/2026-05-07-wms-v1-sync.md`. |
| 2026-05-07 | `7f06c6f` *(anchor unchanged — out-of-order Group R port)* | `1e647d9` | `37d8ad6` | **API port (Group R, SBDEV-1699):** ported v1 `f52c69e`+`41140ce` (Replen — qty req not matching) as v2 commit `c4fcfc1` (wms2-api `develop`). Surface A: `ViewDtoService.getStockPerLocation` rewritten to batched `findByItemdataIdIn` (1 SELECT/page request). Surface B: `ReplenishmentMonitorViewRepository` SQL gains `f.upperbound AS fix_assignment_upperbound` (t5 subquery, outer SELECT, GROUP BY); `ReplenishMonitorSummaryView` adds `getFix_assignment_upperbound()` getter; `ViewDtoService.getReplenishMonitorViewSummary` emits DTO `locationStock`. TM hardening: both call sites gain `@Transactional(value = "tenantTransactionManager", readOnly = true)`. 3 unit tests added (A4 + A9a + A9b). **Group R verification:** other 4 fixes (`d8e1600`, `08f8ae7`, `b7707bf`, `a377010`) confirmed already-done in v2. Plan: `sbdocs/1-Projects/wms2/plan/SBDEV-1699-replenish-qty-requested-wrong-upperbound.md` (ralplan iter 3 — Architect + Critic approved). |
| 2026-05-07 | `7f06c6f` *(anchor unchanged — Group C verification only)* | `1e647d9` | `37d8ad6` | **API verification (Group C — Cron / scheduler):** all 3 candidate fixes confirmed addressed in v2. (1) `cc7bdfd` (mutable list hot fix) — done at `ReleaseOrderJobService.java:652` (`new ArrayList<>(Collections.singletonList(order))`). (2) `f0bef13` (auto-flush optimistic lock) — superseded + better: v2 uses `@Cacheable` on `SyspropService` (5-min TTL via `CacheConfig`) instead of v1's 30s volatile cache; `OptimisticLockingFailureException` catch added at 9 locations across `ReplenishOrderJob` + `OrderReleaseJob`. (3) `204d266` (stale entity in `recalculateOpenOrders`) — done via v2 commit `b68cbbf` ("implemented the plan '260331-recalculate-orders-stale-entity-debug-plan.md'"). Verified at `ReplenishmentOrderMaintenanceService.java:67-90` (no @Transactional on `recalculateOpenOrders`), :93-113 (no @Transactional on `recalculateForItem`), :119-127 (re-fetch in `recalculateOrder`). No new issues. No code changes. |
| 2026-05-07 | `7f06c6f` *(anchor unchanged — phase1 + phase2 sync-branch cherry-picks)* | `1e647d9` | `37d8ad6` | **API ports (cherry-picked from `sync/wms2-api/phase1-standalone-bugs` and `sync/wms2-api/phase2-pallet-name-validation` after discovering both branches were stale by ~3000 lines vs develop):** `887cb32` (`port v1 4c8d500` Import SKU non-existing shipper code, Group M), `cd73716` (`port v1 98fce54` new BOL no shipped date, Group B), `a8af84f` (`port v1 8957b9a` empty BOL export no SKU rows, Group B), `e2b82ed` (`port v1 46130c3` unify parcel cancelled message, Group X), `a9190c6` (`test(phase1)` covering 3 ports), `d7ec79f` (`port v1 d52046e` pallet name dup validation, Group X), `2ba264d` (`test(phase2)`). 163 tests pass post cherry-picks. Sync branches NOT merged; pending cleanup. |
| 2026-05-07 | `7f06c6f` *(anchor unchanged — net-new Group T port)* | `1e647d9` | `37d8ad6` | **API port (Group T, v1 5ada0b0):** ported `5ada0b0` (null transferlane crash) as v2 commit `24280b0`. Three v2 fixes: (1) `TransferOrderService.unlinkTransferLaneFromTransferOrder` now resets state to `CUSTOMER_ORDER_ACTIVATED` alongside clearing `transferlane_id` (was missing in v2); (2) `MobileTransferOrderService.{updateOrder, updateOrderPosition, updateOrderList}` add data-invariant guards + LOG.error diagnostics; (3) `TransferOrderController.processOrderPositionSelect` wraps service call in try/catch BusinessException → standard error envelope (matches existing `processScanUnitLoad` pattern). `updateOrderPosition` signature gains `throws BusinessException`. 4 new tests + 1 enhanced test (state-reset assertion) + 3 existing-signature compile fixups. 71 tests in transfer classes pass. v2-skipped: V1.1.06 SQL migration (no broken rows in v2 yet); dead-code removal of `assignTransferLane(Customerorder)` deferred. |
| 2026-05-07 | `7f06c6f` *(anchor unchanged — 7 deferred-SHA verification only)* | `1e647d9` | `37d8ad6` | **API verification (7 deferred SHAs from sweep — all confirmed addressed in v2):** (1) `423bced` (Phase 1 txn-scoped OMS callbacks) — ALREADY DONE: `PickingorderBusinessService:506-511` afterCommit + `MobilePickingService:483-487, :993-997` afterCommit, both with `TransactionSynchronizationManager.isSynchronizationActive()` guard (stronger than v1). (2) `a6c283e` (`.equals` → `.getId().equals(...)`) — ALREADY DONE: `OrderRestController:841` already uses ID comparison. (3) `0000b38` (staging-lane fixes) — ALREADY DONE: `BillofladingService:751` clears staginglaneId at ORDER_BATCH_CLUB_RUN_FINISHED + `CustomerorderBatchService:340, :358-360` allCanceled handling. (4) `faaab8b` (`getAmountSKU` query fix) — ALREADY DONE + BETTER: `CustomerorderBatchService:386` uses `findByOrderId` AND adds extra `removeIf state==CANCELED` filter at L382. (5) `1334bf1` (port develop-arden gaps — cancel-PACKED, race locks, SQL filters) — ALREADY DONE: every sub-item verified (state≠800 filters across 3 repositories; `forceCancelOrder` package-private at `CustomerorderService:323`; PACKED delegation at L607-608; `cancelBatch` rewrite at `CustomerorderBatchService:220-320+`; `findByIdForUpdate` at MobilePickingService 4 sites; `@Transactional(tenantTransactionManager)` on 5+ methods; PickingController 9 catch pairs `ObjectOptimisticLockingFailureException + PessimisticLockingFailureException`; BOL default sort = `desc("modified")`). (6) `66c79af` (Phase 3 + Location/LocationType CRUD) — ALREADY DONE: MobilePalletizingService cancelled-order branches at :112,147,344; StockunitService.setLockDamaged forces UNIT_LOAD_TYPE_BOX at :369; ReplenishorderRepository.findByStateAndItemdataId at :46; LocationController has create/update/createLocationType/updateLocationType endpoints at :58,77,96,115; LocationService put areaId/rackId/typeId at :250,257,264. The 3 retained `triggerReplenishmentMaintenance` calls in StockunitService (:252, :388, :428) are v2's intentional deduplicated state — match v1 final state (after `08f8ae7` + `b7707bf`); not a regression. (7) `26cc07c` (renamed flyway scripts) — NOT APPLICABLE: v2 maintains independent migration sequence at V2.1.01–V2.1.03 with completely different filenames + content; no v2 migration checksum can be invalidated. v1's V1.1.06 hotfix script for stuck `state=510 AND transferlane_id IS NULL` rows was wineco-dev-only data; not needed in v2 unless symptom appears. **No code changes for any of the 7.** |
| 2026-05-07 | `7f06c6f` *(anchor unchanged — Group A verification only)* | `1e647d9` | `37d8ad6` | **API verification (Group A — Advice):** v1 `cfd9269` (advice persist on close — missing save) confirmed addressed in v2 with a stronger fix. v2 `AdviceService.close()` at line 290-291 uses `saveAndFlush(advice)` (not just `save`) and the method itself has `@Transactional(value = "tenantTransactionManager", rollbackFor = {BusinessException.class, FacadeException.class})` at line 263. v1 fix added save() but did NOT add @Transactional (cited as the root cause in v1 commit message). v2 commits `730aa35` (Feb 28 — flush before bulk JPQL UPDATE) + `9ff08be` (Feb 23 — Hibernate 6 LocalDate fix) collectively cover the full root cause plus the JPQL eviction edge case. No port needed. |
| 2026-05-07 | `7f06c6f` *(anchor unchanged — Group T/B/M/X verification)* | `1e647d9` | `37d8ad6` | **API verification (Group T/B/M/X):** beyond the 7 cherry-picks + 5ada0b0 port logged above, remaining group fixes confirmed addressed in v2: `d72fef2` (null-safe section lookup, Group T) — done at `CustomerorderPositionService:53-67`; `e26e095` (inventory transfer data inconsistency, Group T) — superseded by v2 `a849bfa` (perf: batch pre-fetch in getTransferLineUnitLoads + calcOptimized) which fixes the underlying N+1 differently; `83dddd8` (palletize filter, Group B) — done at `ReportController:367-373` + `ViewDtoService:1334-1338`; `c53f33b` (test merge artifact) — N/A (v2 already 4-arg); `8f71354` (log map size, Group X) — done at `ReleaseOrderJobService:103-104`; `3db0ee3` + `3eca978` (test fixes) — N/A (v2 BillofladingService doesn't use eventPublisher.publishEvent or messageService.createMessage; v1 test fixtures don't apply). No code changes for these. |
| 2026-05-07 | `7f06c6f` *(anchor unchanged — Group P verification only)* | `1e647d9` | `37d8ad6` | **API verification (Group P — Picking):** all 8 candidate fixes confirmed addressed in v2. Already done: `7ac04aa`/`5d8326b` (getBoxTypeNameFromUnitLoad null-check at StockunitService.java:559-566), `3e3c825` (processPick re-read; v2 uses `findByIdForUpdate` at MobilePickingService.java:388-395 — stronger than v1's `findById`), `f3a08d1` (single-SKU OMS race; PickingorderBusinessService.java:256-277), `54ccb78` (CANCELED save; PickingOrderMergeService.java:190-195 uses `saveAll` — stronger than v1's per-item loop), `64968a2` (releaseOrder pessimistic lock; ReleaseOrderJobService.java:107 + PickingorderBusinessService.java:174-177 — implemented as v2 `892169b`), `e516e38` (refresh stale; StockunitBusinessService.java:401-405 — implemented as v2 `930be52`), `7e1b5e6` (lambda compile fix; PickingorderBusinessService.java:431). N/A: `0ecc20e` (v2 architecturally divergent — `findAllByIdForUpdate` bulk pattern). **Decision on Fix #1 partial (`recalculateForItem` lacks @Transactional):** intentionally not ported. v1's REQUIRES_NEW caused JVM self-deadlock (fixed by `d8e1600`); v2's no-annotation + try/catch in `triggerReplenishmentMaintenance:121-127` achieves rollback isolation differently and works correctly. Already-ported candidates verified: `4d73546`+`6c65461` → v2 `da64cc0`; `1752452`/`90ef812` → v2 `99340b0`; `7cf29a9` → v2 `4430824`. Open follow-up (NEW-3, separate investigation): potential lock-ordering inconsistency between `processPick` (PO→CO) and `confirmPick` (CO→PO). No code changes. |
| 2026-05-08 | `7f06c6f` *(no API sweep — Lane A only)* | `d44f593` ✅ | `4c033b9` | **Lane A UI sweep.** **Web-UI:** cherry-picked `1d80e43` (replenish monitor showing bottle count in parcels field — `value: 'qtyNeeded'` → `'qtyHold'`) as v2 `d405de7`. Skipped as v2-already-better: `20ab082` (toast errors in catch blocks — v2 already has `26069e9` with user-friendly "Request failed due to a network or server issue. Please retry." messages); `29e62e0` (toast checker dedup — v2 already has `2639596` w/ same intent). Skipped patch-equivalent: `aa99394` (item.id bulk process — v2 `d933982`). Tag advanced to v1 HEAD `d44f593`. **Mobile-UI:** cherry-picked `43dd052` (trim text fields across ScanInput + cycleCount + moveStock + putaway + picking + transferOrder + truckLoading — 22 files) as v2 `2569b9d`. Skipped as net-zero revert pairs: `d88adea`+`f6eee3c` (rename + revert), `223fdb1`+`bc6dc84` (amount validation + revert). Skipped as already-in-v2-via-`ace9948`: `d37dbfe` (1.26.12 hotfix — patch-id `53ca2bba...` matches v1 `91e8732` already ported in `ace9948`). Skipped patch-equivalent: `04c9c08`, `4c033b9`. Tag advanced to `4c033b9`. **Deferred to next sweep:** `9029a16` (toast checker dedup — needs `plugins/toast.js` install + ~14 store/* error-message upgrades; v2 still has raw `'Error: ' + error` form). **Marked unnecessary:** `93ccd30` (re-add deleted `package.json`) — v2/wms2-mobile-ui maintains its own `package.json` with v2-specific deps (e.g., `keycloak-js@26.x`); v1's restored file is irrelevant. Will be skipped on next sweep regardless of tag position. The cherry-picked `43dd052` will be auto-skipped by `git cherry` next sweep as patch-equivalent. |
| 2026-05-08 | `7f06c6f` *(no API sweep — Lane A carry-over)* | `d44f593` ✅ | `226537b` ✅ | **Lane A carry-over (mobile-ui).** Ported v1 `9029a16` (toast dedup plugin + standard error message) as v2 `e0983e6`. New `plugins/toast.js` (104 LoC wrapper with 2-second sliding-window dedup keyed on `${type}:${message}`); `nuxt.config.js` appended `{ src: '@/plugins/toast.js', mode: 'client' }` after `cookie.js` (preserving mandatory plugin load order from CLAUDE.md); 12 store files (`home`, `index`, `picking`, `palletizing`, `moveUnitload`, `transferOrder`, `cycleCount`, `lookup`, `putaway`, `moveStock`, `truckLoading`, `replenish`) had `this.$toast.error('Error: ' + error)` swapped for the polished sentence — 65 occurrences total, including 4 lines in `home.js`+`index.js`+`picking.js` that were uncommented in the same operation per v1 9029a16. **Skipped from v1 9029a16 hunks:** `config/keycloak.json` (v1 silently downgraded `https` → `http` for `kc.dev.sbo.li` — TLS regression; v2 keycloak config is independent and stays on TLS). Tag advanced to v1 HEAD `226537b`: `9029a16` is now consumed (manually ported), `93ccd30` was previously marked unnecessary, intermediate merge commits (`fa80383`, `a535b55`, `226537b`) are passive. **Mobile-UI is now fully synced with v1 develop.** No tests added (mobile UI has no test suite per CLAUDE.md). Manual smoke verification deferred to user. |
| 2026-06-09 | `a3b4a9b` ✅ | `d44f593` | `226537b` | **API sweep — `wms-api` anchor advanced `7f06c6f` → `a3b4a9b` (first advance off the 2026-03-20 baseline).** Reconciled all 99 non-merge commits in `7f06c6f..develop`: 93 (≤2026-05-08) already resolved in prior sweeps; of the 6 new (post-05-08) commits, ported **`927b8379`** (SBDEV-2033 — drop `triggerReplenishmentMaintenance` from `transferStock`; re-reserved just-released stock on manual split/move) as v2 **`38fcc13`** (wms2-api `develop`, **not pushed**). Already-done in v2: `7086e6d2` (club-run Parcels sort — `CustomerorderBatchRepository:102-122` already wraps query + has countQuery), ~~`4652a80c` (SBDEV-2384 flag-based classification — `ReplenishmentMonitorViewRepository:73`)~~ **[corrected 2026-06-11 — see next row]**, `a3b4a9b` (rollbackFor — v2 convention). Not-applicable: `d0658e79` (.sdkmanrc), `f336903a` (build POM). **Prerequisite this session:** release-only hotfix `64d84cf7` (restore `rollbackFor`, 12 files) cherry-picked into v1 `develop` as `a3b4a9b`, build-verified (Java 8 `mvn package`), pushed to `origin/develop`. v2 port: `mvn test-compile` clean (Java 21); code-reviewer APPROVE. See `sweeps/2026-06-09-wms-v1-sync.md`. |
| 2026-06-18 | `a3b4a9b` *(no API sweep — Lane A only)* | `8730f94` ✅ | `db6f573` ✅ | **Lane A UI sweep (web-ui + mobile-ui).** First Lane A run on this Linux clone — back-filled the missing one-time plumbing (`v1` remote + `v1-synced-upstream` tag at the recorded anchors; not drift — tag never existed here, log was authoritative). **Web-UI:** of 25 commits in `d44f593..develop`, only `28e6009` (Transfer list→detail `isInitializingShipDate` watcher guard) was genuinely new → ported as v2 `9879247`. Skipped: 20 already-in-v2 (17 via `git cherry -`, plus `608d4d3`/`523e4e2`/`80041f4` which `git cherry` mis-flagged `+` but were already applied in v2 — empty/whitespace-only cherry-picks), `7fb2a04` Dockerfile node 18→24 (v2 already `node:24.0.0`), `bd06d46` v1-only portainer CI + BRANCHING.md (not-applicable), 2 merges. Tag → `8730f94`. **Mobile-UI:** ported `1cf9eca` (`.trim()` on scannedValue across 4 scan screens; follow-on to `43dd052`) → v2 `510c945`. Skipped: `e0f1e88` Dockerfile (v2 already node:24), `db6f573` v1-only portainer CI (not-applicable). Tag → `db6f573`. **Neither v2 repo pushed** (review-first). No tests (UI repos have minimal/no suites). See `sweeps/2026-06-18-wms-v1-sync.md`. |
| 2026-06-11 | `a3b4a9b` *(anchor unchanged — correction of 2026-06-09 disposition)* | `d44f593` | `226537b` | **Correction: `4652a80c` (SBDEV-2384) was misclassified already-done.** The 06-09 verdict cited `ReplenishmentMonitorViewRepository:73`, which is the `available_amount` bucket — flag-based in v1 too (never the defect). The defective `on_replenishable_location(_names)` predicates still carried the hardcoded area-name list in v2 (`:92`/`:96`) **and** in the UTC-toolkit view recreates (`V1.2.04`, both `V1.2.99` rollbacks). Ported as wms2-api **`143fa65`** (`feature/utc-timezone`, pushed 2026-06-11): both predicates → `useforreplenish = true`; new **`V2.1.16`** (CREATE OR REPLACE view; doubles as standalone patch for DBs converted with the pre-fix V1.2.04 — applied 2026-06-11 to hydra-dev2, wineco `wh01_om1_v2` dev2 copy + `dev_wh01_om1`, all verified flag-based; **wineco uat @10.0.0.6 pending**); bridge toolkit watermark + `05-verify-bridge.sh` viewdef check → V2.1.16. `V1.0.02` baseline left untouched (v1/v2 byte-identity + Flyway checksums); fresh DBs converge via V1.2.04/V2.1.16 ordering. Impact on Hydra data (dev2): name list over-counted replenishable 18,607 vs flag-true 9,221 (9,386 bottles on pick-only `Storage and Picking`); Hydra's comma-less `Storage Picking and Replenish (from)` never matched the list. `mvn test-compile` clean. Lesson: already-done verdicts for dual-path fixes (query + DB view) must verify each read path incl. `V1.2.04`/`V1.2.99`. |

## Notes

- Hot-fix ports landed between sweeps do NOT update this log. Their coverage rolls into the next regular sweep, where `git cherry` detects patch-equivalents.
- If the anchor is moved backward (to trigger a backfill of older v1 commits), add a row explaining why.
- The `wms-api` column advances only after an API-focused session. **First advance landed 2026-06-09** (`7f06c6f` → `a3b4a9b`); before that it stayed pinned at the `2026-03-20` baseline (`7f06c6f`) through many out-of-order hot-fix ports.
