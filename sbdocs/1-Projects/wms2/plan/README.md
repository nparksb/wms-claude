---
title: "WMS v2 — Active Plans"
type: index
status: active
version: v2
scope: wms2-planning
updated: 2026-08-11
tags: [moc, index, wms2]
---

# WMS v2 — Active Plans

Active fix plans, feature plans, and debug plans for the **modern WMS v2 stack** (`v2/wms2-api` — Java 21 / Spring Boot 3.5.9 / PostgreSQL). Completed plans live in [../../../4-Archieves/wms2/plan/](../../../4-Archieves/wms2/plan/).

See also: [vault index](../../../INDEX.md) · [plan template](../../../9-System/templates/wms-plan-template.md) · [v1 → v2 sync workflow](../../../2-Areas/wms-v1-v2-sync/README.md)

---

## Current plans (filesystem snapshot)

- [260405-PgBouncer_Connection_Pool_Strategy_2026-04-05.md](260405-PgBouncer_Connection_Pool_Strategy_2026-04-05.md)
- [260523-UTC-TIMEZONE-MIGRATION.md](260523-UTC-TIMEZONE-MIGRATION.md)
- [260527-hydra-v1-to-v2-migration-runbook.md](260527-hydra-v1-to-v2-migration-runbook.md) — *Hydra (wh01, NY) UTC migration **run record**; procedure lives in the [SOP](../../../2-Areas/wms-utc-timezone-migration/README.md). A→C→F rehearsed on dev 2026-06-05; G–K pending*
- [260606-wineco-v1-to-v2-migration-runbook.md](260606-wineco-v1-to-v2-migration-runbook.md) — *WineCo (wsl) v1→v2 UTC migration **run record**; sibling of the Hydra runbook above; procedure lives in the [SOP](../../../2-Areas/wms-utc-timezone-migration/README.md)*
- [260628-shipitez-v1-to-v2-migration-runbook.md](260628-shipitez-v1-to-v2-migration-runbook.md) — *ShipItEZ v1→v2 UTC migration **run record**; two warehouses (wh02/nywh NY + wh01/c1wh LA), both pre-bridge v1; readiness GREEN 2026-06-28, nothing executed yet. LA has a seqentities multi-island item; procedure lives in the [SOP](../../../2-Areas/wms-utc-timezone-migration/README.md)*
- [260422-v2-testing-migration-rollup.md](260422-v2-testing-migration-rollup.md) — *reviewed 2026-06-22; coordination layer for the 3 testing plans below (start here) — see §9 decisions (D1 PG-lane owner hard-blocks P1 §4)*
- [260420-v2-integration-tests-h2-migration-report.md](260420-v2-integration-tests-h2-migration-report.md) — *reviewed 2026-06-22; approved execution plan*
- [260420-v2-port-plpgsql-functions-to-java.md](260420-v2-port-plpgsql-functions-to-java.md) — *reviewed 2026-06-22; Phase A baseline gate cleared 13/13 (#47 merged)*
- [260421-v2-replace-pg-advisory-lock.md](260421-v2-replace-pg-advisory-lock.md) — *reviewed 2026-06-22; sibling of PL/pgSQL plan — lock confirmed load-bearing (rollup D2)*
- [260520-rest-security-permitall-hardening.md](260520-rest-security-permitall-hardening.md)
- [SBDEV-2238-4.6-oms-sync-reconciliation-job.md](SBDEV-2238-4.6-oms-sync-reconciliation-job.md) — *draft; MEDIUM — daily WMS↔OMS drift-detection job (last SBDEV-2238 sibling). Code prereqs (SBDEV-2221 + 4.1) now merged; still externally blocked on the OMS GET endpoint (stub below) + state-mapping sign-off + SRE alert rule.*
- [SBDEV-2732-configurable-default-putaway-location-hierarchy.md](SBDEV-2732-configurable-default-putaway-location-hierarchy.md) — ***BOTH PHASES MERGED 2026-08-11 — READY TO ARCHIVE.*** *Rewritten 2026-08-12; the previous entry here still said "PHASE 2 NOT STARTED" and was ~4,000 words of superseded detail. Feature complete: 4 tiers (SKU → merchant → warehouse sysprop → `PutAwayLane`). Phase 1-API merge `889298d` + `V2.2.13`; Phase 2 = steps 18a/A/B/C/D in `wms2-api` (PRs #141-#148) and steps 19/19a/20/21/22 in `wms2-web-ui` (PRs #42-#49), all merged base-first with no orphans. **Its verify script reads `PHASE=all` 285 pass / 0 fail** — green for the first time. ClickUp `on dev`. **Outstanding: (a) an optional product read of the step-19a diversion wording (a one-line `messages.properties` edit), (b) SBDEV-2930 — the mobile per-page reset gap, a separate ticket.** Shipped for reuse and now consumed by SBDEV-2643: `LocationPicker.vue`, `defaultPutawayLocationField.vue`, `GET /putawayConfig/eligibleLocations`, the 7-value `BlockingReason`, and `util/keycloakRoles.js` — the working `sb_admin` gate (⚠ `sb_admin` rides the Keycloak **`groups`** claim, so a client-side `hasResourceRole` is false for every real admin). ⚠ **`wh01_hydra_v2` never got `V2.2.13`** — it has no `flyway_schema_history` so the boot migrator skips it; MOOT on DEV because that tenant is INACTIVE (the only active DEV tenant is WineCo's `dev_wh01_om1`), but it would matter if reactivated.*
- [SBDEV-2643-sku-default-putaway-location-ui.md](SBDEV-2643-sku-default-putaway-location-ui.md) — *⚠ **WAS MISSING FROM THIS README ENTIRELY** until 2026-08-12, despite being `urgent` and 2,600 lines. **draft r7 — UNBLOCKED, awaiting approval.** URGENT — the SKU tier (tier 1) of the putaway hierarchy: the configuration UI on the SKU screen plus the per-SKU effective-destination read. **SBDEV-2732 is fully merged, so 2643 now has ZERO external blockers**; all four remaining phases (`B1-pre` → A1 → A2 → B1 → B2) are implementable today. Verify baseline **23 pass / 63 fail / 0 SKIP** (api `bcfdc47` / web `e702a42`) — zero skips means every FAIL is 2643's own work. **r7 caught four plan defects before implementation:** the permission gate was wrong for the *second* time (r6's `hasResourceRole('sb_admin', clientId)` is false for 100% of real admins — the role rides the `groups` claim); the scope banner contradicted its own D1 row for three revisions *and* named the already-merged SBDEV-2821; **B2 was specified to clone a component 2732 built for reuse** (2.5 d → ~1.0 d once it extends `defaultPutawayLocationField.vue`); and five verify rows could not pass a correct implementation — `A3-tx` asserted the **exact opposite** of 2732's own `P2A-ctl-no-tx` row. Also fixed a latent `multiline_contains` bug (a `/` in the pattern was a perl syntax error) that likely affects other plans' scripts. **24/24 mutations caught.** ✅ **ALL OPEN QUESTIONS ANSWERED 2026-08-12:** **Q10** → (A) DEV-only on `wms2-wineco-dev`, which carries the ticket's *actual* subject (`ICE PACK` itemdata **874400** System-client; `ICEPACK` flowbin location **225817**; active FLA **22742154**) — no stand-in needed, and the one the plan had designated was a **hydra id on a wineco tenant**; **Q11** → yes, `client 0/System/System-Client` on every reachable tenant; **Decision 3 Q3** → mirror 2732's already-approved `putawayDestinationDivertedToLane` copy; **Q4** → option (ii), which **added Phase A4** — the `name` search parameter on `GET /putawayConfig/eligibleLocations` that 2732 assigned to 2643 and nobody built (**2,564 candidates on wineco-dev = 13 sequential round-trips per dialog open**). Phases: `B1-pre` → A1 → A2 → **A4** → B1 → B2, ~2.5 d. Baseline moved to **23 pass / 70 fail / 0 skip** (the 7 new fails are A4; pass count unmoved, the correct signal for added scope). ⚠ **Three further script traps found by RUNNING the new rows:** an undefined fn in an `if` **guard** deleted all 7 rows silently (total looked unchanged, not broken), a negative passed vacuously on an untouched tree, and a PCRE `(?i)` handed to the ERE helper matched literally.***
- [SBDEV-2736-outbox-dispatcher-status-blind-rejection.md](SBDEV-2736-outbox-dispatcher-status-blind-rejection.md) — *draft **r2** (post-ralplan review); HIGH — outbox dispatcher marks rows SENT on HTTP 2xx alone, but legacy OMS returns 200 with a non-Success verdict in **two different envelope shapes** → rejections recorded as deliveries. DB-proven: 613 rows on wineco-dev; ~48-68% of real BOL-shipped notifications rejected May–Jul 2026. Phase 1 = observability only (no semantics change), **r3 scope widened to all three WMS→OMS egress paths** after production showed 95.5% of rejections on the direct-POST path the original scope excluded. Spawned SBDEV-2737 (WMS sends `positions:[]`), SBDEV-2738 (OMS envelope normalization), **SBDEV-2748** (inventory export failing 19 months on one unmapped SKU — 575 occurrences, found by looking outside the original scope).*
- [SBDEV-2729-system-sku-receiving-null-label-token.md](SBDEV-2729-system-sku-receiving-null-label-token.md) — ***APPROVED r8** (architect + critic, 5 review rounds); **URGENT** — system SKU (ICE PACK, HMG) cannot be received: `String.replace(CharSequence, CharSequence)` is null-hostile and 4 of the 12 ZPL token sources in `SharedService.createCaseLabel` are nullable columns. Root cause reproduced on `openjdk 21.0.11`. **Verified implementable by an independent implementation**: whole verify script `62 pass, 0 fail`, full suite `4538 tests, 2 failures` (both pre-existing on `develop`). Split **PR1 (urgent)** / **PR2 (hardening)**, PR1 with a named acceptance subset. **Two caveats the approval does not cover:** `db_verified: partial` and confidence ~50-60% on `itemdata.winetype` specifically, so **§7.2 step 0 (get the verbatim error string from the ticket's screen recording / HMG log) is BLOCKING** — if it does not name `replacement`, §2 reopens. But reviews also found a **second, independent live defect that does not depend on the hypothesis**: `SharedService:66` `findById(unitload.getBoxtypeId())` with `boxtype_id` NULL on **80.1%** of unit loads (10,718/13,381), **281** of them on the reprint path — HTTP 500 today. That alone justifies PR1.*
- [SBDEV-2777-stock-history-client-id-blind-mis-aggregation.md](SBDEV-2777-stock-history-client-id-blind-mis-aggregation.md) — *draft; HIGH — `public.stock_history()` groups and joins on `item_nr` only, so every client stocking a shared SKU gets the **all-clients** stockrecord aggregate. DB-proven on `dev_wh01_om1`: SKU `PNRO23` returns 8,322 received for both its clients where the truth is 1,074 / 7,248 (7.7× overstatement). **Not a v1→v2 porting miss** — the missing clauses are in no migration script and in neither repo's git history; they were hand-applied to WineCo's v1 DB, so the repo baseline has always been blind and **all 5 v2 tenants inherit it** (affected SKUs: wsl 135, dev 43, c1wh 13, hydra 7, nywh-shipitez 0). Propagates into `transaction_detail` BEGINNING/ENDING and `transaction_summary` beginning/ending inventory via 4 internal calls. Fix is one `V2.2.x` `CREATE OR REPLACE`, unconditional, **no Java change** (signature unmoved); corrected aggregation pre-validated inline. **Caveat: authored without the ralplan Critic pass** — see the plan header. Other v1 tenants likely still blind → separate ticket.*
- [SBDEV-2238-4.6-oms-order-state-get-endpoint.md](SBDEV-2238-4.6-oms-order-state-get-endpoint.md) — *draft STUB; MEDIUM — paired OMS prerequisite for 4.6: OMS must expose read-only `GET /api/order/{externalId}` (200+state / 404-unknown) for the reconciliation job to compare against. Re-verified still absent in oms-laravel-api 2026-07-27; no reusable substitute, and `externalId` is ambiguous WMS-side.*

- [SBDEV-2797-outbound-bol-bulk-export-first-only.md](SBDEV-2797-outbound-bol-bulk-export-first-only.md) — ***APPROVED** (architect + critic, ralplan consensus); NORMAL — **ticket premise corrected: the reported repro is not reachable on `develop`.** `exportBolPop.vue:72` really does send only `selectedItems[0]`, but the Open tab's bulk Export button has been commented out since the initial commit and the Closed tab has no `show-select`, so the truncation is **latent**. Approved scope is therefore to **build** bulk BOL export (wms2-api + wms2-web-ui, 6 fixes A–F). Closed-tab-only multi-select: DB-proven **0 of 316 `OPEN` BOLs can produce a single export row** (both export queries hinge on `billoflading_position`, created only at truck loading), vs 2,298/2,516 CLOSED. Empty sheets are **kept** — `a8af84f` deliberately made position-less BOLs export empty. One sheet per BOL + a leading Summary index; `"Inbound BOL"` mislabel retired on both paths. No `@Transactional` (client-socket pinning; option (c) is Spring self-invocation). No CORS/config prerequisite. **API PR must deploy before the UI PR.** Verify script written and pre-fix baselined at **8 pass / 63 fail / 3 skip**, with 4 invariant rows counter-tested; 5 vacuous assertions caught across review.*

- [SBDEV-2802-cycle-count-export-client-column-headers-swapped.md](SBDEV-2802-cycle-count-export-client-column-headers-swapped.md) — ***implemented / MERGED** 2026-08-03 — **PR [#122](https://github.com/SiteBossInc/wms2-api/pull/122) merged to `develop`** (`76054fc`), ClickUp `on dev`. **v2 only — v1 half still open under this ticket, so do NOT archive.** Merged WITHOUT the §5 ops confirmation, on explicit direction; **both independent lanes passed on the second attempt** (the first pair aborted on usage-credit exhaustion, not on findings): conformance **PASS** (0 blockers, every §2 row + §4 criterion verified against fresh output) and code review **0 critical / 0 high / 1 medium / 1 low**, both since fixed. Verify **19 pass / 0 fail / 0 skip** including a real Surefire run and `mvn clean compile` — not skips. NORMAL — **deliberately thin plan**: diagnosis was completed in full while implementing SBDEV-2632 and lives on the ticket, so this doc records the finding rather than re-deriving it. Every cycle-count export labelled the two client columns backwards — `"Client Name"` over `getClNr()` and `"Client ID"` over `getName()`, across all 6 header arrays in `CyclecountService`, zero correct usages in the repo. Pre-existing, **not** a 2632 regression — 2632 reproduced it deliberately behind a `NOTE — deliberate bug-parity` comment. **Decision: swap the labels, never the values** — leaves every physical column's bytes in place (a macro reading column A always got the client number and still does) whereas swapping values would silently change each column's meaning under an unchanged header row; review proved the production change reduces to label-lines-only under a filter-and-diff. `db_verified: false` by design (pure literal ordering). **Ops confirmation remains a MERGE GATE** — header text is operator-visible and no repo evidence can settle whether an external consumer keys on it. Regression guard looks columns up **by header name**, not fixed index (a fixed index passes for the wrong reason on the merged sheet, whose leading `Cycle Count` column shifts everything by one); review mutation-proved all 4 assertion sites, incl. a second targeted mutation because the first left the *detailed* assertions unreached. Verify script itself was negative-tested 3 ways and **had a dead check** the Critic caught — A6 used `mvn -q | grep "BUILD SUCCESS"`, which can never match since `-q` suppresses INFO, and it had skipped silently in all 3 rounds because `mvn` was off PATH. Also corrected 3 ticket inaccuracies: **6** arrays not 5, `exportCycleCount2` is **dead code** (so 4 of 6 are operator-reachable), and SBDEV-2632's still-live verify script was an unlisted consumer pinning the old order. **v1 half not started and still owned by THIS ticket** — not SBDEV-2631, whose charter is the v1 bulk-export mirror; routing v1 there would leave it owned by nobody.*

- [SBDEV-2781-expected-return-date-stray-time-and-tz-shift.md](SBDEV-2781-expected-return-date-stray-time-and-tz-shift.md) — ***blocked-scope-decision**; NORMAL — **⚠ the ticket's ENTIRE user-visible complaint is already fixed on `origin/develop`.** Two unlinked commits from one 2026-07-31 session close every AC: `dfe24f8` (API, PR #116) for the date and **`e6ca85a` (UI) for the stray time, across all three notices components**. The plan below was drafted against a **stale local checkout — `v2/wms2-web-ui` was 11 commits behind `origin/develop`** — and its primary deliverable (Fix A) needs no work; caught only at the TDD-gate step, when the fresh worktree already contained the fixed template. **Lesson: `git fetch` + diff `develop..origin/develop` in every affected repo BEFORE enumerating §0** — reading the working tree is not reading the merge target (the API repo was 2 behind and its analysis survived; the UI repo was 11 behind and its headline finding did not). Honest baseline against `origin/develop`: **17 pass / 7 fail / 2 skip** (the earlier `15/11` graded the stale tree and is void). What genuinely remains is hardening, not the reported bug: **Fix C** (3 HAL-exported `@RestResource` projections on `AdviceRepository` still emit midnight-UTC — a service-layer fix cannot guard a Spring Data REST query, same trap as SBDEV-1666; 0 consumers verified across all 5 UIs + `oms-laravel-api`), **Fix D** (`outboundBolDetails.vue:57` stray time on `billoflading.shipped` — real but on the *outbound BOL* screen, not returns), **Fix B** (dead `dayofdelivery` slot `e6ca85a` left behind). Awaiting the requester's call on whether these ship under 2781 or a new hardening ticket. Retained diagnostics worth keeping: reproduced the reporter's screenshot exactly (`"2026-07-31T00:00:00.000Z"` → `07/30/26 8:00:00 pm` ET, `5:00:00 pm` PT — so the ticket and `dfe24f8`'s commit message are one bug on two tenants); `db_verified: true`, all **2,216** RETURN advices carry a `dayofdelivery` (100% blast radius) vs **0/2,216** for `dayofdeliveryuntil`; §0 enumerates every `date`-typed column in the tenant schema (5) rather than only the named symbol, which is what surfaced C/D/B at all. Also **found and fixed two `verify-plan-template.sh` helper bugs** — `mvn_test_passes` false-**FAILs** (greps for `BUILD SUCCESS`/`Tests run` that `mvn -q` never prints; measured exit 0 on a clean suite — the identical trap SBDEV-2802's A6 hit) and both test helpers false-**GREEN** on a missing test (`-DfailIfNoTests=false` and an unmatched `--testPathPattern` each exit 0). Original (now-superseded) analysis:*
  *NORMAL — Expected Return Date on Inbound Notices. **Ticket premise half-obsolete: the reported date shift was already fixed.** PR [#116](https://github.com/SiteBossInc/wms2-api/pull/116) (`dfe24f8`, 2026-07-31 — **one day after the ticket was filed**, and never linked to it) applied `ViewDtoService.toLocalDate()` to `dayofdelivery`/`dayofdeliveryuntil`, so the API now serves a bare `"2026-07-31"`. Reproduced the reporter's screenshot exactly against the UI's own `moment-timezone`: the old wire value `"2026-07-31T00:00:00.000Z"` renders **`07/30/26 8:00:00 pm`** in `America/New_York` (and `5:00:00 pm` in LA — matching `dfe24f8`'s commit message, so the two reports are the same bug on two tenants). **What is still open is the ticket's headline AC**: `openNotices.vue:79` + `closedNotices.vue:61` render a second `$formatTimeOnly` line under the date, which post-#116 now reads a fabricated **`12:00:00 am`** — measured, not inferred. `db_verified: true` — `advice.dayofdelivery` is a SQL `date`, and **all 2,216 RETURN advices carry one (100% blast radius)** while `dayofdeliveryuntil` is 0/2,216, which is why only one field was ever reported. **§0 enumerates every `date`-typed column in the tenant schema (5) rather than only the named symbol**, which surfaced two adjacent sites (both pulled in scope, D4): a **dead** `dayofdelivery` slot in `openNoticeReceiptTable.vue` (its `headers` array has no such column, so Vuetify never invokes it — a live reintroduction trap) and `outboundBolDetails.vue:57` stray time on `billoflading.shipped`. Also closes the **HAL landmine** (D2): three `@RestResource`-exported native projections on `AdviceRepository` still emit the midnight-UTC instant because **a service-layer fix cannot guard a Spring Data REST query** — same trap as SBDEV-1666; **zero consumers verified across all five UIs + `oms-laravel-api`**, so unexport rather than maintain a second serialization path. Detail screens, both ingestion paths and the Excel export were each checked and are **already correct** — guarded by G1–G3 so a revert of #116 is caught. **ralplan deliberately skipped** per the skill's mechanical-fix exception (4 template/annotation edits, no design space) — stated in the plan header. Verify script pre-fix baselined at **15 pass / 11 fail / 0 skip**, the 11 FAILs being exactly the 11 deliverables; `T4` PASSing independently confirms #116's tests are green on `develop`. **Found and fixed two `verify-plan-template.sh` helper bugs while baselining** — `mvn_test_passes` false-**FAILs** (greps for `BUILD SUCCESS`/`Tests run` that `mvn -q` never prints; measured exit 0 on a clean suite — the identical trap SBDEV-2802's A6 hit) and both test helpers false-**GREEN** on a missing test (`-DfailIfNoTests=false` and an unmatched `--testPathPattern` each exit 0). Remaining: `wms-tdd-gate` → implement.*

- [SBDEV-2854-replenish-rejects-non-flowbin-destination.md](SBDEV-2854-replenish-rejects-non-flowbin-destination.md) — *draft, awaiting review; **URGENT** — client V2 testing: Replenishment rejects `Club01` with `"Destination is not a flowbin!"`. **NOT a v2 regression** — `db_verified` on two DBs that v1 rejects identically (same location id 225748, same type `cases and pallets`, 0 FLAs both sides), so nothing was lost in migration and this is a deliberate behavior change lifting a limitation the product never had. **Supersedes the archived `260424-club-location-replenish-fix.md`**, which described the same defect on the same location four months earlier, was never implemented (no commit in either repo ever touched the gate), and whose recommended fix would **not** have worked: `Club01` already holds 3 case unit loads, so widening the type allowlist alone just moves the error to `"Destination has already a unit load!"` at `:920`. The flowbin assumption is **four** stacked preconditions, not one — type gate, empty-location requirement, FLA auto-creation, and `finishReplenishmentOrderInternal:501-520` deriving the destination UL **from the FLA** and creating one at `:510`, which would silently re-bind shared `Club01` to one SKU at commit time and defeat a validation-only fix. Approved shape is an **FLA-free destination path** behind sysprop `REPLENISH_ALLOWED_DESTINATION_LOCATION_TYPES`, **default `flowbin` so no tenant's behavior moves until opt-in**; rollback is a sysvalue edit, no code revert. Also fixes two adjacent bugs the ticket never mentioned: an **inverted** guard at `:407-411` throwing `"Unit load already exists."` when the UL does *not* exist (blocks the intended flowbin happy path today) and the `:390`/`:908` guard asymmetry. Reported UI path re-traced — it posts `/replenish/multi-unitloads`, so the live site is `:917`, not the `checkDestination` `:399` the symptom suggests. Qty AC closed as **expected behavior** (`realAmountNeeded` 1 vs staged `qtySelected` 12) → separate UX ticket. Migration takes **`V2.2.10`** (PR #132): it originally took `V2.2.13` and left `V2.2.10` for SBDEV-2732, but that gap would silently wedge Flyway for any tenant reaching it first (§5.1 H3), so it renumbered **down** to stay contiguous and **SBDEV-2732 moved to `V2.2.13`** on 2026-08-06. Verify script validated **both** directions — `2 pass / 41 fail` on real `develop`, `43 pass / 0 fail` on a synthetic post-fix tree, plus an F6 duplicate-version counter-test; 4 self-inflicted script bugs caught while baselining (inverted `perl` negation polarity, an unbounded `.*?` that leaked past the method into already-correct code, `file_contains_ml` false-PASSing on a **missing** file, and a line-counting `grep -c` that can never reach 2 on a construct that wraps a newline). **ralplan deliberately skipped** on explicit direction — recorded as §10 Q4. **v2 only; v1 sibling flagged not dropped** (§4). Remaining: review → close O1 (UAT `Club01` config — the one DB not reachable) → `wms-tdd-gate`.*

- [SBDEV-2930-mobile-workflow-pages-resume-stale-operator-state.md](SBDEV-2930-mobile-workflow-pages-resume-stale-operator-state.md) — ***MERGED 2026-08-12 — wms2-mobile-ui PR #32 into develop, merge `98eae72`, ClickUp `on dev`. Merged develop re-verified: 115/115 Jest, verify 49 pass / 0 fail. ⚠ DO NOT ARCHIVE until the §8.4 manual rows are run on DEV** — the merge was made on explicit instruction before manual QA, and this defect has **no telemetry surface**, so those ten rows are the only compensating control; M10 is the sole runtime check on the review Medium, which was derived from the call graph and never reproduced live. **Review found a Medium that this fix itself introduced:** `store/picking.js` `timer` is a live `setInterval` handle, not data — nulling it on reset broke an accidental self-heal (`scanSource.vue`'s `if (!this.timer)` guard), so re-entering Picking ran TWO intervals, `count` decremented twice a second, the timeout watcher fired early and **auto-passed a pick position**. The obvious repair would have been worse than the bug: `timer` rides the persisted blob, every page load gets a fresh `Window` whose timer-id counter restarts at 1, and `plugins/keycloak.client.js:320` creates the token-refresh interval during boot and so holds one of the lowest ids — `clearInterval(state.timer)` after a reload could **silently stop token refresh and log an operator out mid-shift**. Fixed with a module-scoped `liveTimer` outside Vuex; this also killed a **pre-existing** instance of the same bug (`scanSource.submit()` called `clearTimeOut` unconditionally). **THREE of my own claims failed testing and were corrected rather than shipped** — the root guard did not catch a root `resetState` (the bare mutation type was never committed; both review lanes caught it independently); fixing that silently broke a DIFFERENT detection, because `namespaced: false` changes a mutation's TYPE NAME and not the state Vuex hands it, so a bare commit placed before the sentinel repaired the very fault the sentinel detects; and a test claimed to pin the `clearInterval` ARGUMENT but only pinned its guard, so swapping the argument alone left the suite green. **Two verify-script defects found by negative-testing my own script:** `A-picking-rb` lost containment (`.*?` under `/s` spans past the mutation, so a hand-listed reset plus any later `Object.assign(state, initialState())` elsewhere in the file was a FALSE GREEN — now tempered-greedy), and `M1-release` passed on DEAD CODE if `setTimer` stopped syncing `liveTimer`. NORMAL — **`v2/wms2-mobile-ui`, not `wms2-api`** (the only front-end-only plan in this folder). Shared handhelds resume the **previous operator's** in-flight job: the whole Vuex root state persists to `localStorage['vuex-mobile']` minus 3 warehouse/tz keys, and workflow pages render straight off the rehydrated `process` marker. Cycle Count with a stale `'14_count'` opens the previous operator's count screen and its submit posts **their** `cyclecountPosition` — a well-formed request no server-side check can reject. **`db_verified: false` is correct, not a gap** — there is no DB surface; the gate is discharged by three **executed** Jest probes instead. **§0 enumeration contradicted the ticket in three places, all widening scope:** `palletizing.vue` and `replenish-request.vue` are missing from the ticket's table entirely (the latter *shares* the `replenish` module with `replenish.vue`, so one module backs two different entry screens), and — the load-bearing correction — **the pages the ticket scored "partial"/"yes" are not partly fixed, they are broken.** Probe 2 on `picking.vue` proved a `mounted()` reset sets `process` to `'0_section'` **and still renders the stale `Pick` screen** (`ScanSection` absent, `currentPosition` intact), because Vue 2 creates and mounts children *before* the parent's `mounted()` — and `pick.vue`'s own `mounted()` **commits two mutations** advancing the previous operator's position. So asserting on `state.process` alone reports every partial page as FIXED; the only honest assertion is `findComponent(EntryScreen).exists()`. `lookup.vue`, which the ticket marks "yes — resets", is in this category. Approved shape: `resetState` on all 11 workflow modules **rebuilt from the `state()` factory** (`Object.assign(state, initialState())`) rather than putaway's hand-listed 5 fields — `picking` has **20** fields and hand-listing is how this ends up half-done again — called from `created()` on all 11 pages, with `putaway` retained as the control. Blanket reset chosen over per-user ownership stamping and over excluding workflow modules from the reducer (§4.4). Root state (`selectedWarehouse`/`warehouses`/`warehouseTimezone`) explicitly untouched — that is the SBDEV-2726 / UTC-migration boundary, guarded by `GUARD-root`. **Verify script validated both directions during authoring:** `5 pass / 42 fail` on real `origin/develop` (all five passes genuinely-satisfied pre-existing state, incl. the `putaway` control) → `14 pass / 33 fail` on a representative post-fix worktree, and the fix design itself was **run** (`ScanSection` true / `Pick` false / working set null — the exact inverse of the baseline). **2 self-inflicted script bugs caught by baselining:** `file_contains_ml` interpolated the pattern into perl's `m/.../` so the `/` in `'putaway/resetState'` terminated the regex (the control row false-FAILed — the same trap SBDEV-2643 hit), and the `BNEG-*` negatives used line-based `grep` on a multi-line `mounted()` block so they **passed vacuously**. Harness constraint recorded for the implementer: 3 `.vue` files use optional chaining **inside their template**, which `vue-jest@3.0.7` cannot compile — blocks mounting `putaway.vue` and `replenish.vue`; stub via `jest.mock`, do **not** upgrade `vue-jest` or rewrite the `?.` guards. **v1 confirmed affected and worse** — `v1/wms-mobile-ui` has the same gap plus a bare `createPersistedState()` with no key and no reducer, i.e. the default `vuex` key SBDEV-2726 identified as the cross-app leak; split to **SBDEV-2932** by explicit decision. PR #31's logout clear does **not** cover this (session boundary only). Remaining: review → `wms-tdd-gate` → implement.*

- [SBDEV-2931-dead-nuxtlogo-spec-fails-yarn-test.md](SBDEV-2931-dead-nuxtlogo-spec-fails-yarn-test.md) — ***MERGED 2026-08-12 — wms2-web-ui PR [#53](https://github.com/SiteBossInc/wms2-web-ui/pull/53) into `develop`, merge `1f2605b`, commit `7b9029a`, ClickUp `on dev`. Merged develop re-verified: verify 8 pass / 0 fail, Jest 27/27 suites, 251 passed, exit 0; 0 `NuxtLogo` paths and 0 content matches remain.*** *Every predicted figure hit exactly: verify `4 pass / 4 fail` → **`8 pass / 0 fail`**, Jest `1 failed, 27 passed, 28 total` / exit 1 → **`27 passed, 27 total` / exit 0**, tests **251 → 251 (zero coverage loss, measured)**, ESLint **3739 problems in both trees — zero delta**. Diff is **1 file, +0/−9**. ⚠ **Archive-gated on manual rows M3/M4/M6** (dev server, `yarn build`, clean-clone) — those are the only checks that confirm the file was inert in the *app build* rather than by argument; M1/M2/M5 were exercised. ⚠ **Conformance is self-assessed, not independent** — the `verifier`/`code-reviewer` lanes were skipped (plan §9.2 pre-classed this Trivial, `Code-review step: none`); the compensating controls are the mutation-tested verify script and a **wiring proof** (the same script against the main checkout still FAILs `A1`–`A3`, so the 8/0 came from the worktree). ⚠ **v1 twin SBDEV-2937 still `Open` — `v1/wms-web-ui` remains red.** **LOW / trivial** — the smallest plan in this folder: delete one 9-line dead Jest spec. **`v2/wms2-web-ui`, not `wms2-api`.** `test/NuxtLogo.spec.js` imports `@/components/NuxtLogo.vue`, which **has 0 commits in the repo's entire history** — stock `create-nuxt-app` scaffold rot from the initial import `3462148` (2024-07-16), not a regression. The suite dies at module resolution **before any `test()` registers**, so it contributes **0 tests**: the headline number everyone reads (`251 passed`) is untouched, and only the two nobody reads — suite tally and **exit code** — go red. That asymmetry is exactly why it rotted for two years while being independently rediagnosed **five times** (§0 cites 9 lines across 7 vault docs, incl. SBDEV-2732's own admission that every "26 suites" figure was taken with `--testPathIgnorePatterns=NuxtLogo`). **Three corrections to the ticket, all measured:** (1) its evidence table is **stale** — it says `26 passed / 27 total` + `250 tests`, live `develop` is **`27 passed / 28 total` + `251 tests`**, so its AC #2 hard-codes 250 and a *literal* reading would fail a correct fix (restated as "unchanged from the pre-fix baseline", §10 D3); (2) **`v1/wms-web-ui` carries the byte-identical spec** from the same commit and is **proportionally worse — 1 of only 3 suites red** — absent from the ticket entirely, split to **SBDEV-2937** by explicit decision, so **v1 knowingly stays red** when this ships (§4); (3) the ticket's CI-gate premise is **forward-looking, not a red pipeline** — `.gitlab-ci.yml` is a single tag-gated docker `build` stage and all four `.github/workflows/*.yml` are image builds, so **nothing runs `yarn test` anywhere**; gate deferred by decision D2 and recorded in §8 rather than dropped. `db_verified: false` **is correct, not a gap** — no DB surface exists; discharged by four **executed** probes, of which **P2 is load-bearing**: `--testPathIgnorePatterns=NuxtLogo` measured `27/27` suites + **`251 passed`** + **exit 0**, proving "green" and "zero coverage lost" *before* any change. Option B (add the component) rejected — it trades dead test code for dead **production** code that enters the `collectCoverageFrom` denominator and *lowers* coverage; option C (`testPathIgnorePatterns` in config) rejected as institutionalising the very habit the ticket exists to break. **No new test, deliberately**: the acceptance property is the runner's own exit code, which no suite can assert about itself (§6) — it lives in verify `B3`/`B4`. **`wms-tdd-gate` skipped on the named "no test surface" row** — no Java repo to write into, and the pre-fix failing artifact *is* the file being deleted. **ralplan skipped** per the mechanical-fix exception; the two real judgement calls went to the user instead (§10 D1/D2). **Verify script validated in BOTH directions and mutation-tested:** **`4 pass / 4 fail`** on real clean `develop` → **`8 pass / 0 fail`** on a real worktree with only the spec removed (`jest exit=0`, `27/27`, `251`), then **all four wrong-fix mutations caught, each by exactly one row and no other** — option B trips `N1`, option C trips `N2`, a filter smuggled into `package.json` trips `N3`, and **renaming `jest.config.js` away trips `N2` rather than vacuously passing**, which is the `file_not_contains` fail-open trap defeated by an explicit `[ -f ]` precondition. **Harness landmine recorded for the implementer:** do **not** grade this script through a symlink shadow root (the recipe used elsewhere in this vault) — Jest resolves symlinked specs to their real paths outside `rootDir`, finds **zero** tests, and `B3`/`B4` fail closed on a *correct* fix (observed `6 pass / 2 fail` with empty summary lines); use a real `git worktree`. Remaining: review → implement (single `git rm`, one commit).*

- [SBDEV-2947-sku-putaway-picker-storage-tier-default.md](SBDEV-2947-sku-putaway-picker-storage-tier-default.md) — ***pr submitted 2026-08-13 → [wms2-web-ui #61](https://github.com/SiteBossInc/wms2-web-ui/pull/61); critic-reviewed, TDD-gated, redesigned twice on evidence.*** *NORMAL; **`v2/wms2-web-ui` only, no API change.** The Default Putaway Location picker offers only goods-in (`DEFAULT`-tier) locations until "Show storage locations" is enabled, and on WineCo **zero** are eligible at SKU scope — empty dropdown for all 10,480 SKUs, including the ice-pack case SBDEV-2643 exists to configure. Fourth defect from the 2026-08-13 manual-test session, behind wms2-api #152/#153 and wms2-web-ui #56. **`db_verified: true`; the SQL reproduces `PutawayDestinationRules.CHAIN` rule-for-rule**, cross-checked against four figures the production source asserts independently (2,738 candidates / 1,532 ineligible / 1,345 of 2,068 FLA-bound / 516 rows at tiers 2-3) — all four match. ⚠⚠ **THE PLAN WAS WRONG TWICE AND BOTH CORRECTIONS CAME FROM EVIDENCE, NOT REVIEW OF PROSE.** (1) r1 claimed the empty tier was **structural**; a `critic` pass (run after `ralplan` was skipped on the small-change exception — the exception did not apply) **falsified it** by measuring three more databases. (2) r2 still keyed the default on scope; **surveying all 4 UAT tenants showed only 1 is affected** — `wh01_om1_v2` wineco 0 of 12 eligible, vs `wh01_hydra_v2` / `wh01_shipitez_v2` / `wh02_shipitez_v2` all **2 of 2 ✅** — so a scope-driven default would have changed behaviour on three tenants with no defect. **The entire difference is ONE `location_constraint` row**: `overstock pallet` permits `{5}` on wineco and `{4,5}` everywhere else, and every tenant's SKUs are type 4 (Case). Raised as its own ticket **[868kr5ur0](https://app.clickup.com/t/868kr5ur0)** (config decision, non-blocking). **r3 shape:** the picker opens the storage tier **from the data** (`hasGoodsInOption` over the server's `tier`), never from scope; a wording-only `dedicatedBinTier` prop picks the fallback advice; the caption's "Enable …" instruction is gated on `eligibleDefaultCount > 0` — **not** on scope, which would have stranded a hydra operator at SKU scope where the switch legitimately stays closed. **Two ticket figures corrected:** "2,068 usable" is really **1,206** (2,068 is the flowbin count; 1,206 is what PR #56's own caption prints, so the ticket contradicted the screen it described); and "HubAndSpoke has no constraint rows so accepts nothing" **inverts the rule** — `LocationConstraintService:61-63` **fails OPEN**, those ten are rejected for `crossdockinglane`. **Both contracts validated in both directions and mutation-tested: `12 pass / 22 fail` on clean `origin/develop` → `23 / 11` at the gate → `34 pass / 0 fail` + Jest `82/82` proven reachable; 14/14 mutations caught across r1–r3; full suite 356/356 vs 338/338 clean, zero regressions.** ⚠ **FOUR self-inflicted defects found by RUNNING the script and its tests, all of which had passed review:** `D-gate` passed vacuously (a `</div>`-tempered gap leaked past a pre-existing `<span v-if="scope !== 'SKU'">`); the `SELF-wired` self-test failed against a correctly-wired script (it parsed the description field with `"[^"]*"`, which breaks on an escaped quote); `file_contains_n_times` wraps **`grep -c`** so a duplicate on the *same line* was invisible (added `occurrences_at_most`); and — the best argument in the vault for tests-before-code — **test T24 caught a defect in the plan's own watcher**: `immediate: true` fires before the first page of a paginated read, and an empty array has no goods-in option either, so without a `rows.length > 0` guard the tier opened on **every** tenant, visibly only under a slow read. A third SKU-only predicate (P2.7(f)) was missing from the rule table; it reduces **15% of SKUs to exactly one eligible row**, which made two manual rows wrong. Five Java line references were off, one by 71 lines (read from a stale revision — the plan's own headline hazard). ⚠ **Manual QA must include a non-wineco tenant**: the fix is a deliberate no-op on 3 of 4, and nothing else proves it inert there. **IMPLEMENTED + PUSHED 2026-08-13** (`742dc84`, `879693a`): final contracts re-measured at PR time — verify **47 pass / 0 fail**, targeted Jest **96/96**, full suite **370/370** vs 338/338 clean, 29/29 mutations caught. ⚠ **No review lane ran against `879693a` itself** — it carries the 5th pass's own fixes, and three consecutive rounds each introduced a defect while fixing the previous one; PR review is the remaining gate on that hop. Remaining: PR #61 review → manual QA (**must include a non-wineco tenant**) → archive.*

- [SBDEV-2952-transfer-club-source-by-usefortransfer-flag.md](SBDEV-2952-transfer-club-source-by-usefortransfer-flag.md) — ***IMPLEMENTED 2026-08-14 → [wms2-api #158](https://github.com/SiteBossInc/wms2-api/pull/158) (`25f4ef3`), PR open awaiting review.*** *NORMAL; `v2/wms2-api` only, single PR. **Preventive, no live symptom** — found while investigating SBDEV-2951. Transfer Picking and Club Run decide which locations may be sourced by matching the area **name** against six hardcoded strings (`UnitloadRepository.java:106-114`, callers `TransferOrderService.java:328` + `CustomerorderBatchService.java:1079`), when the schema already carries the predicate as `location_area.usefortransfer`. A client who renames an area or adds a second picking area loses stock from both screens silently. Swap to `usefortransfer = true OR area.name = 'users'` on **one** shared 2-arg method. **`db_verified: true` — set-identity proven on all SIX reachable tenant DBs** (`dev_wh01_om1` 18,910 / `wh01_om1_v2` 12,518 / `wh01_hydra_v2` 362 + 347 / `wh01_shipitez_v2` 1,600 / `wh02_shipitez_v2` 60), **empty symmetric difference every time**; `usefortransfer` is `NOT NULL` and no location has a NULL area. ⚠⚠ **THE REAL RISK IS NOT THE ONE THE TICKET NAMES, AND IT TOOK TWO PASSES TO STATE CORRECTLY.** §6.1.1: this change makes `usefortransfer` **load-bearing for two operator screens where it is currently inert — in BOTH directions**, writable via `PATCH /locationArea/{id}` (`LocationAreaRepository.java:11-12`, exported CRUD; the Master Data UI shows the flag **read-only**). *Case 2* — flagging `Outbound` surfaces **2,856,635 units of `Shipped` stock** into a picker's list. *Case 3, which r2 MISSED and is the worse one* — **clearing** the flag on a stocked storage area silently removes its stock: **246,883 units (`Storage and Replenish`) / 88,198 (`Inbound`) / 62,797 (`Storage and Picking`)** on wineco dev, **397,878 total**, with no error and no warning. That is *this ticket's own failure mode reproduced under a new trigger* — the refactor doesn't create the class of failure, it **moves the trigger from "rename an area" to "clear a flag"**. Judged a favourable trade (a discoverable flag beats an undocumented string list; the fix case is the common event while both hazards need a deliberate admin act), but it is the plan's most consequential decision. Deliberately left unguarded — a name-based guard would re-create the anti-pattern being removed; raised as Q6 (recommendation: at minimum an audit-log entry on flag change). Both directions get manual rows M10/M11. ⚠ **ZERO automated coverage for the SQL**: `UnitloadRepositoryTest` is `@Disabled` (SBDEV-2099), every other reference is a Mockito stub, and **`ddl-auto=none` + native `@Query` unparsed at boot means a typo reaches an operator, not a build** — so 4 of the ticket's 5 ACs are manual-only (M1/M4/M5/M7/M10) and only AC5 is testable. ⚠ **r1 → r3 falsified FIVE of the plan's own claims** (the fifth being case 3 above, missed by r2): SBDEV-2951 is **not** blocking (measured — no overlapping hunks, PR #157 touches `TransferOrderService` @47-288 / `CustomerorderBatchService` @93-1278, disjoint from 328-331 / 1079-1084); no startup safety net; **v1 carries the identical defect** (`v1/.../UnitloadRepository.java:108-116`, Club-Run-only caller, §12 → paired ticket Q5); and §6.1.1 above. **Verify script proven THREE ways: 0 pass/16 fail pre-change, 16/0 on a post-change fixture, and 11/5 adversarial** — the negative test caught a self-inflicted false green where `method_block_contains` passed its env vars *after* `perl -e`, so perl read them as filenames, `$ENV{}` came back undef and the block regex collapsed to "match anything", silently vacuous-greening R3–R7 on **unchanged code**. Anti-pattern sweep says **1 of 1** — every other live `AREA_*_NAME` use is a single-area identity lookup. Next: review → TDD gate → implement.*

- [SBDEV-2961-order-release-silent-section-exclusion.md](SBDEV-2961-order-release-silent-section-exclusion.md) — ***r3 draft — TWO review rounds complete; architect "sound enough to gate", critic APPROVE WITH CHANGES, all findings applied. Awaiting TDD gate.*** *MEDIUM; `v2/wms2-api` + a 2-file `v2/wms2-web-ui` operator remedy. **A Pick & Pack order whose client has `section_id = NULL` is dropped by the release query and stalls at `RAW` forever with no error, no state and no log.** Blocked Ibrar's 2026-08-12 Hydra UAT test; the triage first chased a dead cron lane, which **is** the bug. Root cause is one predicate, `CustomerorderPositionRepository.java:88`. **`db_verified: true`** — differential re-run on `wh01_hydra_v2` (0 rows with the predicate, both without), cron independently proven alive, plus a 6-database client-axis sweep (**0 divergences / 595,407 PICK_PACK orders**). ⚠⚠ **r1 WOULD HAVE SHIPPED A SILENT NO-OP — the exact failure class the ticket exists to kill.** It wrote via `customerorderRepository.save()` from `processOrderGroup`, which runs inside `streamOrderPositionsForEach`'s `@Transactional(readOnly = true)` boundary (`:777-783`); a `REQUIRED` write **joins** that tx and is discarded unflushed, and because `OrderReleaseJob:167-171` handles the **last** buffered group *outside* the tx, exactly one order per tick would have stamped — perfectly shaped to fool a 2-row manual test. **r1's mock tests and grep script both certified it working.** r2 replaced it with a single-statement **CAS in a `REQUIRES_NEW` service method**. Backed by production evidence (§3.2.2a): state 50 has exactly **two writers, both inside `releaseOrder`** — itself `REQUIRES_NEW` on this identical path — so the **16 state-50 rows** on `wh01_hydra_v2` + `wh01_om1_v2` (latest 2026-08-04) prove the mechanic commits. Bonus: bulk JPQL is immediate SQL, so dropping `REQUIRES_NEW` now fails **loudly** with PG `25006` instead of silently. ⚠ **THE STATE WAS DESIGNED AND NEVER IMPLEMENTED.** `git log -S CLIENT_HAS_NO_SECTION` = two commits: `initial checkin` (no writer) and **SBDEV-1656 `d6f28cbf`, which ADDED two healing reads for a structurally unreachable state** — dead since 2025-10-28. `wms2-state-machine-catalog.md:54`/`:107` and `constantValues.js:112` already model it, so the fix is *become the writer*. **r3 corrections (round 2):** CAS window narrowed `state < 200` → allow-list `IN (:raw, :futurePickingDate)` — the wide form **relabels hold states 50/55-58 as 45 and destroys the stock diagnosis** if a section is removed; CAS must set **`co.modified`** because bulk JPQL bypasses `AuditingEntityListener`, leaving no record of *when* an order was stamped; and a **short-circuit on `orderStatus == 45`** — **both reviewers independently** found the CAS firing per section-less order **per tick** (~110/min against a pool of 5 already holding the cursor), not per transition. Also: the `@Version` racer is entity `save()` via `setPickingDate:227→274` (not another bulk update — *do not* add a version bump, it turns a lost stamp into a 500); a **STRICT_STUBS** trap in `OrderReleaseJobTest` (`BaseUnitTest.java:18`) needs `lenient().when(...)`, never class-level LENIENT; `TransactionManagerArchTest.java:48-67` is a machine gate on the TM rule; and `updatableState`/`disableUpdate` are **dead code**, which is the real reason not to touch them. ⚠ **Fix E was demoted from blocker to hygiene — the architect retracted its own r1 finding and it was verified independently:** `order_imported` maps to DTO keys `orderCreated`/`orderImported` that appear **nowhere** in either UI, so Fix B breaks no visible tile and Fix E restores none. The Order Monitor has **always** been individually blind to these orders — part of why the bug survived. Same error class as r1's A2 rationale (a change justified by a consequence that does not exist); A2 likewise demoted after the jars showed a missing native-query alias returns **null**, not an exception. ⚠ **Reviewers CONFLICTED twice; both resolved with evidence.** Transaction boundary: critic right, architect wrong (`REQUIRED` joins an active tx). Client axis: architect right — the guard must read `co.client_id` because that is what `releaseOrder:601` resolves. **Verify script r3: `5 pass / 36 fail`, exit 1** (Maven rows separately proven under r2: `M_job_tests` PASS — r1's `-q` helper could *never* go green on a green build). **THREE false-greens in r2's own script, each fixed and then ABLATION-PROVEN against a deliberately-wrong shadow:** a `// REQUIRES_NEW is mandatory` **comment** greened the row labelled "r1's defect" (now comment-filtered → FAILs, and PASSes when correctly annotated); the brace-anchored guard slice let `B3_guard_returns` match a `return;` **20 lines away**, so a guard that stamps and **falls through** passed — the bounded `followed_by` window now FAILs that while `B3_calls_cas`/`B3_warn` still PASS; and a fully `@Disabled` class matched the Maven regex (`, Skipped: 0` added). ⚠ **Caught by RUNNING it, not reading it:** an r3 row I had just written, `B2_propagation`, passed on the unfixed build because `releaseOrder` already carries `Propagation.REQUIRES_NEW` at `:119` → now requires exactly 2. **Third consecutive revision where the negative test, not prose review, found the defect.** Note `B1_cas_not_wide` is a directional guard that **cannot fail pre-fix** — not evidence of work done. ⚠ **Fix A and Fix E have NO automated proof** — v2 Testcontainers cannot boot (SBDEV-2217), ITs ship `@Disabled`, so manual rows M1/M4/M5 are the entire evidence base and "0 fail" must not be read as "the queries are right". Manual test #1 mandates **≥ 3 stalled orders** — the one step that would have caught r1's partial stamp. Scope: **v1 carries the identical defect** and is **explicitly out of scope by user decision 2026-08-14** (§4 records the live v1 fleet stays exposed, untracked). Backfill of the 118 section-less clients is **[SBDEV-2963](https://app.clickup.com/t/868krfv18)**. Triage: [260814-hydra-uat-three-flow-qa-triage.md](../../../3-Resources/reports/260814-hydra-uat-three-flow-qa-triage.md) §1. Next: **TDD gate → implement**.*

- [SBDEV-2962-pallet-label-rejection-no-expected-format.md](SBDEV-2962-pallet-label-rejection-no-expected-format.md) — ***implemented (r3), [PR #160](https://github.com/SiteBossInc/wms2-api/pull/160) open** — LOW–MEDIUM; `v2/wms2-api` only, single PR. **Scanning a pallet label the tenant's patterns reject said only `String is not valid: 'TEST1'` — never what WOULD be accepted.** Blocked Ibrar's 2026-08-12 club-line palletize on Hydra UAT; diagnosing it needed a DB query. Now renders `String is not valid: 'TEST1'. Expected format: AOUT-000001 (accepted: WC_\d{16}|OUT-\d{6}|OUT\d{6}|AOUT-\d{6})`. **`db_verified: true`** — sysprops read live on 3 tenants. ⚠⚠ **ONE KEY, 6 SITES, 3 SERVICES, THREE DIFFERENT VALID FORMATS** — `MobileMoveStockService` validates a **destination** against `STRING_PATTERN_SEPARATE_STOCK` (`SU-\d{6}`), not a pallet label, so a hard-coded "Expected format: AOUT-######" would be actively WRONG there. That is why the format travels as a per-call-site argument. **Three defects a critic lane caught in the plan before any code was written, each of which would have shipped:** (1) the plan told the implementer to assert on `BusinessException.getParameter()` — **that method does not exist**, so the TDD gate would have hit a compile wall; (2) the accept condition is **two OR'd regexes** (`pattern || convertedPrintingPattern`) and the plan surfaced only one, producing a **self-contradictory message** — example `AOUT-000001` beside a regex it does not match, confirmed on hydra AND wineco; (3) `%1s` is **not** positional, it is `%s` with minimum width 1 — proven in production, an empty label rendered `String is not valid: ' '` (a padded space). Fix uses explicit `%1$s`/`%2$s`. ⚠ **Amendment r3, found while implementing:** §3.3 said read both sysprops at the two null/empty guards and pass all three args. Doing that literally would have **introduced a new HTTP 500** — `convertFormatToRegex` throws unchecked on a malformed sysprop and `PalletizingController` catches only `BusinessException`/`FacadeException`. Those two sites now pass the example only. The verify rows had already encoded that shape (`C_both_patterns` expects 3, not 5), so the prose was wrong and the acceptance criteria were right. A conformance lane later recommended the unsafe version; rejected on the same grounds. **A missed call site degrades rather than 500s** (`MissingFormatArgumentException` → `concatenateKeyAndParameter` → `noValidString, 'TEST1'`), and the direction is asymmetric — Fix C without Fix A is harmless, Fix A without Fix C is not — so all of it lands in ONE commit. **Verify: 41 pass, 0 fail, 1 skip** (baseline `20 pass, 19 fail`). 60 tests across 5 classes; full suite 5038 run, 2 pre-existing failures. **Four verify-script defects found by RUNNING it, two of them false GREENS:** a two-class Maven row filled in by a green sibling; `mvn` absent from PATH so every Maven row scored 127 as an honest-looking FAIL; a file-level `catch (RuntimeException)` row satisfiable by a guard in another method; and a symbol census passing on a call that never reached the exception's argument list. Plus a fifth at the gate: a **negative** row broken by my own Javadoc mentioning the forbidden symbol. ⚠ **Nothing detected a swapped argument pair** until a review lane proved it — every assertion was an unordered `contains()`, and `String is not valid: '<format>'. Expected format: TEST1` satisfies them all. Fixed with ordered assertions, **ablation-proven**. **Four test classes the plan specced as "new" already existed** (`StringConverterUnitTest`, `BusinessExceptionUnitTest`, `MobileTruckLoadingServiceTest`, `MobileMoveStockServiceTest`) — grep the test tree before writing §6. **Deliberately NOT fixed:** the 325-key parent-bundle gap (§8.2) and `convertFormatToRegex`'s unchecked throws (§8.3) — separate tickets. Scope **message only**; label generation deferred (generator exists at `BillofladingService:774-779`) — **follow-up ticket not yet filed**. **v1 carries the identical defect and is explicitly out of scope.** Triage: [260814-hydra-uat-three-flow-qa-triage.md](../../../3-Resources/reports/260814-hydra-uat-three-flow-qa-triage.md) §2.*

- [SBDEV-2968-mobile-ui-function-gating-enforcement.md](SBDEV-2968-mobile-ui-function-gating-enforcement.md) — ***draft — pending review. Ralplan consensus run complete (Critic ITERATE → all 3 must-fixes applied); TDD gate NOT yet run (§10.12 open).*** *NORMAL; `v2/wms2-api` + `v2/wms2-mobile-ui`. **The 12 mobile workflow tiles are gated client-side only — `store/home.js:104-118` filters the menu and nothing re-enforces, so a deep link, browser history entry, or `curl` reaches the handler.** All **66** endpoints across 11 controllers carry zero `@PreAuthorize` and zero function checks; the only gate is the blanket `wms_user` floor at `SecurityConfiguration:143`. **`db_verified: true`** — WineCo dev (96 users) + Hydra UAT: `outbound-forklift` holds 2 view functions yet reaches `/v3/picking`, `/v3/cycleCountLos`, `/v3/putaway`; `MOBILE_UI_VIEW_CANCELLATION` is granted to 3 roles on WineCo, 1 on Hydra and **0 in the code seed**; `WEB_UI_VIEW_TRANSFER_ORDER` is `super-admin`-only so the Transfer tile is invisible to all 6 personas. **Fix = `@RequiresFunction` (FunctionEnum constant *references*, so a rename is a compile error) + one `MobileFunctionGuardInterceptor` + an ArchUnit golden-map test.** Chosen over `@PreAuthorize` because `standaloneSetup` installs no method-security advisor — the exact configuration in which SBDEV-2863 shipped a broken expression for 9 months — and over a service-layer guard because 7 controllers call repositories directly and `GET /v3/truckLoading/orderList` never touches a service at all. **Verified on the classpath (spring-test 6.2.15): `StandaloneMockMvcBuilder.addInterceptors` exists, so the interceptor IS testable in the lane `@PreAuthorize` is not** — that asymmetry is the whole basis of the design. ⚠⚠ **An early "zero shared-endpoint risk" finding was WRONG and nearly scoped the plan incorrectly** — the first sweep only looked for mobile controller paths *in* the web UI, missing that the mobile UI also calls **shared** controllers. Five endpoints (`/v3/stockUnit/transferStock`, `…/storageLocationsForStockMovement`, 2 × `/v3/dashboard/*`, `/v3/replenishOrder/detailView`) are excluded by construction; the honest residual (R12) is that a user denied the Move Stock tile can still `POST /v3/stockUnit/transferStock`. ⚠ **The Critic caught a CRITICAL fail-open**: the package-keyed fail-closed rule covered 10 of 11 controllers because `OrderCancellationController` lives in `controller/` not `controller/mobile/` — fixed by relocating it (verified **zero external references**) plus a `SmartInitializingSingleton` startup assertion over the deployed handler surface. ⚠ **The endpoint count was wrong three times** (planner 77, me 67, critic 66 — **66 is right**), so the golden map is built from reflection, not a number. ⚠ **The optional cache was dropped, not deferred** — invalid SpEL, a key resolving to an identity hash because `TenantProfile` has no `toString()` (its own test would have gone green over a useless key), and self-invocation killing the proxy. ⚠ **`AdminController` inheritance**: all 10 mobile controllers extend it, re-exposing its 13 methods under every prefix — so **SBDEV-2870's five ungated endpoints have ~11 URLs each**; reported there, deliberately not gated here. Also found: `pages/not-authorized.vue:20` references `layout: "splash"` and **no `splash.vue` exists in the mobile repo**. Rollout is **hard-on at deploy, no feature flag (user decision, reaffirmed)** — so §5.1 carries 8 blocking prerequisites incl. a per-tenant Keycloak-joined audit. **Verify script negative-tested: `12 pass, 76 fail, 5 skip` on the unfixed tree**, and two false greens were found and fixed by running it (`F4` passed purely on `AdminController`'s 8 pre-existing `IS_SB_ADMIN` gates; `C12` was vacuous). ⚠⚠ **RE-SCOPED 2026-08-17 after SBDEV-2870 PR #166 — TDD gate HELD; see the new §14.** Four deltas: (Δ1, **new scope**) the plan said `X-Authz-Denied` "is already emitted by SBDEV-2870's damaged-lock gate" and is merely adopted here — **it is not**; that gate, the constant and the CORS entry were reverted from 2870 and re-homed in **SBDEV-2967, which lands AFTER this plan**, so the first-landing plan was waiting on the second. Verified on `989611e`: no `X-Authz-Denied` in `src/main/`, and `corsConfigurationSource` is byte-identical to develop. **New §3.1-A2b makes this plan the owner** of the constant + emitter + CORS exposure — correct on the merits, since the interceptor writes its own denial response while 2870's `@PreAuthorize` gates would have needed an `AccessDeniedHandler` that does not exist. (Δ2) `AdminController` **13→9** mapped methods / **~130→~90** aliases as #166 extracts a standalone `UserAdministrationController`; design untouched because `findUsers` **stayed**, so the alias fixture and the audit instrument survive. (Δ3) `IS_WMS_ADMIN` was deleted as dead — the CSV import is on `IS_SB_ADMIN`. (Δ4) **#166 is NOT merged** (`origin/develop` head `2be4ea5`), so the rebase order is undecided and the golden map must be built after it — new P11. ⚠ **Running the updated verify script found a defect in the correction itself:** 2870 §3.5.1-5 says `SecurityConfigurationTest` "asserts the exposed list exactly"; only **one of its two** cases does (`…doesNotDuplicateHeader…` uses `containsExactly`, `…exposesSkippedCycleCountHeader…` uses `contains`) — taking it at face value would have meant relaxing the wrong assertion. Verify script now **91 rows, re-negative-tested: `16 pass, 87 fail, 5 skip`** (6 of 8 new A2b rows correctly red; the 4 that pre-pass are annotated `[pre-passes]` as regression pins). ✅ **BASE RESOLVED 2026-08-17 (§14.3): PR #166 merged (`27e2f21`) and this worktree fast-forwarded onto it** — the branch had no commits of its own so nothing was replayed; **P11 closed**, and Δ2's counts are now *observed* on develop (AdminController = 9 methods, `UserAdministrationController` present, Flyway head still V2.2.16). Verify baseline unchanged at `16/87/5`, which is the expected result and itself a check — a shift would have meant a row was coupled to something it had no business reading. ⚠ **M23 rewritten 2026-08-17 (§14.4) — the `curl` evidence was inadmissible.** It was drafted twice as a cross-origin `curl -H 'Origin: …'`, but **`curl` applies no CORS policy**, so it reads `X-Authz-Denied` whether or not the header is exposed — a green `curl` is fully compatible with the exact browser-blind failure P10 exists to prevent, and the DevTools Network panel has the same blind spot (CORS restricts what *JS* may read, not what tools display). M23 is now a **local browser test**: under-privileged login → gated tile → assert the denial renders, **no logout**, and `headers.get('x-authz-denied')` is non-null from JS. A credential-free variant was **dropped, not deferred** — the programmatic `addExposedHeader` is override-proof by construction, so it would have been a row incapable of failing. Load-bearing and verified: `nuxt.config.js` has **no `@nuxtjs/proxy`** and calls `:8088` from `:3001`, so local IS cross-origin — **adding a dev proxy would make this whole defect class invisible locally.** P10's evidence chain now runs on a dev machine; the one human input is a `CS-REP`-shaped account. ⚠⚠ **§3.1-A2b STILL HAS NO REVIEW — two dispatched reviewers returned idle with no report, twice (§14.5); nothing was accepted from them.** A mechanical self-audit was run in their place and found **2 real defects in the new rows**: `A31` passed on the unfixed tree while **unlabelled** (a green unlabelled row reads as progress — the exact misreading the `[pre-passes]` convention exists to prevent, reintroduced in the commit that introduced it), and 🔴 **P10's client half had NO row at all** — `H27` asserted only that a *spec* mentions the header while **nothing asserted `plugins/axios.js`**, so the script could have reported `0 fail` while every denial still logged the operator out. Fixed via new **`H28`**; script now **92 rows, `16 pass, 88 fail, 5 skip`**. **The self-audit does not substitute for review** — it covered only what is mechanically checkable, and the A2b *judgment* calls (right owner vs a standalone prereq PR; the `containsExactly` extension; §14.4's instrument argument; the dropped credential-free check; R13's prose mitigation) remain one-author, zero-reviewer. P1–P8 also remain open. **Self-contained review brief written 2026-08-18: [REVIEW-BRIEF-a2b-header-contract.md](REVIEW-BRIEF-a2b-header-contract.md)** — five decisions, nine pre-verified code facts (F1–F9) so the reviewer re-derives nothing, and an explicit record of the author's five errors on this material as calibration. Next: **get a real review of A2b → TDD gate**.*

- [SBDEV-2870-ungated-user-admin-and-damaged-lock-endpoints.md](SBDEV-2870-ungated-user-admin-and-damaged-lock-endpoints.md) — ***RETROACTIVE plan; code written on an unmerged branch, 3 of 6 ACs met, NOT shippable.*** *URGENT; `v2/wms2-api` only. **Seven endpoints performed privileged operations behind nothing but the `/v3/**` → `wms_user` floor** — five manage Keycloak identities (incl. `importUsersFromCsvText`, bulk user creation from a CSV string) and two place stock on a damaged lock. All seven now gated on branch `bugfix/SBDEV-2870-restrict-csv-user-import-to-wms-admin`. **`wms_admin`, not `sb_admin`**, per the target state (role matrix §1.1) — which also dissolved the ticket's original blocker, since `sb_admin` would have 403'd the User Management screen for every customer admin. **`db_verified: true`** — only `super-admin` holds `WEB_UI_ACTION_ADJUST_LOCK_DAMAGED` on WineCo dev, so the damaged-lock gate removes that capability from ~13 of 52 live users incl. all 4 `CS-REP`. ⚠⚠ **THIS PLAN IS RETROACTIVE AND §11.2 RECORDS WHAT THAT COST** — a one-annotation request grew to two authorization mechanisms, three new constants and a global CORS change without a plan or verify script; each open item maps to a section that did not exist while the code was written. **§0.B is the section whose absence cost most:** enumerating every writer of `QUALITY_FAULT` reveals **four** routes to a damaged lock, not two — and the fourth (`PATCH /v3/stockunit/{id}` via Spring Data REST) is still open, so the *route* is gated but the *capability* is not (§10.2, the SBDEV-1666 landmine class). ⚠ **The 5 `@PreAuthorize` gates are UNTESTABLE by construction** — `standaloneSetup` installs no method-security advisor and SBDEV-2217 blocks the `@SpringBootTest` lane, which is exactly how SBDEV-2863 shipped broken for 9 months; bytecode inspection is not a test, so AC-5 reduces to a manual curl matrix (§6.3) that has **not** been run. ⚠ **Top blocker (§10.1/AC-4): no `wms2-web-ui` change, and the claim that `wms_admin` won't 403 User Management is an unverified assumption about Keycloak group membership** — on a different axis from the function that gates the screen, the SBDEV-2947 shape. Since `store/admin/user.js:175,193` fires on screen entry and axios turns a 403 into `$kc.logout()`, an admin holding the function but not the group is **logged out just by opening the screen**. **One independent review: DON'T SHIP** — 3 findings fixed (a vacuous once-per-request test; `X-Authz-Denied` inert without CORS `Access-Control-Expose-Headers`, which would have made both UI plans' retry fix dead on arrival; the `wms_admin` literal), 4 open (§10.1–§10.4). One review claim corrected: the SDR path IS behind `wms_user` because `RestConfiguration:24,32` sets the base path programmatically. **Verify script negative-tested BOTH directions — 8 pass/32 fail pre-fix, 40 pass/0 fail post-fix — and running it found 2 defects of my own:** `B9` was satisfied by a **commented-out** `// @PreAuthorize` (the very defect the ticket exists to fix), and `C7` used a zero-width window with pattern `'.'`, asserting only that the method existed. Full suite 5119 run, 2 failures, both pre-existing on `develop`. Spawned [SBDEV-2979](https://app.clickup.com/t/868kt336b). ⚠ **This entry's opening verdict is superseded — updated 2026-08-17.** The plan is now **PR submitted: [wms2-api #166](https://github.com/SiteBossInc/wms2-api/pull/166) (`989611e`), 6 of 7 ACs met, independently code-reviewed** — not "3 of 6, NOT shippable." The damaged-lock pair moved to **SBDEV-2967 Fix E**, and the `X-Authz-Denied` constant + CORS exposure to **SBDEV-2968 §3.1-A2b** (see §11.4's correction — 2968 lands first and needs them). #166 is **open, not merged**; `origin/develop` head is `2be4ea5`. Residual: AC-5 (one curl for the single `@PreAuthorize` endpoint) and AC-7 wording, with identity creation still open as [SBDEV-2984](https://app.clickup.com/t/868kt73f9). Next: **merge #166 (SBDEV-2968 §5.1-P11 recommends it goes first), then run the AC-5 curl**.*

- [SBDEV-2994-move-stock-unknown-destination-container-generic-error.md](SBDEV-2994-move-stock-unknown-destination-container-generic-error.md) — ***REVIEWED 2026-08-18 across TWO ralplan consensus rounds (5 independent reports, preserved verbatim in `reviews/`) — gate-ready for Fixes A/B/C; Fix D held on the §12 Q5 product decision.***  *NORMAL; `v2/wms2-api` (primary) + `v2/wms2-mobile-ui` (parity). Reported by Zeshan on WineCo dev: mobile Move Stock → Scan Destination Container answers every failure with **"Error: Request failed due to a network or server issue. Please retry."** — a message that is both wrong and unactionable. **`db_verified: true`** — the scanned `UL314581` was `SEND_TO_NIRWANA`'d 2026-06-01 by `panderson`, so `sendToNirvana` (`UnitloadBusinessService:397`) renamed the row to `UL314581-X-21552195` at Nirwana with `entity_lock=2`; there is **no** row with the scanned label. `findByLabelid` misses → `EntityNotFoundException` (unchecked) **escapes the controller's `catch (BusinessException|FacadeException)`** at `StockUnitController:96-102` → 404 → axios rejects → the blanket toast. The `results.errors` branch that would have shown a real message is never reached. **No data corruption** (first statement in the branch, `@Transactional`; re-confirmed by SQL). ⚠ **Not a regression from the Phase-5 null-safety sweep** — pre-sweep it was `NoSuchElementException` → 500 → the same toast; the sweep preserved the UX gap mechanically. **Two in-repo references make the design almost self-writing:** `MobileMoveUnitloadService:126-130` already throws `BusinessException` for the identical miss on the Move Unit Load screen, and **`bulkTransferStock` 50 lines below already catches `EntityNotFoundException`** (`StockUnitController:165-167`) — the single-transfer endpoint is the outlier, not the bulk one. ⚠ **The desktop is already protected and mobile is not**: `wms2-web-ui .../popups/transferStock.vue:140-146` pre-validates via `GET /stockUnit/isUnitLoadIdValid`; the mobile screen validates only that the field is non-empty. That parity gap is why this landed on handhelds. **The load-bearing decision is the SPLIT, not a blanket conversion** (§5.4): operator-/client-supplied identifiers (2 sites) → keyed `BusinessException`; internally-derived FKs, constants and sysprops (11 sites) → keep `EntityNotFoundException`, because "your scan is wrong" is a lie for a broken FK. `wms-exception-taxonomy.md`'s decision tree matches **both** branches here and never wrote the discriminator down — plan step 9 fixes the doc. ⚠ Keyed ctor, **not** the 1-arg `BusinessException(String)`, which silently sets `key="placeholder"` (`BusinessException:42-47`) so `getKey()` cannot discriminate — tests assert `getKey()`, never rendered copy. New keys go in **both** bundles; `messages_en_US.properties` alone is the documented locale trap. **Verify script negative-tested: `4 pass, 21 fail, 1 skip` on the unfixed tree** — and running it found **2 defects in my own script**: the A3 literal is `UnitLoadType` (capital L), not `UnitloadType`, so the row false-FAILed; and P3 was a file-scoped `file_not_contains recalculateForItem`, which is **permanently red** because that call is legitimate at `StockunitService:127` in a *different* method — a grep cannot say "absent from THIS method", so it now pins the SBDEV-2033 guard comment and leaves the behavioural proof to the existing `StockunitServiceTransferStockGuardTest`. Cross-ref: SBDEV-2968 §R12 already flags `/v3/stockUnit/transferStock` as one of five shared endpoints the mobile UI reaches. **§12 Q1/Q2/Q4 all resolved 2026-08-18** — Fix D (mobile pre-validation) is IN, the `isUnitLoadIdValid` probe gains a usability check **and** the desktop toast is reworded to "Container is not available to receive stock" (so `wms2-web-ui` joins the scope for one line), and **R3 is cleared by measurement**: zero `stockrecord` rows targeting `Nirwana`/`Shipped` in 365 days and zero manual moves into an `entity_lock IN (2,104)` destination, on WineCo dev **and** Hydra UAT — so Fix B keeps its full breadth. Worth keeping in view: **A+C alone fix the reported symptom with zero UI changes**, because the server then answers `200 {errors:[…]}` and `store/moveStock.js:171` already renders it; D and the probe change are parity and defence in depth. Only Q3 (the ~18-module blanket-toast sweep) stays open, and it blocks nothing. **Review cost: 22 findings in round 1, 21 in round 2 — including FIVE false claims of mine** (a fabricated 210,167-row population that was really 1; "orElseThrow cannot throw a checked exception", with 34 counter-examples in-repo; an `errors[0].type` field that does not exist — the key is `field`; "bulkTransferStock unchanged" when its catch sits OUTSIDE the loop; and an R3 clearing whose ON_HOLD half *could not fail* because zero such rows exist). **Two findings were split out: [SBDEV-2995](https://app.clickup.com/t/868ktvc2h) (high — `transferStock` accepts the `Nirwana` sentinel as a destination and the stock silently disappears; confirmed on two tenants) and [SBDEV-2996](https://app.clickup.com/t/868ktvc9j) (`MobileMoveStockService.selectDestination` is a second, better-guarded implementation of this very screen, reachable at `POST /v3/moveStock/scanDestination`, whose mobile action is DEAD CODE — the architectural root cause, missed because §0's grep was scoped to two URL fragments).** Fix B was reshaped after measurement killed two of its four branches (`ON_HOLD` = 0 rows tenant-wide; 320,642/320,642 To-Delete ULs are `-X-` mangled so Fix A always fires first) — it now leads with **Shipped** (411,862 ULs), uses an allowlist over an 8-member enum, extracts a shared `DestinationEligibilityService`, and ships **sysprop-gated default OFF with shadow logging** — except the Nirvana refusal, which is deliberately **ungated** because it is SBDEV-2995's data-loss path. ⚠⚠ **The verify script needed THREE revisions and self-testing found eleven of its defects.** Round 1's "4 pass, 21 fail" baseline looked healthy; reviewers then built near-miss wrong implementations and **up to six scored identically to correct**, including an EMPTY STUB whose branch names lived only in `// TODO` comments. Worst single defect: `B12`/`B13` were wired into the runner but **never defined** (an edit aborted before writing), so bash's 127 read as an ordinary FAIL, `0 fail` was unreachable, and the ungated-Nirvana invariant had zero verification. **Lesson now written into §8.7: red-only baselining proves nothing; a correct shadow catches false-REDs; only a family of near-miss WRONG shadows catches false-GREENs** — and two defects (`A4` vacuous at baseline, `method_slice` silently broken by a comment on a backslash-continuation line) were invisible to both reviewers and visible only from the correct shadow. Baseline now **4 pass / 44 fail / 1 skip**, validated both ways. Next: **answer §12 Q5 → `wms-tdd-gate` for A/B/C**.*

> This list can drift. Verified against disk 2026-08-09: `SBDEV-2731` exists on disk but is absent above (`SBDEV-2632` was ARCHIVED 2026-08-09 and is no longer on disk here) (`SBDEV-2732` **is** listed, at the entry above `SBDEV-2736` — the earlier note claiming otherwise was itself stale). `SBDEV-2802` added 2026-08-03, `SBDEV-2781` added 2026-08-05, `SBDEV-2854` added 2026-08-06, `SBDEV-2930` added 2026-08-12, `SBDEV-2931` added 2026-08-12, `SBDEV-2947` added 2026-08-13, `SBDEV-2952` added 2026-08-13, `SBDEV-2961` added 2026-08-14, `SBDEV-2962` added 2026-08-14. ⚠ `SBDEV-2951` exists on disk (created 2026-08-13, PRs open) but is **absent from this list** — add it. The Dataview section below is always authoritative in Obsidian; elsewhere run `ls sbdocs/1-Projects/wms2/plan/*.md`.

---

## Dataview — by priority

```dataview
TABLE
  ticket AS "Ticket",
  priority AS "Priority",
  status AS "Status",
  scope AS "Scope",
  updated AS "Updated",
  owner AS "Owner"
FROM "1-Projects/wms2/plan"
WHERE type != "index"
SORT priority ASC, updated DESC
```

## Dataview — ported from v1

```dataview
TABLE related AS "v1 origin", updated AS "Updated", status AS "Status"
FROM "1-Projects/wms2/plan"
WHERE type != "index" AND related AND any(related, (r) => contains(string(r), "wms1"))
SORT updated DESC
```

## Dataview — by scope

```dataview
TABLE rows.file.link AS "Plan", rows.status AS "Status"
FROM "1-Projects/wms2/plan"
WHERE type != "index"
GROUP BY scope
```

---

## Conventions specific to v2 plans

- **`version: v2`** is required.
- Plans ported from v1 include a `related:` entry pointing at the v1 plan (either active at `../../wms1/plan/...` or archived at `../../../4-Archieves/wms1/plan/...`).
- Architectural divergence from v1 (Java 8→21, Jakarta namespace, `tenantTransactionManager`, extracted services) means **do not cherry-pick** v1 commits. Port via the `wms-v2-migrate` skill and write the plan here.
- When complete, **move** (do not copy) the file to `../../../4-Archieves/wms2/plan/` and set `status: archived`.
